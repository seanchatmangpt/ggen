## Requirements *(mandatory)*

### Functional Requirements

{% for row in sparql_results %}
{%- set requirementId = row.requirementId | default(value="") -%}
{%- set description = row.description | default(value="") -%}
{%- set category = row.category | default(value="") -%}
- **{{ requirementId }}**{% if category %} ({{ category }}){% endif %}: {{ description }}
{%- endfor %}

### Key Entities

{% for row in entities %}
{%- set entityName = row.entityName | default(value="") -%}
{%- set definition = row.definition | default(value="") -%}
{%- set keyAttributes = row.keyAttributes | default(value="") -%}
- **{{ entityName }}**: {{ definition }}
  - Key attributes: {{ keyAttributes }}
{%- endfor %}
