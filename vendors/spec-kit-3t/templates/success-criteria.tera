## Success Criteria *(mandatory)*

### Measurable Outcomes

{% for row in sparql_results %}
{%- set criterionId = row.criterionId | default(value="") -%}
{%- set description = row.description | default(value="") -%}
{%- set measurable = row.measurable | default(value="false") -%}
{%- set metric = row.metric | default(value="") -%}
{%- set target = row.target | default(value="") -%}
- **{{ criterionId }}**{% if metric and target %} ({{ metric }}: {{ target }}){% endif %}: {{ description }}
{%- endfor %}
