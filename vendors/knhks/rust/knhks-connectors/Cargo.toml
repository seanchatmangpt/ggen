[package]
name = "knhks-connectors"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]
path = "src/lib.rs"

[dependencies]
# For no_std compatibility, we'll use core types
# alloc is available via extern crate alloc when needed

[dev-dependencies]


