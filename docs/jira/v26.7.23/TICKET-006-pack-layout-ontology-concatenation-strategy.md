# TICKET-006: Pack layout + ontology.ttl concatenation strategy

## Status

PLANNED

## Parent

EPIC: InterviewAssist v26.7.23

## Source

- RDF resources:
  - `packs/wasm4pm-interview-assist-pack/ontology/*.ttl`
- PRD requirements: (none)
- ARD components: (none)
- Acceptance-test steps: (none)
- Policies: (none)
- SHACL shapes: (none)

## Objective

Resolve, honestly, the gap between the 9-file ontology/*.ttl layout this pack uses and the confirmed fact that crates/ggen-engine/src/pack.rs only auto-loads a single root ontology.ttl — before any ggen sync run is attempted against this pack.

## Current state

BLOCKED — the 9-file layout will not be auto-loaded by `ggen sync run` as-is; this is a disclosed, deferred concern from the prior TTL-authoring phase, not yet resolved.

## Target state

Either (a) a deterministic, order-preserving concatenation step producing a single root `ontology.ttl` from the 9 source files (source files remain the edited truth, concatenation is a build step, not a hand-edit target), or (b) `extra_ontologies` wired in the CONSUMING project's ggen.toml if that mechanism suits better — decision recorded with justification, not assumed.

## Projection classification

- Template: 95%
- Domain data: admitted RDF input under packs/wasm4pm-interview-assist-pack/ontology/ — not counted as template or custom code
- Custom code: 5%
- Expected ratio: 95/5
- Custom-code justification: the concatenation script is generic; 5% covers verifying the ggen-engine's actual auto-load behavior against the live binary rather than trusting documentation.

## Inputs

- packs/wasm4pm-interview-assist-pack/ontology/*.ttl
- crates/ggen-engine/src/pack.rs (ground truth for auto-load behavior)

## Outputs

- packs/wasm4pm-interview-assist-pack/ontology.ttl (generated by concatenation, OR) consuming project's ggen.toml extra_ontologies entries
- scripts/build-pack-ontology.sh

Projected files are canonical source and must not be placed under `generated/`.

## Template responsibility

A generic 'concatenate N Turtle files into one, preserving all prefixes without collision' script — reusable by any future multi-file pack.

## Domain-data responsibility

The 9 source files remain the hand-edited domain truth; the concatenated ontology.ttl is a build artifact, never hand-edited.

## Custom-code boundary

NONE beyond verifying live engine behavior (read-only investigation, not new runtime code).

## Exclusions

- no hand-editing of the concatenated ontology.ttl output
- no domain data invented during concatenation
- no `generated/` directory — the build artifact lives at the pack root per this repo's own convention

## Implementation steps

1. Re-verify crates/ggen-engine/src/pack.rs's auto-load behavior against the current engine source (not memory from a prior session).
2. Decide concatenation vs extra_ontologies based on that verification.
3. If concatenation: write a script that reads the 9 files in the fixed order from TICKET-001's manifest, merges @prefix declarations without duplication/collision, and emits one ontology.ttl.
4. Verify the concatenated file still parses and still passes the TICKET-002 SHACL gate.
5. Verify triple count of the concatenated file equals the union count already established (1,789 pre-fix / 1,789+13 post schema:name fix).

## Admission gates

- TICKET-001, TICKET-002 complete.

## Acceptance criteria

- Given the 9 source files, when the concatenation script runs, then the output ontology.ttl parses and its triple count matches the independently-computed union count exactly.

## Negative tests

- Introduce a duplicate @prefix with a conflicting namespace URI across two source files and confirm the concatenation script fails loudly rather than silently picking one.

## Verification ladder

- Unit: prefix-collision-detection unit test
- Integration: full 9-file concatenation against the real corpus
- End-to-end: N/A with reason — no ggen sync run yet, that's TICKET-010
- Chaos: N/A with reason — single deterministic build step
- Stress: N/A with reason — small corpus
- Benchmark: N/A with reason — no perf target
- Verifier report: concatenation output triple-count match against independent union count

## Receipts

- concatenated ontology.ttl hash
- triple-count match confirmation

## Dependencies

- TICKET-001
- TICKET-002

## Falsifier

If ggen-engine's actual source code shows a different auto-load mechanism than assumed (e.g. it DOES glob ontology/*.ttl in a version newer than last checked), this ticket's decision must be redone against that evidence, not the assumption carried from the prior session.

## Handoff

TICKET-009 (gate integration) and all later `ggen sync run` invocations depend on this ticket's resolved layout.

## Definition of done

- live engine behavior re-verified, not assumed
- concatenation script (or extra_ontologies wiring) exists and produces a verified-correct single-file ontology
- decision and justification documented
