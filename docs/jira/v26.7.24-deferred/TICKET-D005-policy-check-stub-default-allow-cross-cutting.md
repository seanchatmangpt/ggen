# TICKET-D005: policy-check-stub.ts default-allow placeholder is called by 5 of 6 workstream-H adapters

## Status

DEFERRED — permanent structural gap until TICKET-028 lands, cross-cutting across TICKET-034/035/036/037/039

## Re-verification (2026-07-24)

**Partially addressed this session — real, not narration, but not fully closed.** TICKET-028's
real generated policy check landed: `examples/interview-assist/lib/domain/policy-check.ts`
(header: "GENERATED by ggen sync from ... 50-policy.ttl via queries/policy-permissions.rq") is a
real RDF-driven `checkPolicy(action, activeMode): "allowed"|"denied"|"unspecified"` over the 6
admitted `policy/*` odrl:Sets. A real wrapper,
`examples/interview-assist/lib/adapters/policy-check-adapter.ts`, maps each adapter-local
capability id to an `authority-action/*` resource and normalizes the result.

`grep -n "^import.*policy-check" lib/adapters/*.ts` (re-run 2026-07-24) shows 4 of the 5
originally-named call sites now import the real check, not the stub:

| Ticket | File | Wired to |
|---|---|---|
| TICKET-034 (Monaco) | `monaco-adapter.ts` | real `policy-check-adapter` |
| TICKET-035 (sandbox) | `sandbox-executor.ts` | real `policy-check-adapter` |
| TICKET-036 (persistence) | `persistence-adapter.ts` | **still** `policy-check-stub` (default-allow) |
| TICKET-037 (Ollama) | `ollama-adapter.ts` | real `policy-check-adapter` |
| TICKET-039 (Playwright/a11y) | `accessibility-platform-adapter.ts` | real `policy-check-adapter` |

`tests/adapters/sandbox-executor.test.ts` now has a real test proving this isn't cosmetic: under
`policy/authority-broker-default`, `execute-code` carries both a permission and a prohibition in
the real `50-policy.ttl` data, the real checker resolves prohibition-wins, and the test asserts
the call is denied with zero subprocess spawned (verified against a live `ps ax` scan for a
unique marker string) — this is a real policy-check exercising real conflicting RDF data, not the
old always-allow stub.

`persistence-adapter.ts` remains on `policy-check-stub.ts`'s default-allow, and so does
`checksum-adapter.ts` (TICKET-038, not one of this ticket's original 5, but sharing the same
stub). This is a disclosed, reasoned decision, not an oversight: `policy-check-adapter.ts`'s own
module doc records that no capability either adapter touches is linked to any `authority-action/*`
resource in the current RDF graph (checked via `grep -n 'dcterms:requires' 30-capabilities.ttl`
and `grep -c 'authority-action/retain\|authority-action/export' 50-policy.ttl` → 2, neither
appearing in any `odrl:permission`/`odrl:prohibition` statement) — force-mapping would fabricate a
policy edge the ontology does not state.

Net: this ticket's own Definition of done ("all 5 adapters rewired... policy-check-stub.ts
deleted... `grep -r policy-check-stub` returns zero hits") is **not yet met** —
`grep -rn "from \"./policy-check-stub\"" lib/adapters/` still returns 2 hits
(`persistence-adapter.ts`, `checksum-adapter.ts`). But the specific risk this ticket names
("every adapter's policy-enforcement point is a default-allow stub") is now false for 4 of the 5
originally-scoped adapters — a real, load-bearing partial close.

## Priority

P0 — every adapter's policy-enforcement point is a default-allow stub; Architecture Decisions 10 (undisclosed assistance prohibited) and 11 (every actuation is authorized) are not actually enforced by any adapter until this closes

## Parent

EPIC: InterviewAssist v26.7.24-deferred
Relates to: `docs/jira/v26.7.23/TICKET-034-monaco-runtime-adapter.md (and identically TICKET-035, TICKET-036, TICKET-037, TICKET-039)`

## Source

- Research report: Disclosed Gaps Catalog, section (a) Permanent structural limitations
- Citation: "Policy-check-before-action: `buildMonacoConfig` calls `checkPolicy(...)` from `policy-check-stub.ts` before returning a config; TICKET-028's real generated check does not exist yet, so this currently calls a documented default-allow placeholder, not a real domain decision — the call-site wiring is real, the check body is PENDING(TICKET-028)." — `docs/jira/v26.7.23/TICKET-034-monaco-runtime-adapter.md:139-142`
- Citation: "Same PENDING(TICKET-028) default-allow placeholder pattern is disclosed identically in TICKET-035:146-150, TICKET-036:142-143, TICKET-037:145-146, TICKET-039:149-150 — a cross-cutting, currently-true gap: no adapter has a real policy check yet, all defer to policy-check-stub.ts's default-allow." — Disclosed Gaps Catalog

## Objective

Land TICKET-028's real precondition/policy/authority check and rewire all 5 currently-stubbed call sites (TICKET-034/035/036/037/039) from `policy-check-stub.ts`'s default-allow to the real generated check, so policy enforcement is actually live, not merely wired to a placeholder.

## Current state

Each of TICKET-034 (Monaco), TICKET-035 (sandbox), TICKET-036 (persistence), TICKET-037 (Ollama), and TICKET-039 (Playwright/a11y) calls `checkPolicy(...)` before acting — the call-site wiring is real and structurally correct — but the check body is `policy-check-stub.ts`'s documented default-allow placeholder, because TICKET-028 (the real generated policy check) has not landed. No adapter currently enforces a real policy decision.

## Target state

All 5 adapters call TICKET-028's real generated policy/precondition/authority check; `policy-check-stub.ts` is deleted or reduced to nothing (no adapter imports it), and a policy denial actually blocks an action rather than merely being structurally positioned to do so.

## Projection classification

- Template: 85% — matches TICKET-028's own classification for the check itself; this ticket is the rewiring half
- Domain data: the policy rules themselves are already admitted RDF (`ontology/50-policy.ttl`), consumed by TICKET-028
- Custom code: 15% — 5 call-site rewires from stub import to real generated-check import, no new domain logic

## Inputs

- TICKET-028's generated policy/precondition/authority check (once it lands)
- the 5 existing adapter files' `checkPolicy(...)` call sites

## Outputs

- TICKET-034/035/036/037/039's adapter files, each rewired to import TICKET-028's real check instead of `policy-check-stub.ts`
- `policy-check-stub.ts` deleted once zero import sites remain (see D006 for the orphan-file tracking of this same file)

Projected/authored files, once this ticket is picked up, are canonical source and must not be placed under `generated/`.

## Template responsibility

TICKET-028's check itself is generated (workstream F); this ticket only rewires 5 existing call sites to point at it.

## Domain-data responsibility

None new — the policy rules are already fully specified in `ontology/50-policy.ttl`; this ticket makes the existing wiring actually consult them.

## Custom-code boundary

The call-site import swap is trivial custom glue, not a domain-rule reimplementation — must remain a straight substitution, never a parallel policy check invented in the adapter.

## Exclusions

- no adapter independently reimplementing a policy decision instead of calling TICKET-028's real check
- no partial rewire — all 5 call sites must be updated together, since a partially-rewired state would let policy enforcement silently vary by adapter

## Implementation steps

1. Confirm TICKET-028 has landed and its real check function signature matches what the 5 stub call sites expect (or reconcile a signature mismatch first).
2. Rewire TICKET-034's `checkPolicy(...)` import from `policy-check-stub.ts` to TICKET-028's real check.
3. Repeat for TICKET-035, TICKET-036, TICKET-037, TICKET-039 in the same pass (a partial rewire is worse than none — it would make enforcement inconsistent across adapters).
4. Re-run each adapter's existing negative test (e.g. TICKET-035's 'attempt an action the policy check would deny' test) and confirm it now exercises a REAL denial, not the stub's default-allow.
5. Delete `policy-check-stub.ts` once `grep -r policy-check-stub examples/interview-assist/lib/adapters` returns zero hits.

## Admission gates

- TICKET-028 (must be ALIVE before this ticket can close)

## Acceptance criteria

Given a real action that TICKET-028's policy graph would deny, when any of the 5 adapters attempts it, then the adapter refuses before performing the real action, using TICKET-028's real check — not the default-allow stub.

## Negative tests

For each of the 5 adapters, construct a request that TICKET-028's real policy graph denies and confirm the adapter refuses before acting; re-run the SAME negative-test scenario that currently passes only because the stub allows everything, and confirm it now exercises real denial logic (not merely that the test still passes for the same reason as before).

## Verification ladder

- Unit: per-adapter policy-check-before-action unit test against the real TICKET-028 check
- Integration: all 5 adapters composed with TICKET-028's real check in one test run
- End-to-end: exercised via the relevant workstream I vertical scenarios (esp. TICKET-046 hidden tests, TICKET-052 self-play, which depend on real precondition/policy enforcement)
- Chaos: N/A with reason — not a runtime-fault scenario
- Stress: N/A with reason
- Benchmark: N/A with reason
- Verifier report: 5 real-collaborator denial-transcript tests, one per adapter

## Receipts

- per-adapter rewire diff
- 5 real denial-test transcripts
- confirmation `policy-check-stub.ts` has zero remaining importers

## Dependencies

- TICKET-028
- TICKET-034
- TICKET-035
- TICKET-036
- TICKET-037
- TICKET-039

## Falsifier

If any of the 5 adapters still imports `policy-check-stub.ts` after this ticket claims done, or if a policy-denied action is still permitted by any adapter, this ticket is not complete.

## Handoff

D006 (orphan-file tracking of policy-check-stub.ts) closes naturally once this ticket deletes the file.

## Definition of done

- TICKET-028 landed
- all 5 adapters rewired
- policy-check-stub.ts deleted
- 5 real denial tests pass
