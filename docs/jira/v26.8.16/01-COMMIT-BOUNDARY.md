# 01 — Commit Boundary: Land the 2026-08-16 Sweep

Part of [00-OVERVIEW](00-OVERVIEW.md). Highest priority: every other ticket assumes the
sweep's changes survive, and today they exist only in two uncommitted working trees.

## ostar side

`git status --short -- . ':!vendors' | grep -v '^ D' | wc -l` → **62** modified/untracked
files on `fix/ostar-round3-critical`. Content falls into four commit-sized groups:

1. **Restoration + import repairs** — `src/ostar/manufacturing/` back from
   `_deprecated_manufacturing/`, `ofmf/_pyshex_compat.py` + `_typing_io_shim.py`,
   `metrics/pipeline.py`, `process/codegen/conformance_validator.py` (real pm4py
   token-replay replacing dangling `get_dteam_client` calls), `documentation/` modules.
2. **Generated-file repairs + generator fix** — the five ex-SyntaxError files
   (`ontology_models.py`, `ontology_properties.py`, `mcp_tool_registry.py`,
   `codemanufactory_shapes.py`, `governance/roberts_rules.py`), `unrdf/sync.py`
   front-matter parsing, `unrdf.toml` (directory ontology source + namespace fix),
   `templates/mcp-tools.njk`.
3. **Proof honesty** — `vision_2030_e2e_proof.py` (de-faked, retargeted at `ggen-engine`
   and `praxis-graphlaw`'s `chatman_acceptance_admission`; hardcoded Groq key removed —
   verify the key is not reintroduced by any commit), `tests/process/
   test_wvda_agent_placeholders.py` (blocked on [03](03-WVDA-TEST6-FALSE-DOCSTRING.md)).
4. **New entrypoints + deps** — `bridge.py`, `process/ontostar_planner.py`,
   `mu_star_agent.py` CLI, `wvda_agent.py` MCP mode, `pyproject.toml` (pynacl), `uv.lock`,
   `.mcp.json`.

Also present: the `tools/truth-gate/target/` and worktree deletions from the pre-existing
checkpoint — commit or `.gitignore` them deliberately, do not let them ride along
unexamined.

## ggen side

`git status --short` → 3 entries:

- `?? examples/goat-capabilities-verify/` and `?? packs/goat-capabilities-pack/` — real
  finished pack + consumer pair (84K + 28K) with a receipt chain; `pack.toml` honestly
  discloses its one unchecked gap (precondition/effect topology fidelity). Commit as-is;
  the disclosed gap becomes a follow-up ticket, not a blocker.
- `M .cargo-cicd/ocel/chatman/admission_table.ocel.json` — regenerated by the real
  `chatman_acceptance_admission` run during the sweep; legitimate evidence artifact.

## Acceptance

- Both trees: `git status --short` clean (modulo intentionally ignored paths).
- No `git reset`/force operations; fix-forward commits only, conventional-commit format.
- Post-commit re-verification (fresh runs, not memories):
  `.venv/bin/python`-driven import sweep 466 OK / 0 FAIL, `python -m ostar.cli --help`
  exit 0, `vision_2030_e2e_proof.py` passed=5, `cargo check --workspace` clean.

## See Also

- [03-WVDA-TEST6-FALSE-DOCSTRING](03-WVDA-TEST6-FALSE-DOCSTRING.md) — fix before
  committing group 3, or commit that file separately after
