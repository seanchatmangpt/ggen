# v26.8.7 Status — ggen

Real, evidence-based status as of 2026-08-07. **No roadmap, checklist, or task list
document names specific remaining work items for v26.8.7 anywhere in this repo** —
`RELEASE_STANDING.md`/`docs/PUBLICATION_JUDGMENT.md` (see below) define release
readiness as a *gate condition* (CI passes + publication conditions hold), not an
enumerated backlog. This document records objective state so that gate can be
evaluated honestly; it does not invent tasks.

## Version state

- Local `Cargo.toml`: `26.8.6`.
- `origin/main` HEAD (`e884d1fbb`, PR #587, `github-actions[bot]`): bumped to
  `26.8.7`. Pure version-law commit — touches only version-string fields
  (`Cargo.toml`, `crates/ggen-engine/Cargo.toml`, `ggen.toml`, `README.md`,
  `.specify/repo-facts.ttl`, `Cargo.lock`, the receipt lane) plus a real
  receipted `ggen sync run`. Not yet pulled into this checkout.
- **No git tag exists for `v26.8.6` or `v26.8.7`.** `.github/workflows/release.yml`
  (the actual artifact-publishing gate) only triggers on a pushed `v*` tag —
  it has never run for either version.

## Branch state

- Current branch: `chore/ci-errc-sccache`, `cc163b244` (identical to local `main`,
  one commit behind `origin/main`'s version bump).
- Real uncommitted work in this working tree as of this pass (`git status --short`):
  - Staged deletion: 5 `foundry-historical-lineage-clean-room*.yml` workflow files.
  - Modified: `.github/CI_CONVENTIONS.md`, `.github/actions/setup-rust-cached/action.yml`,
    `.github/workflows/pack-gall.yml`, `Cargo.lock`, `crates/ggen-engine/Cargo.toml`,
    `scripts/ci/validate-workflows.py`.
  - This pass's own changes: `ggen.toml`, `packs/ggen-release-pack/ontology.ttl`,
    `RELEASE_STANDING.md`, `RELEASE_STANDING.json`, `docs/PUBLICATION_JUDGMENT.md`
    (see below), plus 3 new template files under `packs/ggen-release-pack/templates/`.
  - Untracked: `crates/ggen-engine/tests/gymact_bridge_pack_e2e.rs`,
    `crates/ggen-engine/tests/gymact_conformance_e2e.rs` (a prior pass this session —
    real conformance-check test against `wasm4pm-compat`, see that test's own doc
    comment for the process-intelligence-boundary rationale).
  - None of the above is committed. Per this repo's standing git-workflow rule,
    nothing here should be committed or pushed without an explicit request.

## Build health

- `cargo build -p ggen-cli-lib --bin ggen`: clean (only pre-existing, unrelated
  `unexpected cfg` warning in `bcinr-pddl`).
- `cargo check --workspace`: clean, same pre-existing warning only.

## Release-standing docs: a real gap found and fixed this pass

`RELEASE_STANDING.md`/`.json` and `docs/PUBLICATION_JUDGMENT.md` are declared
GENERATED artifacts (`packs/ggen-release-pack/ontology.ttl`'s
`grp:ReleaseStandingMdArtifact`/`grp:ReleaseStandingJsonArtifact`/
`grp:PublicationJudgmentArtifact`) — but investigation found **root `ggen.toml`
had no `[[generation.rules]]` entry wiring any of the three**, despite the
ontology being imported via `[ontology] source`. `git log --oneline --all --
RELEASE_STANDING.md` shows exactly one commit (PR #281, the v26.7.20
introduction) — these files were never actually regenerated since, contrary to
what their own headers ("GENERATED by `ggen sync`") and `docs/PUBLICATION_JUDGMENT.md`
condition 22's prose (which describes a real regeneration of this exact file)
imply. This pass:

1. Added the three missing `[[generation.rules]]` entries (`ggen-release-standing-md`,
   `ggen-release-standing-json`, `ggen-publication-judgment`) plus three new
   `.tera` templates under `packs/ggen-release-pack/templates/`, following the
   same `sparql()`-in-template pattern already proven working by
   `gh-actions-errc-pack`'s wired artifacts.
2. Added a staleness disclosure to all three artifact bodies in the ontology
   (not hand-edited into the generated `.md`/`.json`): the 23-condition
   publication-judgment table and the source/binary standing declarations were
   last individually audited at **v26.7.20 (2026-07-20)** and have **not** been
   re-verified at 26.8.x — the version bump the workspace has since undergone is
   purely mechanical (version-law commits), not a re-audit.
3. Ran a real `ggen sync run`: confirmed all 3 files come back `written`
   (not silently skipped), `git diff --stat` limited to exactly the 3 generated
   files plus the receipt lane, and a second sync is a content no-op
   (`skipped: unchanged: content identical` on all 3) — idempotent, same check
   `docs/PUBLICATION_JUDGMENT.md` condition 1's row already uses for this
   mechanism.

## The 23-condition re-audit (ultracode workflow, same session, 2026-08-07)

The staleness this pass initially disclosed was closed for real: an ultracode
workflow launched 24 agents (one per condition + one completeness critic) to
independently re-verify each of the 23 publication conditions against real,
current repo state — actual test runs, actual file reads, actual `gh run`/`git`
queries, never a re-stamped status. The critic caught and this pass corrected
one self-contradicting finding (a claim labeled `ALIVE` while its own text
admitted the live check never completed — kept honest instead) and flagged
two agents that returned no usable evidence (conditions 7 and 16 are marked
"NOT RE-VERIFIED this pass" in their own rows, status carried forward
unchanged, not silently assumed current).

**Real regressions found** (not assumed — each cited in its own row):
- **Condition 18 moved ALIVE → NOT MET**: `.ggen-v2/migration-receipt.json`,
  the persisted artifact this condition's ALIVE status was built on, no longer
  exists on disk. Per 第二十九章's own rule, one NOT MET row is sufficient by
  itself to withhold Level Five publication — this repo already couldn't claim
  it (multiple PARTIAL rows existed before), but this closes any ambiguity.
- **Condition 21 moved ALIVE → PARTIAL**: `book/dist/`'s regenerated
  distribution artifacts no longer exist on disk; the generation scripts
  themselves are unchanged.
- **Condition 22 moved ALIVE → PARTIAL**: `.specify/repo-facts.ttl`'s pack
  inventory has drifted again since `gen:G4`'s 2026-07-20 fix — 82 real pack
  directories on disk vs. 33 registered `rf:Pack` individuals, a larger gap
  than the one previously corrected.
- **Condition 1** (already PARTIAL) gained a newly-disclosed regression: the
  `[[generation.rules]]` entry that regenerated `.github/workflows/
  marketplace-docs.yml` was deleted by an unrelated PR (`18913354b`, PR #489,
  2026-07-31) — the file on disk is now orphaned, no longer regenerated.

**Real improvements found:**
- **Condition 10 moved PARTIAL → ALIVE**: the claims ledger (`docs/aps/
  claims.toml`) grew from 17 to 19 entries and schema validation passes.
- **Condition 20 moved PARTIAL → ALIVE**: a real, live, all-4-target
  cross-host reproducibility CI run has now executed and passed (`gh run view
  31208843697`) — the first time this mechanism has ever produced a real
  green run since it was wired.

All 23 rows, the table's intro, and its Conclusion were updated in
`packs/ggen-release-pack/ontology.ttl` (source — never hand-edited into the
generated `.md`), then regenerated via a real `ggen sync run`: confirmed
`docs/PUBLICATION_JUDGMENT.md` came back `written`, `RELEASE_STANDING.md`/
`.json` correctly stayed `unchanged` (their own bodies didn't need edits — they
already declared `l5_publication_claim: WITHHELD`, which remains true), and a
second sync is a content no-op across all three.

## Bottom line

Concretely, what stands between this repo and a real v26.8.7 release, in
priority order:

1. No `v26.8.6`/`v26.8.7` tag has ever been pushed, so `release.yml` has never
   run for this version line — release readiness per this repo's own gate
   definition is therefore unmet regardless of anything else.
2. This branch is one commit behind `origin/main`'s automated version bump and
   carries real uncommitted work (listed above) that needs a decision:
   commit, discard, or continue.
3. **A real, current NOT MET condition now exists** (condition 18 — see
   above): Level Five publication is unambiguously withheld, not merely
   under-audited. Regenerating `.ggen-v2/migration-receipt.json` (or
   determining why it was removed) is the concrete next step if that claim
   is ever intended.
4. Conditions 7 and 16 still need a real re-audit — their assigned agents
   this pass returned no usable evidence.
