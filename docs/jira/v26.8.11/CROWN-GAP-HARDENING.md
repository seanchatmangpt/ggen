# Crown Gap Hardening

This branch closes ecosystem-level gaps discovered in the 2026-08-11 seven-day review. The executable acceptance law is:

> A claim may acquire standing only when its exact executable subject and every claimed executable surface are exercised or explicitly refused by a machine-checkable inventory.

Cross-repository invariants:

1. SELECT != CONSTRUCT != DO.
2. No generated artifact confers execution authority.
3. Semantic facts that cross repository boundaries require a canonical identity and provenance.
4. Operator applicability is data, not prompt folklore.
5. Process-science results are consumed through an explicit wasm4pm contract; downstream repositories do not silently become alternate authoritative process engines.
6. Crown evidence binds exact input, algorithm, configuration, ontology, artifact, authority and verifier identities.
7. Missing executable coverage is a refusal, not a baseline that may silently rise.

This file is descriptive only; executable guards and machine-readable contracts on this branch are the authority for the bounded claims made by its PR.
