# TAI Enterprise Rebuild WIP Closure

## Scope

This closure applies to the source implementation of `TAI-REBUILD-001` and its `TAI-CMD-G0-G9` roadmap factory. It does not merge unrelated repository programmes or promote external enterprise, certification, contract, or production standing.

## Source closure

The roadmap factory now generates and independently verifies all ten declared checkpoints:

| Checkpoint | Implemented consequence | Typed falsifier |
|---|---|---|
| `G0` | exact Git index, commit, tree, toolchain, and untracked-state observation | `CMD-G0-EXACT-SET` |
| `G1` | ownership record for every tracked surface | `CMD-G1-OWNER-MISSING` |
| `G2` | canonical semantic authority and generated-surface fence | `CMD-G2-PROJECTION-DRIFT` |
| `G3` | bounded internal candidate lattice and independent pairwise coverage | `CMD-G3-CANDIDATE-INVALID` |
| `G4` | bounded external candidate lattice with intent-only authority | `CMD-G4-AUTHORITY-INCOMPLETE` |
| `G5` | dependency-closed composition of eighteen admitted recent capabilities | `CMD-G5-SEMANTIC-DIVERGENCE` |
| `G6` | deterministic plan with structural no-actuator scan and BLAKE3 identity | `CMD-G6-ACTUATOR-IMPORT` |
| `G7` | staged local materialization with atomic rename and observed postcondition | `CMD-G7-PARTIAL-STATE` |
| `G8` | inert BRCE-addressed intent with no grant and zero external calls | `CMD-G8-UNRECEIPTED-ACTUATION` |
| `G9` | machine-readable verifier report and independent exact replay | `CMD-G9-CROWN-INCOMPLETE` |

The canonical option graph contains twenty-four internal options over twelve dimensions and twenty-six external options over thirteen dimensions. Constraints are graph-owned. The verifier enumerates the bounded products, rejects invalid selections, selects pairwise witnesses, and independently recomputes coverage.

## Evidence layout

Execution emits the required evidence beneath:

```text
examples/tai-enterprise-rebuild/.ggen/cmd/
```

The crown report is:

```text
examples/tai-enterprise-rebuild/.ggen/cmd/verifier/report.json
```

It may report `wipRemaining = 0` only when all G0–G9 evidence files exist, all ten typed falsifiers are observed, replay is byte-identical, direct actuation is false, and the required broker is `BRCE`.

## Standing fence

`wipRemaining = 0` means the bounded source programme has no declared implementation placeholder. It does not mean the repository crown, rebuilt enterprise, issuer-recognized certification, government contract, or external production operation is `ALIVE`.

Until the exact-head workflow executes successfully:

```text
source implementation: PARTIAL_ALIVE
exact-head execution: UNKNOWN
external actuation: UNKNOWN
enterprise standing: UNKNOWN
external certification standing: UNKNOWN
```
