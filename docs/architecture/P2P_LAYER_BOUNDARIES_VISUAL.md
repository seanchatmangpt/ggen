# P2P Marketplace Layer Boundaries - Visual Reference

## 🎯 Quick Reference: Layer Responsibilities

```
┌─────────────────────────────────────────────────────────────────┐
│                        CLI LAYER                                 │
│                  (cli/src/cmds/marketplace.rs)                   │
├─────────────────────────────────────────────────────────────────┤
│ ✅ DOES:                          │ ❌ DOES NOT:                │
│ • Parse CLI arguments (Clap)      │ • Business logic           │
│ • Route to domain functions       │ • Error handling details   │
│ • Wrap async execution            │ • libp2p operations        │
│                                    │ • Package validation       │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      DOMAIN LAYER                                │
│              (cli/src/domain/marketplace/p2p.rs)                 │
├─────────────────────────────────────────────────────────────────┤
│ ✅ DOES:                          │ ❌ DOES NOT:                │
│ • Command orchestration           │ • CLI argument parsing     │
│ • Argument validation             │ • Raw libp2p operations    │
│ • User-facing formatting          │ • DHT low-level details    │
│ • State management                │ • Network protocol details │
│ • Call backend trait methods      │                            │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     BACKEND LAYER                                │
│           (ggen-marketplace/src/backend/p2p.rs)                  │
├─────────────────────────────────────────────────────────────────┤
│ ✅ DOES:                          │ ❌ DOES NOT:                │
│ • libp2p networking               │ • CLI argument knowledge   │
│ • Kademlia DHT operations         │ • User output formatting   │
│ • Gossipsub pub/sub               │ • Command routing          │
│ • Peer reputation tracking        │ • State lifecycle          │
│ • Package caching                 │                            │
│ • Implement Registry trait        │                            │
└─────────────────────────────────────────────────────────────────┘
```

## 📊 Data Flow: P2P Search Example

```
┌──────────────────────────────────────────────────────────────────────┐
│ USER INPUT: ggen marketplace p2p search "template"                    │
└──────────────────────────────────┬───────────────────────────────────┘
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ CLI LAYER                                                             │
│ ┌──────────────────────────────────────────────────────────────────┐ │
│ │ MarketplaceCmd::P2p(P2PCommand::Search(args))                    │ │
│ │   query: "template"                                              │ │
│ │   limit: 20                                                      │ │
│ │   min_reputation: 0.5                                            │ │
│ └──────────────────────────────────────────────────────────────────┘ │
│                            ▼ (async wrapper)                          │
│                  crate::runtime::execute(...)                         │
└──────────────────────────────────┬───────────────────────────────────┘
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ DOMAIN LAYER                                                          │
│ ┌──────────────────────────────────────────────────────────────────┐ │
│ │ async fn search_packages(args: SearchArgs)                       │ │
│ │                                                                  │ │
│ │ 1. Validate args                                                 │ │
│ │    ✓ query not empty                                            │ │
│ │    ✓ limit reasonable                                           │ │
│ │    ✓ min_reputation 0.0-1.0                                     │ │
│ │                                                                  │ │
│ │ 2. Create Query object                                           │ │
│ │    Query {                                                       │ │
│ │      text: "template",                                          │ │
│ │      categories: [],                                            │ │
│ │      tags: [],                                                  │ │
│ │      limit: Some(20)                                            │ │
│ │    }                                                             │ │
│ │                                                                  │ │
│ │ 3. Check if P2P initialized                                      │ │
│ │    if !p2p_state::is_p2p_initialized() {                        │ │
│ │      return Err("P2P node not running")                         │ │
│ │    }                                                             │ │
│ │                                                                  │ │
│ │ 4. Get registry instance                                         │ │
│ │    let registry = p2p_state::get_p2p_registry()?;               │ │
│ └──────────────────────────────────────────────────────────────────┘ │
│                            ▼ (trait method call)                      │
│                      registry.search(&query).await                    │
└──────────────────────────────────┬───────────────────────────────────┘
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ BACKEND LAYER                                                         │
│ ┌──────────────────────────────────────────────────────────────────┐ │
│ │ impl Registry for P2PRegistry                                    │ │
│ │                                                                  │ │
│ │ async fn search(&self, query: &Query) -> Result<Vec<Package>> {│ │
│ │                                                                  │ │
│ │   1. Check multi-tier cache (v2.4.0)                            │ │
│ │      if let Some((package, cached_at)) = cache.get(id) {       │ │
│ │        if cached_at.elapsed() < 5min { return cached }         │ │
│ │      }                                                           │ │
│ │                                                                  │ │
│ │   2. Search local packages                                       │ │
│ │      let mut results = local_packages                           │ │
│ │        .values()                                                │ │
│ │        .filter(|p| matches_query(p, query))                    │ │
│ │        .cloned()                                                │ │
│ │        .collect();                                              │ │
│ │                                                                  │ │
│ │   3. Parallel DHT fan-out queries (v2.4.0)                      │ │
│ │      let my_location = self.get_location().await;              │ │
│ │      let best_peers = self.select_best_peers(                  │ │
│ │        0.5,  // min_reputation                                  │ │
│ │        10,   // max peers                                       │ │
│ │        my_location.as_ref()                                     │ │
│ │      ).await;                                                   │ │
│ │                                                                  │ │
│ │      let fan_out = best_peers.len().min(3).max(1);             │ │
│ │      for package_id in discovered.keys() {                      │ │
│ │        if let Ok(Some(pkg)) =                                   │ │
│ │          self.query_dht_parallel(package_id, fan_out).await {  │ │
│ │            if matches_query(&pkg, query) {                      │ │
│ │              results.push(pkg);                                 │ │
│ │            }                                                     │ │
│ │        }                                                         │ │
│ │      }                                                           │ │
│ │                                                                  │ │
│ │   4. Apply limit                                                 │ │
│ │      if let Some(limit) = query.limit {                         │ │
│ │        results.truncate(limit);                                 │ │
│ │      }                                                           │ │
│ │                                                                  │ │
│ │   5. Update cache                                                │ │
│ │      for package in &results {                                  │ │
│ │        cache.insert(                                            │ │
│ │          package.id.clone(),                                    │ │
│ │          (package.clone(), Instant::now())                      │ │
│ │        );                                                        │ │
│ │      }                                                           │ │
│ │                                                                  │ │
│ │   Ok(results)                                                    │ │
│ │ }                                                                │ │
│ └──────────────────────────────────────────────────────────────────┘ │
│            ▲                                                          │
│            │ libp2p Operations:                                       │
│            │ • Kademlia DHT.get_record()                              │
│            │ • Gossipsub.subscribe()                                  │
│            │ • Swarm.next() event loop                                │
│            │ • Peer reputation updates                                │
└──────────────────────────────────┬───────────────────────────────────┘
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ DOMAIN LAYER (return path)                                           │
│ ┌──────────────────────────────────────────────────────────────────┐ │
│ │ 5. Format results for user                                       │ │
│ │    println!("🔍 Searching P2P network for '{}'...", args.query);│ │
│ │    println!("\n📊 Found {} packages", results.len());           │ │
│ │                                                                  │ │
│ │    for package in results {                                      │ │
│ │      println!("  • {} v{}", package.id, package.version);      │ │
│ │      println!("    {}", package.metadata.description);         │ │
│ │    }                                                             │ │
│ └──────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────┬───────────────────────────────────┘
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ USER OUTPUT                                                           │
│ 🔍 Searching P2P network for 'template'...                           │
│                                                                       │
│ 📊 Found 3 packages                                                   │
│   • rust-template v1.0.0                                             │
│     A production-ready Rust project template                         │
│   • api-template v2.1.0                                              │
│     RESTful API template with authentication                         │
│   • cli-template v1.5.0                                              │
│     Command-line application template                                │
└───────────────────────────────────────────────────────────────────────┘
```

## 🔄 Error Flow: Feature Not Enabled

```
┌──────────────────────────────────────────────────────────────────────┐
│ USER INPUT: ggen marketplace p2p start                                │
│ (Build without --features p2p)                                        │
└──────────────────────────────────┬───────────────────────────────────┘
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ CLI LAYER                                                             │
│   MarketplaceCmd::P2p(P2PCommand::Start(args))                       │
│   → crate::runtime::execute(execute_p2p_command(...))                │
└──────────────────────────────────┬───────────────────────────────────┘
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ DOMAIN LAYER                                                          │
│ ┌──────────────────────────────────────────────────────────────────┐ │
│ │ async fn start_node(args: StartArgs) -> Result<()> {            │ │
│ │                                                                  │ │
│ │   #[cfg(not(feature = "p2p"))]                                  │ │
│ │   {                                                              │ │
│ │     return Err(GgenError::feature_not_enabled(                  │ │
│ │       "p2p",                                                     │ │
│ │       "Rebuild with --features p2p to enable P2P functionality" │ │
│ │     ));                                                          │ │
│ │   }                                                              │ │
│ │ }                                                                │ │
│ └──────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────┬───────────────────────────────────┘
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ CLI LAYER (error handling)                                           │
│   Err(e) → Print user-friendly error message                         │
└──────────────────────────────────┬───────────────────────────────────┘
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ USER OUTPUT                                                           │
│ ❌ Error: Feature 'p2p' not enabled.                                 │
│    Rebuild with --features p2p to enable P2P functionality           │
│                                                                       │
│ Example:                                                              │
│   cargo build --features p2p                                         │
└───────────────────────────────────────────────────────────────────────┘
```

## 🛡️ Error Conversion Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ BACKEND LAYER                                                    │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ Result<T, MarketplaceError>                                 │ │
│ │                                                             │ │
│ │ Err(MarketplaceError::NetworkError {                       │ │
│ │   operation: "DHT query",                                   │ │
│ │   reason: "Timeout after 30s"                              │ │
│ │ })                                                          │ │
│ └─────────────────────────────────────────────────────────────┘ │
└──────────────────────────┬──────────────────────────────────────┘
                           ▼ (.map_err())
┌─────────────────────────────────────────────────────────────────┐
│ DOMAIN LAYER                                                     │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ .map_err(|e| GgenError::network_error(                     │ │
│ │   format!("Failed to query DHT: {}", e)                    │ │
│ │ ))                                                          │ │
│ │                                                             │ │
│ │ Result<T, GgenError>                                        │ │
│ │                                                             │ │
│ │ Err(GgenError {                                            │ │
│ │   message: "Network error: Failed to query DHT: ..."      │ │
│ │ })                                                          │ │
│ └─────────────────────────────────────────────────────────────┘ │
└──────────────────────────┬──────────────────────────────────────┘
                           ▼ (propagate)
┌─────────────────────────────────────────────────────────────────┐
│ CLI LAYER                                                        │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ match result {                                              │ │
│ │   Err(e) => eprintln!("❌ Error: {}", e),                  │ │
│ │   Ok(_) => println!("✅ Success")                           │ │
│ │ }                                                            │ │
│ └─────────────────────────────────────────────────────────────┘ │
└──────────────────────────┬──────────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│ USER OUTPUT                                                      │
│ ❌ Error: Network error: Failed to query DHT: Timeout after 30s │
└─────────────────────────────────────────────────────────────────┘
```

## 🎯 Trait Abstraction Example

```
┌──────────────────────────────────────────────────────────────────┐
│ TRAIT DEFINITION (ggen-marketplace/src/traits/mod.rs)            │
│ ┌──────────────────────────────────────────────────────────────┐ │
│ │ #[async_trait]                                               │ │
│ │ pub trait Registry: Send + Sync {                            │ │
│ │   async fn search(&self, query: &Query)                     │ │
│ │     -> Result<Vec<Package>>;                                │ │
│ │                                                              │ │
│ │   async fn get_package(&self, id: &PackageId)               │ │
│ │     -> Result<Package>;                                     │ │
│ │                                                              │ │
│ │   async fn publish(&self, package: Package)                 │ │
│ │     -> Result<()>;                                          │ │
│ │                                                              │ │
│ │   async fn metadata(&self) -> Result<RegistryMetadata>;     │ │
│ │ }                                                            │ │
│ └──────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
                              ▲                 ▲
                              │                 │
        ┌─────────────────────┴───┐  ┌──────────┴────────────────┐
        │                          │  │                            │
┌───────────────────────┐  ┌──────────────────────┐  ┌──────────────────────┐
│ P2PRegistry           │  │ CentralizedRegistry  │  │ FederatedRegistry    │
│ (libp2p)              │  │ (HTTP API)           │  │ (Multi-source)       │
│                       │  │                      │  │                      │
│ impl Registry for     │  │ impl Registry for    │  │ impl Registry for    │
│ P2PRegistry { ... }   │  │ CentralizedRegistry  │  │ FederatedRegistry    │
└───────────────────────┘  └──────────────────────┘  └──────────────────────┘
```

**Key Point:** Domain layer calls `registry.search()` - it doesn't care which implementation!

## 🔒 Feature Flag Compilation

```
┌──────────────────────────────────────────────────────────────────┐
│ WITH --features p2p                                               │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│ CLI LAYER                                                         │
│   ✓ MarketplaceCmd::P2p variant available                        │
│   ✓ execute_p2p_command() function compiled                      │
│                                                                   │
│ DOMAIN LAYER                                                      │
│   ✓ start_node() with P2P logic                                  │
│   ✓ publish_package() with P2P logic                             │
│   ✓ search_packages() with P2P logic                             │
│   ✓ p2p_state module with global registry                        │
│                                                                   │
│ BACKEND LAYER                                                     │
│   ✓ P2PRegistry struct and implementation                        │
│   ✓ libp2p dependencies linked                                   │
│   ✓ Kademlia DHT available                                       │
│   ✓ Gossipsub available                                          │
│                                                                   │
│ Binary Size: ~15MB (includes libp2p)                             │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│ WITHOUT --features p2p (default)                                  │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│ CLI LAYER                                                         │
│   ✓ MarketplaceCmd::P2p variant available (for nice error msg)   │
│   ✗ P2P logic compiled out                                       │
│                                                                   │
│ DOMAIN LAYER                                                      │
│   ✓ start_node() returns feature_not_enabled error               │
│   ✓ Other P2P functions return feature_not_enabled               │
│   ✗ P2P implementation compiled out                              │
│   ✗ p2p_state returns feature_not_enabled                        │
│                                                                   │
│ BACKEND LAYER                                                     │
│   ✗ P2PRegistry not compiled                                     │
│   ✗ libp2p dependencies not linked                               │
│   ✗ No DHT code in binary                                        │
│                                                                   │
│ Binary Size: ~8MB (no libp2p overhead)                           │
└──────────────────────────────────────────────────────────────────┘
```

## 📋 Layer Communication Contract

```
┌──────────────────────────────────────────────────────────────────┐
│ CLI → DOMAIN                                                      │
├──────────────────────────────────────────────────────────────────┤
│ Input:  Parsed arguments (StartArgs, PublishArgs, etc.)          │
│ Output: Result<(), GgenError>                                    │
│ Rules:  • Must not contain business logic                        │
│         • Only route to domain functions                         │
│         • Wrap async execution via runtime                       │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│ DOMAIN → BACKEND                                                  │
├──────────────────────────────────────────────────────────────────┤
│ Input:  Validated domain objects (Query, PackageId, Package)     │
│ Output: Result<T, MarketplaceError>                              │
│ Rules:  • Call trait methods only (Registry, PackageStore)       │
│         • Convert errors to GgenError                            │
│         • No direct struct knowledge                             │
│         • Backend-agnostic logic                                 │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│ BACKEND (Internal)                                                │
├──────────────────────────────────────────────────────────────────┤
│ Input:  libp2p events, network messages                          │
│ Output: Structured domain objects (Package, RegistryMetadata)    │
│ Rules:  • No CLI knowledge                                       │
│         • Pure networking/storage logic                          │
│         • Implement traits defined in ggen-marketplace/traits    │
│         • Return MarketplaceError on failures                    │
└──────────────────────────────────────────────────────────────────┘
```

## ✅ Validation Checklist for New Features

When adding new P2P functionality, verify:

- [ ] **CLI Layer**
  - [ ] Only argument parsing with Clap
  - [ ] Routes to domain function
  - [ ] No business logic
  - [ ] Async wrapper via `crate::runtime::execute()`

- [ ] **Domain Layer**
  - [ ] Orchestration logic only
  - [ ] Validates arguments
  - [ ] Calls backend trait methods
  - [ ] Formats user output
  - [ ] Converts MarketplaceError → GgenError
  - [ ] Uses `#[cfg(feature = "p2p")]` appropriately

- [ ] **Backend Layer**
  - [ ] No CLI knowledge
  - [ ] Implements Registry trait
  - [ ] Pure networking/storage logic
  - [ ] Returns MarketplaceError
  - [ ] Uses `#[cfg(feature = "p2p")]` for P2P-specific code

- [ ] **Tests**
  - [ ] CLI tests for argument parsing
  - [ ] Domain tests for orchestration
  - [ ] Backend tests for networking
  - [ ] Feature flag tests (enabled/disabled)

---

**Reference:** [P2P Architecture Validation Final](P2P_ARCHITECTURE_VALIDATION_FINAL.md)
