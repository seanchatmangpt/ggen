# Stub / WIP Audit — 2026-08-08

Status: `PARTIAL_ALIVE`

## Admitted subject

- Repository: `seanchatmangpt/ggen`
- Base: `main`
- Observed head: `c37b46015b8e5ab40be771d61aafe3d7c7af084c`

## Confirmed findings

### WIP-001 — Canonical-looking `MASTER_TODO.md` is stale against the current tree

`docs/MASTER_TODO.md` is dated 2026-04-01 and still classifies the `construct` command as a live P1 stub at the old path `ggen-cli/src/cmds/construct.rs`. That path no longer exists in the current tree. Current integration tests explicitly state that the `ggen construct` subcommand was removed and consolidated to `ggen sync`.

**Classification:** `INCOMPLETE_WIP / STALE_AUDIT_PROJECTION`

A master TODO that names removed paths and superseded commands can no longer serve as an authoritative incompleteness inventory. It mixes potentially real current defects with historical topology.

**Recommended closure:** regenerate the TODO inventory from the current workspace graph and exact head, preserve obsolete findings in an archive, and bind every live finding to a current file/symbol/test.

### WIP-002 — Removed `construct` surface retains an entire ignored integration-test suite

`crates/ggen-cli/tests/construct_command_test.rs` contains eight tests, all ignored with the reason `ggen construct subcommand removed; CLI consolidated to ggen sync (v26_5_19+)`. Several test bodies still assert historical `not_implemented` behavior.

**Classification:** `INCOMPLETE_WIP / DEAD_TEST_SURFACE`

These tests no longer verify an admitted production interface and are permanently excluded from ordinary execution. Retaining them in the active test tree creates stale behavioral specifications and noise in ignored-test accounting.

**Recommended closure:** migrate any still-relevant behavioral assertions to `ggen sync`, then archive/delete the removed-command tests. Do not simply unignore them because their subject no longer exists.

## Additional inventory evidence

The master TODO currently enumerates numerous P0/P1/P2 stub claims (SHACL always-pass, marketplace empty/error returns, placeholder MCP execution, hardcoded graph export, ignored tests, etc.). Those April findings are **not automatically re-admitted as current defects** by this audit because current-tree verification is required item-by-item.

## Standing

`PARTIAL_ALIVE`: current head and current ignored test bodies were directly observed; the repository's historical master TODO is proven stale, so its remaining entries require re-verification before being promoted to current findings.
