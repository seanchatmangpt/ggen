# W5: Clean Room Rebuild

Workspace compilation and testing are executed inside an ephemeral directory to guarantee isolation from the active workspace environment, verified by `clean_room_rebuild.json`.
