{# how-to-use.md — ggen multi-row template; variables accessed as row["varname"] #}
# How to Use This Pattern Language

> *"The patterns are not isolated. They form a language. And the language is generative."*

---

## The Executable Pattern UX

Every pattern follows the same three-step invocation:

1. **Define your domain in TTL** — `.specify/domain.ttl`
2. **Configure ggen.toml** — add a `[[generation.rules]]` entry
3. **Generate** — run `ggen sync`

---

## Pattern Categories

{% for row in sparql_results %}
### {{ row["categoryLabel"] | default(value="") }}

{{ row["categoryDescription"] | default(value="") }}

**Patterns in this category:** {{ row["patternCount"] | default(value="") }}

{% endfor %}

---

## Navigation by Need

| If you need to... | Start with... |
|-------------------|---------------|
| Structure your whole application | Pattern 1: DOMAIN ONTOLOGY AS TRUTH |
| Express domain concepts as types | Pattern 7: VALUE OBJECT RECORD |
| Persist domain objects | Pattern 13: JPA ENTITY MAPPING |
| Implement use cases | Pattern 19: SERVICE LAYER |
| Handle HTTP requests | Pattern 21: REST CONTROLLER |
| Run concurrent I/O safely | Pattern 23: VIRTUAL THREAD SERVICE MESH |

---

## Confidence Stars

| Stars | Meaning |
|-------|---------|
| `*` | **Emerging** — Useful but locally scoped |
| `**` | **Established** — Proven, use in production |
| `***` | **Fundamental** — Essential; omitting it damages the system |

---

*Generated by ggen sync from java26-patterns.ttl*
