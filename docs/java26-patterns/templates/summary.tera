{# SUMMARY.md — ggen multi-row template; variables accessed as row["?varname"] #}
# Summary

[Introduction](README.md)

---

# The Pattern Language

- [How to Use This Pattern Language](how-to-use.md)

---

# Super-Patterns

These patterns establish the fundamental super-structures that span the entire Java 26 application.

{% for row in sparql_results %}{% if row["?categoryLabel"] == "Super-Patterns" %}
- [{{ row["?patternNumber"] }}. {{ row["?patternLabel"] }} {{ row["?confidenceStars"] | default(value="") }}](patterns/{{ row["?filename"] }})
{% endif %}{% endfor %}

---

# Domain Model Patterns

These patterns govern how domain knowledge is expressed as Java 26 records, sealed types, and value objects.

{% for row in sparql_results %}{% if row["?categoryLabel"] == "Domain Model Patterns" %}
- [{{ row["?patternNumber"] }}. {{ row["?patternLabel"] }} {{ row["?confidenceStars"] | default(value="") }}](patterns/{{ row["?filename"] }})
{% endif %}{% endfor %}

---

# Persistence Patterns

These patterns manage how domain objects are persisted and retrieved using modern Java data access.

{% for row in sparql_results %}{% if row["?categoryLabel"] == "Persistence Patterns" %}
- [{{ row["?patternNumber"] }}. {{ row["?patternLabel"] }} {{ row["?confidenceStars"] | default(value="") }}](patterns/{{ row["?filename"] }})
{% endif %}{% endfor %}

---

# Application Layer Patterns

These patterns orchestrate use cases, services, and controllers using Java 26 features.

{% for row in sparql_results %}{% if row["?categoryLabel"] == "Application Layer Patterns" %}
- [{{ row["?patternNumber"] }}. {{ row["?patternLabel"] }} {{ row["?confidenceStars"] | default(value="") }}](patterns/{{ row["?filename"] }})
{% endif %}{% endfor %}

---

# Infrastructure Patterns

These patterns connect the application to external systems, frameworks, and runtime environments.

{% for row in sparql_results %}{% if row["?categoryLabel"] == "Infrastructure Patterns" %}
- [{{ row["?patternNumber"] }}. {{ row["?patternLabel"] }} {{ row["?confidenceStars"] | default(value="") }}](patterns/{{ row["?filename"] }})
{% endif %}{% endfor %}

---

# Reference

- [Pattern Map](pattern-map.md)
- [Glossary](glossary.md)

---

*Generated by ggen sync from java26-patterns.ttl*
