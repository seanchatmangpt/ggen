# Generation Ledger

GENERATED by `ggen sync` from `.specify/generations.ttl` — edit the ontology, not this file.

This ledger names two generations: **G0**, the manufacturing state this round started
from, and **G1**, what this round manufactures. The split exists so that no single round
certifies its own output — G1 records what it built; a later, separately-receipted round
must inspect it before any row below may move from NOT MET to MET on that round's own say-so.

## G0 — starting state

| Fact | Value |
|---|---|
| Commit | `ad91067020cfe468a4cf77d1bd8ab11072ddd71a` |
| Commit subject | feat: L5 pack ecosystem -- SPARQL gates on both engines, TCPS reference conformance, self-hosted verification, generated manuals/book, reflexive receipts |
| Workspace version (`Cargo.toml` `[workspace.package]`) | 26.7.18 |
| Toolchain (`rust-toolchain.toml`) | nightly-2026-06-22 |
| Binary standing | `unverified-reproducibility` |

## G1 — what this round manufactures

| Fact | Value |
|---|---|
| Root `ggen.toml` `[[generation.rules]]` count (before → after) | 9 → 10 |
| `book/src/` output file count (before this round's chapter addition) | 735 |
| `book/scripts/check_book.py` chapter-links count, before | 367 |
| `book/scripts/check_book.py` chapter-links count, after | 368 |
| `claude-code-pack` declared `.claude/` outputs | 4 |
| ...of which present on disk today | 0 |
| Self-hosted evidence-loop script | `examples/tcps-generated/scripts/verify.sh` |
| ...always exits 0 (records reality; gates judge it separately) | true |

## 15-condition doctrine Definition-of-Done

5 of 16 conditions MET as of this round. A young lineage should show more NOT MET rows than MET ones — that is the honest, expected state, not a defect in this table.

| # | Condition | Status | Evidence |
|---|---|---|---|
| 1 | docs/GENERATIONS.md is machine-GENERATED from .specify/generations.ttl by a root ggen.toml [[generation.rules]] entry, never hand-edited. | **NOT MET** | The rule (`docs-generations-ledger`) and its template exist and were independently checked: the ontology parses (96 quads, `ggen graph validate`) and the Tera template's `sparql()`/`{% for %}`/`{% endfor %}` blocks are balanced. But a full `ggen sync run` -- the only thing that actually writes docs/GENERATIONS.md -- currently aborts on an earlier, unrelated rule (see dod02) before reaching this one, so docs/GENERATIONS.md has not yet been produced by the pipeline. Marking this MET before a sync actually emits the file would itself be the self-certification failure chapter 337 names. |
| 2 | `just gen-receipt` runs a real `ggen sync run` and completes successfully, printing a real one-line confirmation. | **NOT MET** | The justfile `gen-receipt` recipe was added and its shape verified (invokes `{{GGEN}} sync run`, echoes a confirmation line) -- but running the underlying command this round (`cargo run -p ggen-cli-lib --bin ggen -- sync run --dry-run`) FAILS before reaching this ledger's own rule: a pre-existing, unrelated bug in the `docs-constitution-mirror` rule (packs/ggen-constitution-pack/templates/constitution.md.tmpl's front-matter `laws:` query variable is not populated by the engine -- FM-GEN-008 TEMPLATE_VARIABLE_MISSING) aborts the whole sync first. Confirmed via `git diff --stat -- packs/ggen-constitution-pack/` returning empty that this round did not introduce that bug. `.specify/generations.ttl` itself was independently validated with `ggen graph validate --files .specify/generations.ttl` (96 quads, valid Turtle, no parse errors), so this rule's own input is sound; it simply has not been exercised end-to-end because an earlier rule in the same manifest currently fails closed. |
| 3 | (supplementary, not part of the original 15) a heterogeneous, independently-built second binary was run against the same sources as an inspection/dual-manufacture check. | **MET** | gen:G2Heterogeneous: ggen@26.7.13 (a genuinely different build, predating this workspace's ggen.toml `version` field) was run against the live tree and refused loudly with FM-CONFIG-002 rather than silently diverging or corrupting state -- a real, disclosed divergence between two independently-built toolchains, not a fabricated agreement. |
| 3 | G0 and G1 are receipted by DIFFERENT rounds -- the round that writes a rule never also certifies it in the same commit. | **MET (as of G2)** | gen:G2, added 2026-07-20 in a separate round/commit from the one that wrote G0/G1 (2026-07-19), records `gen:inspects gen:G1`. G0/G1's own same-round authorship remains as originally disclosed above; the condition is satisfied going forward by G2's existence, not retroactively rewritten. |
| 4 | `.ggen-v2/receipt.json` exists and its BLAKE3 chain hash was verified via `ggen receipt verify` after this round's sync. | **MET** | Ran `ggen receipt verify` after G3's sync, 2026-07-20: `{"valid":true,"chain_hash":"81ca46a1...","payload_hash":"50d675f3...","graph_hash":"8a4511f3...","outputs":14,"signed":true,"signature_valid":true}`. Also confirmed by hand that G3's prev_chain_hash_hex equals G2's chain_hash_hex, i.e. the chain is continuous across this round's two real sync runs. |
| 5 | claude-code-pack's 4 declared output files (2 agents, 1 workflow, 1 skill) are all present on disk under .claude/. | **NOT MET** | Checked all 4 declared `to:` targets with `[ -f ... ]`: independent-verifier.md, pack-transcriber.md, implement-verify.js, l5-pack-method/SKILL.md -- 0 of 4 exist. The pack has not been synced into this repo state. |
| 6 | level-five-book-pack is wired into root ggen.toml's [[generation.rules]] so the book regenerates on `ggen sync run` rather than being a standalone artifact. | **NOT MET** | grep for "level-five-book" / "book-pack" in ggen.toml returned zero matches; none of the 7 existing [[generation.rules]] reference this pack. book/src/ is populated but not currently reachable from the live sync pipeline. |
| 7 | book/scripts/check_book.py passes (every SUMMARY.md link resolves to an existing file with the required section markers). | **MET** | Ran `python3 book/scripts/check_book.py` after admitting chapter 337 and its SUMMARY.md entry -- "OK: 368 links, 368 markdown files" (was 367 before this round). |
| 8 | The self-hosted evidence loop (examples/tcps-generated/scripts/verify.sh) has been RUN this round and its evidence/checks.ttl + receipts/inspection-receipt.json inspected, not merely confirmed to exist. | **NOT MET** | Only the script's existence, executable bit, and header comment were verified (`ls -la`, `head -40`). It was not executed as part of this round's work. |
| 9 | `just pre-commit`'s 9 gates all pass on the current tree after this round's edits. | **NOT MET** | Not run this session; this round only touched .specify/generations.ttl, ggen.toml, justfile, packs/level-five-book-pack/ontology.ttl, and book/src/**, none of which were compiled or gated before this ledger was written. |
| 10 | The generation ledger (.specify/generations.ttl) itself is admitted by a SHACL or SPARQL gate the same way other packs' individuals are (e.g. level-five-book-pack's gates/010_required.rq). | **NOT MET** | No gates/*.rq or SHACL shape file was authored for gen:Generation or gen:DoDCondition individuals this round. |
| 11 | A second `ggen sync run` on an unchanged graph is byte-identical for docs/GENERATIONS.md (idempotency proven, not assumed). | **MET** | G2 then G3: `ggen sync run` invoked twice with zero source changes in between, 2026-07-20. Every one of the 14 tracked outputs (including docs/GENERATIONS.md) reported "skipped: unchanged: content identical" on BOTH runs; `git diff --stat` across the whole tree (excluding the receipt/evidence lane) between G2 and G3 was empty. Idempotency proven by direct diff, not assumed. |
| 12 | The new book chapter (337, 自らを造る機械は、自らを検査しなければならない) is reachable via the pack's own `to: "src/{{ path }}"` templated render, not only via a hand-written book/src file. | **NOT MET** | Because level-five-book-pack is not wired into ggen.toml (see dod06), book/src/tcps-standing/337-jikoken-kensa.md was written by hand to satisfy check_book.py; ggen sync run was never invoked to render it from the ontology addition. |
| 13 | OTEL pipeline spans (pipeline.load/extract/validate/generate/emit) were captured for the sync run that produced docs/GENERATIONS.md, per the project's OTEL Validation requirement. | **NOT MET** | No RUST_LOG=trace capture was performed this round. |
| 14 | This round's commit message cites concrete receipts (test counts, gate results) rather than narrated completion, per docs/jira and CLAUDE.md's Evidence-First Principle. | **NOT MET** | No commit has been made yet as of writing this ledger; this row cannot be MET until that commit exists and is checked. |
| 15 | A named future round (G2) exists that will independently inspect G1's claims -- i.e. this ledger names its own successor rather than claiming final standing. | **MET** | gen:G2 (added 2026-07-20, a separate round from the one that authored G0/G1) records `gen:inspects gen:G1`; gen:G3 inspects G2; gen:G2Heterogeneous independently inspects G2 from a different toolchain. This ledger in turn names no successor of its own yet -- a future round must inspect G2/G3/G2Heterogeneous's claims before they can be treated as final, per the same discipline. |


## See also

- `packs/level-five-book-pack` chapter 337 (自らを造る機械は、自らを検査しなければならない) — the doctrine this ledger's G0/G1 split implements.
- `.claude/rules/coding-agent-mistakes.md` — the 6-question patch contract this ledger's evidence citations answer to.
