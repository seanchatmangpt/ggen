# Level 5 Pack-Closure Promotion Program

GENERATED by `ggen sync` from `.specify/pack-l5-promotion.ttl` -- edit the ontology, not this file.

Governing equation: `autosel = ggen(MaximalClosure(P_L5))`, where `P_L5 = { Promote(p) | p in P }` and `P` is every compatible ggen pack. Current per-dimension maturity (see `docs/packs/PACK_MATURITY_MODEL.md`) is an OBSERVATION, not a scope boundary -- every pack below is a promotion obligation until it reaches a terminal standing.

## 1. Level 5 pack census (34 packs)

| Pack | Wave | Standing | Current ceiling |
|---|---|---|---|
| ggen-verify-pack | 1 | furthest-advanced pack(s); source of reusable promotion machinery, itself not yet full Level5 | Its strong dimension is L3→L4 Actuation-closure (real FM-PACK-013 sync refusal, proven under both graph engines); rest L1-L3 |
| tcps-cli-pack | 1 | promotion actively underway; ≥1 capability closed, ≥1 open | same shape as tcps-ffi-pack. cap06 CLOSED 2026-07-22 (wave3 tcps-cluster cell, worktree wf_8730e757-e2c-1): packs/tcps-cli-pack/templates/dispatch_sabotage_proof.rs.tmpl projects 4 new tcps:CliCase individuals via a real sparql: frontmatter into crates/tcps-cli/tests/tcps_cli_dispatch_sabotage_proof.rs, which spawns the REAL compiled `tcps` binary (env!("CARGO_BIN_EXE_tcps"), a genuine subprocess boundary crossing, not a re-implementation of main.rs's dispatch) and asserts its real exit code -- 3 known-subcommand positive controls (検査/版/環境, all exit 0, each transcribed from an actual `./target/debug/tcps <cmd>` run before being written into the ontology, not guessed) plus 1 adversarial case (an unrecognized subcommand `でたらめ`, which hits main.rs's own `_ => { 使い方(); ExitCode::from(2) }` fallback arm). 2/2 tests passing. Non-vacuity independently verified: flipped the adversarial case's tcps:expectedExitCode 2->0, re-synced, got a real assertion failure (actual exit code 2 != declared-wrong expectation 0), then restored byte-for-byte (pure-insertion diff confirmed) and re-synced to 2/2 green. Idempotent: second sync's live receipt reports the new test file 'skipped: unchanged: content identical'; sha256 aee77a83... identical across two independent full cycles. Scope note, disclosed: 検査's own internal conditional (二本柱が閉じている()) is NOT independently adversarial-testable through the CLI's argv surface, because its input (豊田生産方式::原初形()) is a hardcoded const fn with every field true -- no argv reaches a false branch, so this is the pack's only real refusal path reachable this way, and the 2-vs-1 case split is exhaustive of it, not artificially thin. Full cargo test --workspace green throughout (18+7+5+2+2 = 34 tests across the 4 tcps-* crates). |
| tcps-core-pack | 1 | furthest-advanced pack(s); source of reusable promotion machinery, itself not yet full Level5 | L5 on Generation-depth/Consumer-effort/Test-generation; L4 on Handler-gap/Regen-lifecycle/Target-API-fidelity; L3 on Ontology-expressiveness (see mat:tcps rows). cap06 CLOSED 2026-07-22 (wave3 tcps-cluster cell, worktree wf_8730e757-e2c-1): packs/tcps-core-pack/templates/pokayoke_sabotage_proof.rs.tmpl projects 12 new tcps:RangeGuardPassCase/FailCase + tcps:MatchGuardPassCase/FailCase individuals via a real sparql: frontmatter (no hardcoded EXPECTED array) into examples/tcps-generated/tests/tcps_core_pokayoke_sabotage_proof.rs, which calls the REAL 範囲防止/一致防止 guard structs' 検査する method (防止条件<入力> trait) directly -- not a subprocess ggen sync refusal, a runtime guard-logic proof. 5/5 tests passing (`cargo test --test tcps_core_pokayoke_sabotage_proof`): 3 range + 3 match positive controls, 3 range + 3 match adversarial-input fail cases, plus a SPARQL-row-count floor check. Non-vacuity independently verified: changed rgcFailBelowMinimum's tcps:rangeInput 99->150 (still declared a RangeGuardFailCase but now genuinely in-range), re-synced, re-ran -- got the predicted 4 passed/1 failed with a real assertion message ('guard fails OPEN, not closed'), then restored byte-for-byte (git diff shows pure insertion, no residual mutation) and re-synced to 5/5 green again. Idempotent: a second `ggen sync run` after restoration reports `tests/tcps_core_pokayoke_sabotage_proof.rs: skipped: unchanged: content identical` in the live receipt (not just claimed -- read directly from .ggen-v2/receipt.json's admission log), and the file's own sha256 was identical (f99e97e1...) across two independent full verify+sync cycles. Full `cargo test --workspace` green throughout (18+7+5+2+1 = 33 tests across the 4 tcps-* crates, 0 failures). `just verify-tcps` clean at the end (receipt valid:true, signed:true, signature_valid:true, 133 outputs). |
| tcps-ffi-pack | 1 | promotion actively underway; ≥1 capability closed, ≥1 open | same shape as tcps-std-pack; Test-generation 0/0 'honestly, not fabricated' per the doc -- the reference itself ships no tests |
| tcps-release-pack | 1 | promotion actively underway; ≥1 capability closed, ≥1 open | L3-L5 mixed (L5 Consumer-effort, L4 Handler-gap/Regen-lifecycle, L3 Generation-depth/byte-verbatim fidelity/Test-generation, L2 Ontology-expressiveness) |
| tcps-std-pack | 1 | promotion actively underway; ≥1 capability closed, ≥1 open | L2-L5 mixed (L5 Consumer-effort, L4 Generation-depth/Handler-gap/Regen-lifecycle/Target-API-fidelity, L3 Test-generation 1/1 real test, L2 Ontology-expressiveness). cap06 CLOSED 2026-07-22 (wave3 tcps-cluster cell, worktree wf_8730e757-e2c-1): packs/tcps-std-pack/templates/persistence_sabotage_proof.rs.tmpl projects 2 new tcps_std:PersistCase individuals via a real sparql: frontmatter into crates/tcps-std/tests/tcps_std_persistence_sabotage_proof.rs, which calls the REAL 受領証を保存する with a genuinely adversarial filesystem precondition (a 根 path whose parent component is a pre-existing regular file, so fs::create_dir_all fails with a real OS NotADirectory error) plus a positive control on a writable root. This crate's 受領証を保存する never branches on receipt field values -- io::Result on create_dir_all is its ONLY real conditional behavior -- so a 2-case suite is exhaustive here, not artificially thin (unlike tcps-core-pack's much larger guard-condition boundary-value space). A live self-check caught and fixed a real tautology bug during development: an earlier draft derived the filesystem precondition FROM tcps_std:expectedOutcome directly, so the constructed precondition always matched whatever outcome was declared and the assertion could never fail regardless of ontology content -- fixed by adding a SEPARATE tcps_std:rootScenario predicate that alone drives precondition construction, with expectedOutcome compared independently (see ontology.ttl's pscRootBlockedByFile comment). Non-vacuity re-verified against the FIXED template: flipped expectedOutcome Err->Ok with rootScenario unchanged, re-synced, got a real failure ('left: false, right: true ... returned Err(Os { code: 20, kind: NotADirectory ... })'), then restored byte-for-byte (pure-insertion diff confirmed) and re-synced to 2/2 green. Idempotent: second sync's live receipt reports both crates/tcps-std/Cargo.toml (needed a new [dev-dependencies] tempfile="3" entry, added to tcps_std:manifestText) and the new test file as 'skipped: unchanged: content identical'; sha256 508961bc... identical across two independent full cycles. Full cargo test --workspace green throughout. |
| tcps-wasm-pack | 1 | promotion actively underway; ≥1 capability closed, ≥1 open | same shape as tcps-ffi-pack |
| clap-noun-verb-pack | 3 | promotion actively underway; ≥1 capability closed, ≥1 open | L2 ceiling all dims except Test-generation L3 (29/29 in receiptctl: 20 embedded Tier1/2/3 + 9 new SPARQL-derived, CI-wired via guard-pack-proofs.sh). cap05 CLOSED 2026-07-21 (commit b49475a5e, cherry-picked from worktree cell-c's 58ee9488f): this pack has no separate hand-transcribed proof file the way praxis-core-pack/star-toml-pack do -- its Tier1/2/3 proof is embedded as a #[cfg(test)] mod inside clap_noun_verb_routes.rs.tmpl itself (the wrapper fns it exercises are private to that module), and that embedded proof is frozen in lockstep with the routes file by skip_if: "ggen:local-override", so it can only certify self-consistency, never catch the routes file drifting behind a later ontology.ttl edit once frozen. Adds clap_noun_verb_sparql_derived_proof.rs.tmpl: a second, independent template with its own commands/nouns SPARQL queries (same predicates the routes template already queries), no freeze_policy/skip_if, re-querying the live ontology graph on every sync and comparing against src/clap_noun_verb_routes.rs and docs/clap_noun_verb_routes.md as they actually sit on disk. Independently re-verified in this reconciliation pass (not just re-running the cell's claim): ran `cargo test -q` fresh in examples/receiptctl after cherry-picking -- confirmed exactly 20 embedded tests + 9 new tests, both green, matching the cell's counts precisely (not the stale 9 recorded in maturity.ttl from 2026-07-18, since receiptctl's own ontology has since added 3 more commands). Drift-sensitivity (per the cell's own live-executed proof, not independently re-run this pass): freezing src/clap_noun_verb_routes.rs via its skip_if marker and editing SessionVerify's cnv:doc in ontology.ttl left the frozen routes.rs stale, kept the embedded proof green (self-consistent with the stale file), and made the new proof correctly FAIL citing the exact stale doc comment; reverted cleanly. |
| wasm4pm-algorithms-pack | 3 | promotion actively underway; ≥1 capability closed, ≥1 open | L2 ceiling, L3 Test-generation (22/22, CI-wired, re-run live 2026-07-22). cap05 CLOSED 2026-07-22 (L5 wave3, cell wasm4pm-catalog-structural) -- found ALREADY CLOSED, not merely not-applicable, same class of finding as cargo-cicd-pack's cap05: independently read all 3 proof templates directly -- algorithms_catalog_proof.rs.tmpl, algorithms_invocation_dispatch_proof.rs.tmpl, algorithms_target_fidelity_proof.rs.tmpl -- each carries a real `sparql:` frontmatter with a from-scratch Tera for-loop building EXPECTED_TOTAL/EXPECTED_CATEGORY_COUNTS/the full-field per-row block and COMPOSABLE, no hand-typed EXPECTED array (2 of 16 catalog-proof tests are deliberately-kept named anti-tautology regression cases, matching star-toml-pack's kept-hand-transcribed-twin precedent, not the primary coverage mechanism). Verified non-vacuous with a real, reversible drift test (not just frontmatter inspection): edited `pi:Algo_inductive_miner`'s `pi:category` from `discovery` to `conformance` in this pack's ontology.ttl AND its verbatim mirror in wasm4pm-facts-pack/ontology.ttl (required together -- editing only one side correctly tripped `gates/020_single_valued.rq` then `gates/030_value_constraints.rq` via [FM-PACK-013] against the union graph receiptctl builds from all 6 wired packs, proving those cross-pack consistency gates are real and live, not decorative); re-synced (after the documented FM-PACK-008 ggen.lock re-lock and 5 FM-WRITE-005 stale-target deletions, not force:true) and confirmed the regenerated `CATALOG` const's `category` field AND the regenerated proof's `EXPECTED_CATEGORY_COUNTS` aggregate (conformance 4->5, discovery 20->19) and per-row assertion changed together automatically with zero hand edits to the test file; all 40 cross-pack tests (22 here + 18 in wasm4pm-facts-pack) green against the NEW value; reverted both ontology.ttl files byte-for-byte (git checkout, confirmed empty diff -- a first revert attempt via an unscoped `sed` accidentally flipped 4 unrelated legitimate `conformance` rows per file back to `discovery` too and was caught via `git diff` before being synced, then corrected via `git checkout`), re-synced, re-tested: 40/40 green, idempotent second sync (`git status` clean), receipt valid+signed. Handler-gap (cap02) adjudicated, NOT structurally N/A (correcting this entry's own prior note): `src/w4pm_algorithms_invocation_dispatch.rs` + its 3/3-passing proof is a real dispatch surface, not catalog-only -- `dispatch_verified()` makes a genuine compiled call into the pinned `wasm4pm-compat` crate's own `discover_ocel_dfg` (respecting CLAUDE.md's process-intelligence-boundary rule -- no local reimplementation) for the 1 of 60 rows carrying `pi:verifiedAgainst`, and typed-refuses (`DispatchError::NotComposable`) the other 59 rather than stubbing/panicking. This has existed since commit ad9106702 (predates wave1) but `.specify/maturity.ttl`'s own `mat:cg_003` `d2`/`l2` score ('L2 catalog only, no calls') was stale -- `git blame` confirms the score row and this dispatch template landed in the SAME commit, so the score never accounted for the file that shipped beside it; corrected in maturity.ttl this pass to L2->L3 with the same evidence. |
| wasm4pm-facts-pack | 3 | promotion actively underway; ≥1 capability closed, ≥1 open | L1 Handler-gap (doc/lookup only, adjudicated below), L2 elsewhere, L3 Test-generation (18/18, CI-wired, re-run live 2026-07-22 -- corrects this entry's stale '12/12': 6 registry_proof + 12 full_coverage_proof tests, matching pack.toml's own 'L5-push round-4' 18/18 claim, which this entry had never picked up). cap05 CLOSED 2026-07-22 (L5 wave3, cell wasm4pm-catalog-structural) -- found ALREADY CLOSED, not merely not-applicable, same class of finding as cargo-cicd-pack's cap05 (this file, cargo-cicd-pack entry): `full_coverage_proof.rs.tmpl` (12 tests) cross-checks TWO independently-issued SPARQL projections against each other with zero hardcoded EXPECTED array (its own doc-comment: 'walks wasm4pm_facts_registry::{BREEDS, ALGORITHMS} ... via a SEPARATE SPARQL SELECT than breed_algorithm_registry.md.tmpl uses'); `registry_artifact_proof.rs.tmpl` (6 tests) is, by its own documented design, a deliberately-kept hand-transcribed anti-tautology twin ('WITHOUT ever re-running the SPARQL that produced the artifact -- so none of these assertions can be tautological') -- the same hand-proof-plus-SPARQL-proof coexistence pattern star-toml-pack's cap05 closure established as acceptable, not a gap. Independently re-verified this pass: 18/18 re-run green; a real reversible drift test performed jointly with wasm4pm-algorithms-pack (see that entry) also exercised this pack's regeneration path end to end (wasm4pm_facts_registry.rs / full_coverage_proof.rs / BREED_ALGORITHM_REGISTRY.md all regenerated) -- all 18 tests stayed green through the edit and the revert. Handler-gap (cap02) adjudicated: genuinely, legitimately structural-N/A / L1-terminal by design, CONFIRMED (not merely asserted) by reading the generated code directly -- `src/wasm4pm_facts_registry.rs`'s `lookup_breed`/`lookup_algorithm`/`algorithms_by_category`/`breed_count`/`algorithm_count` are pure in-memory linear-search accessors over static `BREEDS`/`ALGORITHMS` tables: zero external calls, zero dispatch to any other system, zero I/O. This is a materially different shape from wasm4pm-algorithms-pack (which DOES dispatch to an external crate for 1/60 rows, see that entry) -- correcting this entry's and wasm4pm-algorithms-pack's prior grouping ('Handler-gap may be structurally N/A like wasm4pm-facts-pack', which treated the two packs as the same shape without checking). This pack alone matches the maturity model's own Matrix-1 L1 definition verbatim ('N/A -- nothing to handle') by design (pack.toml: 'Admitted wasm4pm graph facts ... carried verbatim') -- a legitimate terminal shape for this dimension, not a promotable gap. |
| chicago-tdd-tools-pack | 4 | promotion actively underway; ≥1 capability closed, ≥1 open | L2-L3 mixed (L2→L3 Ontology-expressiveness via shapes.ttl, L3 Test-generation 9/9 CI-wired, meta-proof asserting on its own generated proof's text). cap06 CLOSED 2026-07-22 (worktree wf_7aace947-05f-1): ontology.ttl's single incidental negative-witness (receiptctl-unknown-verb) is now a systematic 5-case sabotage suite -- three reuse ctt:composesCommand against a real required-arg cnv:Command (ReceiptEmit/UserCreate/SessionLogin) to hit clap's required-argument enforcement with the flag omitted, two use a hand-typed ctt:args literal to hit clap's subcommand-resolution (known noun, unrecognized verb) and unknown-argument enforcement (valid command, unrecognized flag) -- three distinct real clap-noun-verb failure modes, none fabricated: every exit code and stderrNeedle was transcribed from a live run of the built examples/receiptctl binary (stdout/stderr captured separately, matching CliHarness::run's own std::process::Command::output() capture) before being written into the ontology. Re-synced via a freshly-built target/debug/ggen 26.7.27 (never the stale global 26.7.13), hitting and remediating the same FM-PACK-008 (ggen.lock re-lock) and FM-WRITE-005 (stale generated-target delete) refusals praxis-core-pack/star-toml-pack's cap05 closures document -- not weakened. Before/after: 4/4 -> 9/9 tests in tests/chicago_tdd_tools_boundary.rs; tests/chicago_tdd_tools_boundary_proof.rs's meta-proof count moved 4->4 (unchanged test count, new content) while EXPECTED_TEST_COUNT moved 4->9. Idempotent: second `ggen sync run` reports every chicago-tdd-tools-pack output `unchanged: content identical`. Non-vacuity independently verified: flipped all 5 new individuals' ctt:expectExitCode 1->0, re-synced, re-ran `cargo test` and got exactly the predicted 4 passed / 5 failed (the untouched happy-path + the pre-existing unknown-verb test still green, all 5 new tests failing with a real exit-code-mismatch panic message), then restored ontology.ttl byte-for-byte (git diff empty), re-synced, and confirmed 9/9 green again. Full examples/receiptctl suite green throughout (`cargo test -q`, 14/14 test binaries `ok`, 0 failures). |
| wasm4pm-cognition-pack | 4 | promotion actively underway; ≥1 capability closed, ≥1 open | L2-L3 mixed (L2→L3 Ontology-expressiveness via shapes.ttl, L3 Test-generation 57/57 CI-wired) |
| wasm4pm-compat-pack | 4 | promotion actively underway; ≥1 capability closed, ≥1 open | L2 ceiling, L3 Test-generation (17/17, CI-wired via guard-pack-proofs.sh, real behavioral proof). cap06 CLOSED 2026-07-21 (commit da278d798, cherry-picked from worktree cell-d's 083051c49): packs/wasm4pm-compat-pack/templates/wasm4pm_compat_events_proof.rs.tmpl extends the one incidental DanglingEventObjectLink witness into a systematic sabotage suite covering all 5 real OcelRefusal variants the pinned wasm4pm-compat =26.6.29 crate declares (src/ocel.rs, confirmed against the exact crates.io registry checkout at that version) -- DanglingEventObjectLink (pre-existing) plus 4 new: EmptyEventObjectLinks (via LinkedOcel::admit, same gate as the existing test), MissingReferenceObject, MultipleReferenceObjects, and ViolatesLocalityPrinciple (all three via OcelLog::validate_gianola_2026_locality). Each new test starts from this pack's own generated emit_* output, same pattern as the pre-existing test. Independently re-verified in this reconciliation pass (not just re-running the cell's claim): ran `cargo test -q --test wasm4pm_compat_events_proof` fresh after cherry-picking -- 17/17 passing; re-probed non-vacuity myself by reverting the MultipleReferenceObjects sabotage condition (removing the extra event-object link) and re-running that single test -- it genuinely failed, then was restored byte-for-byte and the full 17/17 suite passed again. Idempotent: second `ggen sync run` reports no diff in src/tests/docs. Scope note: cap06's capDesc also names "compile-fail" fixtures as a distinct sub-modality; none were added here -- only refusal-path/adversarial-input coverage is closed, not a trybuild-style compile-fail suite. |
| affidavit-pack | 5 | promotion actively underway; ≥1 capability closed, ≥1 open | L1-L3 mixed, L4 Test-generation (46/46 incl. real adversarial error-path coverage: tamper-a-commitment, chain-tamper, forgery-rejection). cap01 PARTIAL 2026-07-21 (commit 5b29611cc, examples/affidavit-verify): src/affidavit_types.rs.tmpl's four plain-data structs (ObjectRef/OperationEvent/CheckOutcome/Verdict) plus ProfileId's variant<->string mapping are now projected from 20 new afd:DataStruct/afd:DataField/afd:ProfileIdVariant facts (4 afd:DataStruct + 15 afd:DataField + 1 afd:ProfileIdVariant, independently recounted) in ontology.ttl via a structs/fields/profile_variants sparql: frontmatter (same pattern as praxis-core-pack's/star-toml-pack's cap05 closures) -- no longer a hand-written struct literal for that subset. Verified live: ggen sync run against examples/affidavit-verify/ writes all 9 files, cargo test -- 46/46 pass (same count as round 4, generated text byte-identical to the old hand-written text). Idempotent: second sync run reports only the force:true affidavit_lib_wiring.rs written, rest skipped unchanged. Drift-sensitivity independently verified: editing afd:objectRefStruct_qualifier's afd:fieldType from Option<String> to Option<u32> and re-syncing (after the expected FM-PACK-008/FM-WRITE-005 refusals, resolved via re-lock + delete-stale-target, not by weakening either gate) changed the generated qualifier field's type to match; ontology.ttl confirmed reverted byte-for-byte, all 46 tests green again. |
| anti-llm-cheat-lsp-pack | 5 | promotion actively underway; ≥1 capability closed, ≥1 open | L1-L2 mixed, L4 Test-generation (34/34 incl. 8 verbatim reference tests) |
| cargo-cicd-pack | 5 | promotion actively underway; ≥1 capability closed, ≥1 open | L1→L2 mostly, L3 Test-generation (24/24, up from 15/15: 15 catalog + 9 new dispatch-behavior tests). Handler-gap CLOSED 2026-07-22 (cap02, worktree wf_7aace947-05f-5): cargo_cicd_dispatch.rs.tmpl generates src/cargo_cicd_dispatch.rs -- one real, compilable, callable std::process::Command wrapper fn per live command (51 of 52 catalog rows; the deprecated cc:doctor-bare empty-verb row is excluded via a second, independent `FILTER(?verb != "")` query, since `cargo-cicd doctor ""` is not a real invocation), mirroring mcpp-pack's already-proven mcpp_dispatch.rs.tmpl structure and invoking the real `cargo-cicd` binary (confirmed via ~/cargo-cicd/Cargo.toml's [[bin]] name). Verified live in a new committed consumer, examples/cargo-cicd-verify (mirrors examples/praxis-core-verify): real `ggen sync run` + `cargo test`, 24/24 passing (15 catalog + 9 dispatch, independently re-run), second sync byte-identical (idempotent), and drift-sensitivity proven via a real reversible ontology edit (cc:git-status's verb "status"->"statusdrift"->"status") -- the generated wrapper renamed accordingly and a hand-pinned spot-check FAILED TO COMPILE against the old name, then both reverted clean. Wired into scripts/ci/guard-pack-proofs.sh's CONSUMERS array (independently confirmed: examples/cargo-cicd-verify is listed there), full gate passes (3 consumers). |
| lsp-max-pack | 5 | promotion actively underway; ≥1 capability closed, ≥1 open | L2 ceiling, L3 Test-generation (12/12: 7 pre-existing hand-transcribed + 5 new SPARQL-derived, real regex/toml exercise); Target-API-fidelity UNVERIFIED. cap05 CLOSED 2026-07-21 (commit c3fae8346, cherry-picked from worktree cell-e's a8fe5661c): applies praxis-core-pack's cap05 reference pattern to lsp-max-pack's own lm:LintRule ontology shape. Unlike praxis-core-pack/star-toml-pack, neither of this pack's generated-source templates carries a freeze_policy key -- the real gap closed here is that rule_pack_proof.rs.tmpl's EXPECTED_RULES literal lives in the template's own Rust source text (no sparql: key at all), so it only changes on a hand-edit, never automatically from an ontology.ttl edit. Added rule_pack_sparql_derived_proof.rs.tmpl (real sparql: frontmatter, identical `rules` query to rule_pack.toml.tmpl, no freeze_policy, a from-scratch Tera for-loop building a LIVE_RULES array). Built examples/lsp-max-verify/ (no pre-existing consumer existed). Independently re-verified in this reconciliation pass (not just re-running the cell's claim): ran `cargo test -q` fresh after cherry-picking -- 7/7 + 5/5 = 12/12, matching the cell's counts exactly; independently reproduced the full drift-sensitivity experiment myself end to end (not merely re-reading the report): changed UnwrapRule's lm:severity from "error" to "warning" in ontology.ttl, hit and remediated the same two documented gate refusals the cell hit (FM-PACK-008 stale ggen.lock hash -- deleted to re-lock; FM-WRITE-005 differing-target refusal on 3 separate targets in turn -- deleted each stale target, no force:true used), then confirmed the new proof's LIVE_RULES re-derived to "warning" and passed (5/5) while the pre-existing hand-transcribed proof failed with the exact reported message ("severity mismatch for LSPMAX-UNWRAP-001, left: \"warning\", right: \"error\""); reverted ontology.ttl byte-for-byte (confirmed via git diff empty), re-locked, re-synced twice (files_generated=0 on the second run, confirming idempotency), and confirmed all 12 tests green again with a clean working tree. |
| mcpp-pack | 5 | promotion actively underway; ≥1 capability closed, ≥1 open | L1-L2 mixed, L3 Test-generation (18/18 incl. a real SHACL sabotage gate); Target-API-fidelity capped L1 by an UNRELATED external build break (wasm4pm-compat 26.6.29 fails to compile even on this repo's pinned nightly) |
| mfact-pack | 5 | promotion actively underway; ≥1 capability closed, ≥1 open | L1-L2 mostly, L3 Test-generation (14/14); Target-API-fidelity L1 (README-derived) |
| mfw-pack | 5 | promotion actively underway; ≥1 capability closed, ≥1 open | L1-L2 mostly, L3 Test-generation (13/13); Target-API-fidelity L1 (AGENTS.md-derived) |
| osx-clnr-pack | 5 | promotion actively underway; ≥1 capability closed, ≥1 open | L1-L2 mixed, L4 Test-generation (38/38, incl. 3 byte-identical verbatim reference tests -- from 2 of the real ~10 ~/osx-clnr/tests/*.rs files -- passing against generated code; round 2, 2026-07-20, added human_bytes as a genuinely SPARQL-derived fn reusing the existing oclnr:ByteUnit table, not a 3rd hand-transcription) |
| praxis-core-pack | 5 | promotion actively underway; ≥1 capability closed, ≥1 open | L2 ceiling, L3 Test-generation, CI-wired: examples/praxis-core-verify/ (commits 427270383/bdda5a341) makes praxis-core-live default-on. cap05 CLOSED 2026-07-21 (commit 92bbe5339): packs/praxis-core-pack/templates/refusal_taxonomy_sparql_derived_proof.rs.tmpl carries a real sparql: frontmatter key (no hardcoded EXPECTED array), run through a real ggen sync run against examples/praxis-core-verify/ (initial FM-PACK-008 ggen.lock hash-pin refusal fixed via re-lock, not by weakening the gate), 18/18 tests passing (10 hand-transcribed + 4 verbatim reference + 4 new SPARQL-derived). Drift-sensitivity independently re-verified: editing ontology.ttl's pxc:ResourceExhausted category from CategoryCapacity to CategoryTemporal and re-syncing changed LIVE_ONTOLOGY_ROWS's row and failed the OLD hand-transcribed proof (2 tests) while the new proof passed 4/4 against the changed live value; ontology.ttl confirmed reverted byte-for-byte to CategoryCapacity, all 18 tests green again. Idempotent: second sync run reports written: [] on all 6 outputs. scripts/ci/guard-pack-proofs.sh covers it via plain `cargo test -q`. cap09 CLOSED 2026-07-21 (commit f944775ba, cherry-picked from worktree cell-b's 737e970a1, this wave's reconciliation pass): cap09 is ggen sync run's own pipeline behavior, not pack-specific template logic -- scripts/ci/guard-pack-proofs.sh now runs `ggen receipt verify` (jq-checked on valid/signed/signature_valid) immediately after each consumer's sync, for both examples/receiptctl and examples/praxis-core-verify, failing the gate loudly if the receipt isn't valid and signed. Independently re-verified in this reconciliation pass (not just re-running the cell's claim): ran the full script fresh against a rebuilt release binary, all 3 consumers pass with the new receipt-verify step; separately tamper-tested fail-closed behavior directly against examples/praxis-core-verify's real .ggen-v2/receipt.json -- flipping one hex digit in chain_hash_hex makes `ggen receipt verify` exit 1 with "payload hash mismatch" naming both the stored and recomputed hash, restoring the byte-for-byte original makes it pass again (signature_valid: true). guard-pack-proofs is already a just pre-commit dependency, so this is reachable from the Definition of Done. |
| rmcp-pack | 5 | promotion actively underway; ≥1 capability closed, ≥1 open | L2-L3 domain-pack ceiling: single external crate (rmcp, github.com/modelcontextprotocol/rust-sdk, crates.io `rmcp = "2.2"`), source-audited 2026-07-21 by `cargo add rmcp --features server,client,macros,transport-io,transport-child-process` + `cargo fetch` into ~/.cargo/registry/src/.../rmcp-2.2.0/ and reading that vendored source directly, not docs.rs prose or ~/remo/rmcp (confirmed empty of Cargo.toml/src). 72 ontology individuals: 43 rmcp:HandlerMethod (all 27 ServerHandler + all 16 ClientHandler trait methods), 7 rmcp:Macro, 7 rmcp:Transport, 15 rmcp:Feature -- reconfirmed by direct grep of ontology.ttl this census pass. Test count 26/26, reconfirmed live this pass (`cargo test -q` in examples/rmcp-verify): 12 hand-transcribed (rmcp_handler_catalog_proof.rs, anti-tautology discipline, no sparql: frontmatter) + 5 real-behavior (rmcp_stub_handler_proof.rs, instantiates RmcpStubHandler and asserts on a real rmcp::model::Tool returned by the actual compiled crate) + 4 SPARQL-derived (cap05) + 5 refusal-path/adversarial-input (cap06). Six of 12 capabilities closed. cap03 (deterministic regen) -- CLOSED, re-verified live this pass: repeated `ggen sync run` after every edit reports `skipped: unchanged: content identical` for every freeze_policy:checksum output; the sole force:true output (src/rmcp_lib_wiring.rs) is byte-identical every re-sync (confirmed via git diff). cap04 (fixed-point convergence) -- CLOSED 2026-07-21 (commit fcff3b05c): deleted every generated file in examples/rmcp-verify (all of src/*.rs except the hand-written src/lib.rs, all of tests/, all of docs/) plus .ggen/freeze/rmcp-pack, ran one `ggen sync run` from that near-empty state -- all outputs `written`, byte-identical to the committed tree (git status --short empty), 26/26 tests passing against the freshly-rebuilt files. cap05 (generated verification) -- CLOSED 2026-07-21 (commit e8e61e8dc): packs/rmcp-pack/templates/rmcp_handler_catalog_sparql_derived_proof.rs.tmpl reapplies praxis-core-pack/star-toml-pack's wave-2 pattern -- real sparql: frontmatter (identical server_rows/client_rows queries to rmcp_handler_catalog.rs.tmpl), no freeze_policy, LIVE_SERVER_ROWS/LIVE_CLIENT_ROWS baked fresh every sync and asserted against src/rmcp_handler_catalog.rs's actual on-disk text via include_str!. Drift-sensitivity independently verified live: temporarily edited rmcp:server-call-tool's rmcp:defaultBehavior and re-synced -- the old hand-transcribed proof failed exactly as predicted (`assertion failed: row.default_behavior.contains("CallToolResult::error")`) while the new proof stayed 4/4 green against the changed live value; reverted byte-for-byte (git diff empty), pack hash matched pre-edit, 26/26 green again. cap06 (generated negative witnesses) -- CLOSED 2026-07-21 (commit e8e61e8dc): rmcp_default_handler_proof.rs.tmpl defines RmcpDefaultHandler with zero method overrides -- `impl ServerHandler for RmcpDefaultHandler {}` compiles because rmcp-2.2.0's own src/handler/server.rs:432-434 declares the entire trait body as one `server_handler_methods!()` macro invocation, confirmed by reading the real vendored source -- then calls the real, sync `get_tool()` default body and asserts `None` for an unregistered name, an empty name, and an adversarial path-traversal/injection-shaped name against the actual compiled rmcp crate, covering the refusal-path and adversarial-input sub-modalities. Compile-fail sub-modality independently verified live too: renamed rmcp:server-get-tool's methodName in the ontology, resynced, got a real `error[E0425]: cannot find value LIVE_GET_TOOL_DEFAULT_BEHAVIOR in this scope`; reverted byte-for-byte, resynced, 26/26 green again. cap07 (generated documentation) -- CLOSED (pre-existing from the phase-1 skeleton commit 4b4e16bbb, reconfirmed live this pass by reading its content): docs/rmcp_reference.md is a genuine SPARQL-SELECT-projected markdown table over all 4 classes (rmcp:HandlerMethod/rmcp:Macro/rmcp:Transport/rmcp:Feature), not hand-written prose describing generated code after the fact. cap09 (generated receipts) -- CLOSED 2026-07-21 (commit fcff3b05c): examples/rmcp-verify wired into scripts/ci/guard-pack-proofs.sh's CONSUMERS array. Independently re-verified in this census pass (not just re-running the prior phase's claim): ran the full script fresh against a rebuilt debug ggen binary (26.7.28) across all 5 wired consumers -- `guard-pack-proofs: OK (5 consumers: sync clean, idempotent, receipt valid+signed, all generated proofs pass)`; separately confirmed via a direct `ggen receipt verify` against examples/rmcp-verify's live .ggen-v2/receipt.json: `{"valid": true, "outputs": 11, "signed": true, "signature_valid": true}`. guard-pack-proofs is already a `just pre-commit` dependency, so this is reachable from the Definition of Done. |
| star-toml-pack | 5 | promotion actively underway; ≥1 capability closed, ≥1 open | L2 ceiling, L3 Test-generation, CI-wired 2026-07-21 (commit 4f145bf31, cherry-picked from worktree cell-a's 39fe8f5eb -- examples/star-toml-verify added to scripts/ci/guard-pack-proofs.sh's CONSUMERS array, mirroring examples/praxis-core-verify's posture; independently re-verified in this reconciliation pass by running the full script fresh against a rebuilt release binary -- all 3 consumers, including star-toml-verify, pass with 15/15 tests: 8 hand-transcribed + 7 SPARQL-derived). cap05 CLOSED 2026-07-21 (commit 7e8e6556d): packs/star-toml-pack/templates/star_toml_config_sparql_derived_proof.rs.tmpl carries a real sparql: frontmatter (identical a_sections/fields/bounds queries to star_toml_config.rs.tmpl), no hardcoded EXPECTED array -- from-scratch Tera for-loops build LIVE_SECTIONS/LIVE_FIELDS/LIVE_BOUNDS. Run through a real ggen sync run against the newly-built examples/star-toml-verify/ (initial FM-PACK-008 ggen.lock hash-pin refusal and FM-WRITE-005 frozen-target refusal, both fixed via documented remediation -- re-lock and delete-stale-target -- not by weakening the gate), 15/15 tests passing (8 hand-transcribed + 7 new SPARQL-derived). Independently re-verified 2026-07-21: a first re-verification pass using the stale globally-installed ggen 26.7.13 produced a spurious retry_count Option<u32>-vs-u32 regression unrelated to any ontology edit; re-running the identical steps with the correctly rebuilt workspace target/debug/ggen (26.7.25) reproduced the reported 15/15 result exactly, confirming the discrepancy was a binary-staleness artifact of the verification pass, not a defect in the commit under review. Drift-sensitivity independently re-verified with the correct binary: editing ontology.ttl's SampleRateField stp:maxValue from 1.0 to 2.0 and re-syncing changed LIVE_BOUNDS's sample_rate row from (telemetry, sample_rate, 0, 1) to (telemetry, sample_rate, 0, 2) and the generated validate() guard/message to match; the new SPARQL-derived proof's 7/7 tests passed against the changed live value while the old hand-transcribed proof failed exactly as predicted -- out_of_range_sample_rate_parses_but_fails_validate panicked on its hardcoded sample_rate=1.5 probe ("validate() must reject sample_rate=1.5 against stp:minValue=0.0/stp:maxValue=1.0"), now in-range under the live [0,2] bound. ontology.ttl confirmed reverted byte-for-byte to maxValue=1.0 (git diff empty) and resynced; all 15 tests green again, working tree clean. |
| wasm4pm-pack | 5 | promotion actively underway; ≥1 capability closed, ≥1 open | L1-L2 mostly, L3 Test-generation (10/10) |
| dogfood-lifecycle-pack | 6 | counted, not yet adjudicated against the 12-capability bar | L1-L2 across the board per .specify/maturity.ttl (Obligation-lifecycle L1, Actuation-closure receipting 'UNVERIFIED') |
| ma-case-study-pack | 6 | counted, not yet adjudicated against the 12-capability bar | Uniform L2 across all 7 case-study dimensions per .specify/maturity.ttl -- the least-differentiated pack in the whole scored population |
| self-monitoring-pack | 6 | promotion actively underway; ≥1 capability closed, ≥1 open | L2-L3 mixed (Derivation-power L3, Fire-precision L2→L3, rest L1-L2) |
| claude-code-pack | 7 | counted, not yet adjudicated against the 12-capability bar | UNSCORED against the 12-capability bar; real generation machinery exists (4 templates in packs/claude-code-pack/templates/: agent_independent_verifier.md.tmpl, agent_pack_transcriber.md.tmpl, implement_verify_workflow.js.tmpl, skill_l5_pack_method.md.tmpl) but no consumer is committed inside this repo -- it is exercised only via a TempDir-based test (crates/ggen-engine/tests/composed_packs_e2e.rs), never a permanent examples/ fixture like examples/cargo-cicd-verify or examples/affidavit-verify. Real, disclosed, not glossed over. |
| ggen-constitution-pack | 7 | promotion actively underway; ≥1 capability closed, ≥1 open | First-ever adjudication (2026-07-21, cell-f census; independently re-verified in this wave's reconciliation pass -- all commands below re-run fresh, not merely re-read from the report). Real, live-verified evidence for cap03/cap07/cap12, real gaps confirmed for the rest. cap03 (deterministic regen) -- holds live: a real root `ggen sync run --dry-run` against this checkout today reports `CONSTITUTION.md: skipped: unchanged: content identical`; `crates/ggen-engine/tests/constitution_obligation_e2e.rs` (3/3 passing, re-run live in this reconciliation pass) proves `praxis_core::receipt_epoch::ObligationCount` tracks `ccn:mechanized` from a live SPARQL query, sabotage-tested (flipping one law's `ccn:mechanized` false->true drops `required` by exactly 1) -- against a SYNTHETIC 5-law fixture, not this pack's real 14-law ontology; `crates/ggen-engine/tests/composed_packs_e2e.rs`'s `thirty_packs_compose_into_one_consumer` (2/2 passing, re-run live in this reconciliation pass) spot-checks `docs/CONSTITUTION-pack.md` exists after a real 30-pack `ggen sync run` and confirms byte-identical second-sync. cap07 (generated docs) -- real: `CONSTITUTION.md` is a genuine, current, root-generated SPARQL-SELECT-over-`ccn:Law` mirror (14 rows, `ORDER BY ?num`), not hand-written prose. cap02 (generation surface) -- real caveat, not closed: `packs/ggen-constitution-pack/templates/constitution_pack.md.tmpl` (-> `docs/CONSTITUTION-pack.md`) is NEVER actually produced by this repo's own root `ggen.toml` (confirmed: no `[[packs]]`/`[packs]` table exists there at all; only `root-templates/constitution.md.tmpl` -> `CONSTITUTION.md` is wired, via the `docs-constitution-mirror` declarative rule) -- it is proven live only via `composed_packs_e2e.rs`'s synthetic frontmatter-schema consumer, never exercised in this repo's own committed tree. Genuine, newly-discovered defect this pass: `ccn:LawVII`'s own `ccn:enforcedBy`/`ccn:note` ('crates/ggen-engine/src/sync.rs:1857 currently hardcodes andon: Andon::Green unconditionally') is now STALE -- direct read of the live file today shows `write_receipt` at line 1991 (not 1857), and line 2483-2485's own code comment states plainly: 'The legacy top-level Andon field is derived from the same v2 precedence, not hardcoded -- this is the hardcoded-Green literal this migration replaces.' (independently reconfirmed in this reconciliation pass: the literal string 'andon: Andon::Green' does not occur anywhere in sync.rs). This matches `packs/ggen-release-pack/ontology.ttl`'s OWN condition-12 text ('derive_andon now genuinely computes andon ... replacing the prior hardcoded-Green write sites') almost verbatim -- ggen-constitution-pack's own admitted-semantics mirror is currently self-contradicted by a sibling pack's more current evidence, exactly the kind of divergence Principle XIV's Governance clause names as a defect to resolve, caught here rather than papered over. `ccn:LawVII` likely should flip toward `ccn:mechanized true` (or at minimum get a corrected citation) on a future pass; not changed here since this pass is adjudication/reconciliation, not pack editing. Re-verified live: 14 `ccn:Law` individuals, 5 `ccn:mechanized true` / 9 `ccn:mechanized false` (matches `packs/ggen-release-pack/ontology.ttl`'s own condition-13 count exactly). |
| ggen-release-pack | 7 | promotion actively underway; ≥1 capability closed, ≥1 open | First-ever adjudication (2026-07-21, cell-f census). cap03 (deterministic regen) -- holds live: a real root `ggen sync run --dry-run` against this checkout today reports all 5 of this pack's outputs (`.github/workflows/publish-candidate.yml`, `.github/workflows/marketplace-docs.yml`, `RELEASE_STANDING.json`, `RELEASE_STANDING.md`, `docs/PUBLICATION_JUDGMENT.md`) as `skipped: unchanged: content identical`; `composed_packs_e2e.rs` (2/2 passing, re-run live in this reconciliation pass) independently confirms idempotence as part of a real 30/31-pack union, though its own spot-check list covers 6 OTHER packs, not this one. Correction to cell-f's original census (this reconciliation pass independently re-ran the claim rather than trusting either the cell or the wave's own auditor): the pack's only dedicated proof, `crates/ggen-engine/tests/ggen_release_pack_workflow_e2e.rs` (the sole test anywhere naming PublishCandidateWorkflow/RELEASE_STANDING/PUBLICATION_JUDGMENT/marketplace-docs.yml), is GREEN, not red. Cell-f's original report claimed '0/2 -- currently RED' with an `[FM-CONFIG-002]` schema-dispatch failure; that specific claim did NOT reproduce and is corrected here rather than passed through. The wave's independent auditor re-ran it 3 times fresh against a clean build and got 2/2 every time; this reconciliation pass independently re-ran it 3 more times on top of that (6 consecutive green runs total, across two independent sessions) -- `marketplace_docs_workflow_regenerates_byte_identical_to_committed_file` and `marketplace_docs_workflow_second_sync_is_idempotent` both pass consistently, no FM-CONFIG-002 seen. Per this repo's own CLAUDE.md note that builds can flap nondeterministically under concurrent worktree sessions, the most plausible account is a transient environment artifact specific to cell-f's own worktree session at the time -- not a diagnosed code defect, and not reproducible now. cap05 (generated verification) -- the pack's clearest, most severe REAL gap, independently reconfirmed this pass: of 5 real outputs, only marketplace-docs.yml has any dedicated, exercised proof coverage; publish-candidate.yml is named only in a doc-comment inside ggen_release_pack_workflow_e2e.rs, never actually asserted against by either of its 2 real test functions, and RELEASE_STANDING.json, RELEASE_STANDING.md, docs/PUBLICATION_JUDGMENT.md have ZERO references anywhere in crates/*/tests/. No test is generated FROM the ontology either (no sparql: frontmatter deriving expected values -- same structural gap already disclosed for tcps-release-pack/star-toml-pack pre-cap05-closure). cap01 (authoritative source) -- structurally weak by the pack's own admitted design (pack.toml: 'following the literal-template-body pattern of packs/tcps-release-pack' -- whole pre-formatted blobs stored as grp:workflowBody/grp:artifactBody literals, not composed from finer facts) AND concretely stale, independently reconfirmed this pass: RELEASE_STANDING.json/.md and PUBLICATION_JUDGMENT.md's literal bodies all hardcode 'v26.7.20' while the live project version (Cargo.toml/ggen.toml [project].version) is '26.7.26' -- 6 patch releases stale -- and scripts/ci/guard-version-consistency.sh (this pack's own generated CI step, independently re-read this pass) does NOT catch it: that script only compares .specify/repo-facts.ttl/Cargo.toml/ggen.toml, never this pack's own generated artifacts. cap02 (generation surface) -- asymmetric: packs/ggen-release-pack/templates/*.tmpl (4 files) self-produce publish-candidate.yml/RELEASE_STANDING.json/RELEASE_STANDING.md/docs/PUBLICATION_JUDGMENT.md from a generic [packs]-composed consumer alone, but .github/workflows/marketplace-docs.yml needs a ROOT-repo-only declarative rule plus a Tera template living OUTSIDE the pack (.specify/templates/docs/ggen_marketplace_docs_workflow.yml.tera) -- a generic downstream consumer of just this pack cannot reproduce that 5th output. cap07 (generated docs) -- real: docs/PUBLICATION_JUDGMENT.md is genuinely SPARQL-SELECT-rendered from grp:PublicationJudgmentArtifact, confirmed current live. cap06 -- zero: no gates/ directory exists for this pack at all (unlike ggen-constitution-pack's 2, even-inert, gates). |
| gh-terraform-pack | 7 | counted, not yet adjudicated against the 12-capability bar | UNSCORED against the 12-capability bar; real generation machinery exists (49 templates in packs/gh-terraform-pack/templates/, independently recounted) but, same shape of gap as claude-code-pack, no consumer is committed inside this repo -- only TempDir-based e2e tests (crates/ggen-engine/tests/gh_terraform_pack_e2e.rs, gh_terraform_acceptance_e2e.rs) and external sandbox repos exercise it. |
| github-actions-pack | 7 | counted, not yet adjudicated against the 12-capability bar | UNSCORED against the 12-capability bar; wave7 census (2026-07-22) found this pack's OWN ontology.ttl self-description (its gha:L5Condition individuals) is stale and understates its real state, re-verified directly against the filesystem this pass: gha:L5C03 claims 'No templates exist at this commit' (false -- packs/github-actions-pack/templates/ has 4: emit_evidence_action_yml.tmpl, reusable_rust_inspection_caller_yml.tmpl, reusable_rust_inspection_workflow_yml.tmpl, setup_ggen_action_yml.tmpl), gha:L5C06 claims 'gates/ directory empty at this commit' (false -- packs/github-actions-pack/gates/ has 2: 010_required.rq, 020_security.rq), and gha:L5C07 claims 'github_actions_pack_e2e.rs not yet written' (false -- crates/ggen-engine/tests/github_actions_pack_e2e.rs exists). |
| level-five-book-pack | 7 | counted, not yet adjudicated against the 12-capability bar | UNSCORED against the full 12-capability bar, but wave7 census (2026-07-22) independently re-verified the strongest cap09/cap08-adjacent evidence found anywhere in this file: book/.ggen-v2/receipt-log.jsonl has 17 real chained entries (`wc -l` re-confirmed live) and book/MANIFEST.sha256 covers 1172 real generated files (`wc -l` re-confirmed live), not transcribed from the census report unchecked. |


## 2. Level 5 capability model (12 required capabilities)

| # | Capability | Definition |
|---|---|---|
| 01 | Authoritative semantic source | The pack's ontology is the sole source of truth for what gets built; no fact lives only in a template or hand-written module. |
| 02 | Complete generation surface | Every file the consumer needs (types, logic, tests, docs, provenance, release surface) is generated -- no hand-authored production file required. |
| 03 | Deterministic regeneration | The same ontology + templates produce byte-identical output on repeated ggen sync run, verified live, not asserted. |
| 04 | Fixed-point convergence | Regeneration from a fully-deleted consumer converges to the same result as incremental regeneration -- erase-and-rebuild is a real, exercised path. |
| 05 | Generated verification | The proof suite is itself generated from the ontology (not hand-written against generated code), and its expected values derive from ontology facts, not hardcoded literals. |
| 06 | Generated negative witnesses | Sabotage/falsification fixtures (compile-fail, refusal-path, adversarial input) are generated, proving the pack fails closed, not just that it succeeds. |
| 07 | Generated documentation | Reference docs are projected from the ontology, not hand-written prose describing generated code after the fact. |
| 08 | Generated provenance | Every generated artifact traces to the ontology facts and template version that produced it (source-manifest / digest chain), machine-checkable. |
| 09 | Generated receipts | Sync emits a cryptographic receipt covering this pack's contribution, chained and verifiable (ggen receipt verify). |
| 10 | Generated release surface | Packaging/CI/release artifacts for the generated subsystem are themselves generated, not hand-maintained alongside it. |
| 11 | Generated evolution path | An upstream ontology or target-API change flows through to a re-synced consumer with a semantic diff and no manual repair. |
| 12 | Consumer replacement | A consumer with zero knowledge of the target's own implementation can delete its generated tree and regenerate a working, tested, receipted subsystem from ggen.toml + the pack alone. |


## 3. Pack promotion dependency graph

Wave placement is the current dependency-ordering proxy (see `.specify/pack-l5-promotion.ttl`'s
`l5p:PromotionWave` individuals) until a finer per-pack `l5p:dependsOn` edge set is authored.
Packs in an earlier wave are promotion prerequisites for packs in a later wave that reuse their
vocabulary or generated surface.

### Wave 1 — Level 5 reference packs

The 6 TCPS packs: furthest along today, source of the fidelity-oracle pattern (examples/tcps-generated) every later wave reuses.

- **ggen-verify-pack** — furthest-advanced pack(s); source of reusable promotion machinery, itself not yet full Level5
- **tcps-cli-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **tcps-core-pack** — furthest-advanced pack(s); source of reusable promotion machinery, itself not yet full Level5
- **tcps-ffi-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **tcps-release-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **tcps-std-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **tcps-wasm-pack** — promotion actively underway; ≥1 capability closed, ≥1 open


### Wave 2 — Reusable Level 5 promotion machinery

Extract from wave 1: generic idempotent-regen verification, generic CI-wiring gate, a generation-rule pattern for ontology-derived (not hardcoded) test expectations, a generic negative-witness/sabotage harness pattern. The ontology-derived-test-expectations pattern is now proven, not just designed: a sparql:-frontmatter proof template (packs/praxis-core-pack/templates/refusal_taxonomy_sparql_derived_proof.rs.tmpl, commit 92bbe5339) ran through a real ggen sync run, passed 4/4, and was shown drift-sensitive against a live ontology.ttl edit -- praxis-core-pack is the reference implementation; star-toml-pack is the named next pack to reapply it to.



### Wave 3 — Foundational packs

Packs other packs' ontologies reuse vocabulary from (clap-noun-verb-pack's cnv: namespace is reused by mcpp-pack and cargo-cicd-pack; wasm4pm-facts-pack/wasm4pm-algorithms-pack underlie wasm4pm-cognition-pack).

- **clap-noun-verb-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **wasm4pm-algorithms-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **wasm4pm-facts-pack** — promotion actively underway; ≥1 capability closed, ≥1 open


### Wave 4 — Composition packs

Packs whose generated surface composes multiple foundational vocabularies (wasm4pm-cognition-pack, wasm4pm-compat-pack, chicago-tdd-tools-pack).

- **chicago-tdd-tools-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **wasm4pm-cognition-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **wasm4pm-compat-pack** — promotion actively underway; ≥1 capability closed, ≥1 open


### Wave 5 — Domain packs

Packs modeling one external target crate/repo's domain (affidavit-pack, osx-clnr-pack, mfact-pack, mfw-pack, cargo-cicd-pack, mcpp-pack, anti-llm-cheat-lsp-pack, star-toml-pack, praxis-core-pack, wasm4pm-pack, lsp-max-pack).

- **affidavit-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **anti-llm-cheat-lsp-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **cargo-cicd-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **lsp-max-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **mcpp-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **mfact-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **mfw-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **osx-clnr-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **praxis-core-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **rmcp-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **star-toml-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **wasm4pm-pack** — promotion actively underway; ≥1 capability closed, ≥1 open


### Wave 6 — Interface packs

Knowledge-hook and case-study packs: self-monitoring-pack, dogfood-lifecycle-pack, ggen-verify-pack, ma-case-study-pack -- governance/verdict surfaces layered over the domain packs.

- **dogfood-lifecycle-pack** — counted, not yet adjudicated against the 12-capability bar
- **ma-case-study-pack** — counted, not yet adjudicated against the 12-capability bar
- **self-monitoring-pack** — promotion actively underway; ≥1 capability closed, ≥1 open


### Wave 7 — Consumer packs

Packs describing ggen/repo-self surfaces: claude-code-pack, gh-terraform-pack, github-actions-pack, level-five-book-pack, ggen-constitution-pack, ggen-release-pack -- census-pending as of this program's creation, unscored in .specify/maturity.ttl.

- **claude-code-pack** — counted, not yet adjudicated against the 12-capability bar
- **ggen-constitution-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **ggen-release-pack** — promotion actively underway; ≥1 capability closed, ≥1 open
- **gh-terraform-pack** — counted, not yet adjudicated against the 12-capability bar
- **github-actions-pack** — counted, not yet adjudicated against the 12-capability bar
- **level-five-book-pack** — counted, not yet adjudicated against the 12-capability bar


### Wave 8 — Maximal closure

Every non-terminal pack has reached Level5/Incompatible/Superseded/RejectedWithProof; compute MaximalClosure(P_L5).



### Wave 9 — autosel

Generate autosel from MaximalClosure(P_L5). Not before wave 8 completes.



## 4. Shared promotion machinery map

Wave 2 ("Reusable Level 5 promotion machinery") is the explicit charter for this artifact —
see the wave-2 entry in section 3 above. Populated as machinery is actually extracted and
proven reusable across ≥2 packs, not speculatively.

## 5. Promotion-wave plan

See section 3 (dependency graph) — the 9 waves ARE the plan, ordered `l5p:order` "1".."9".

## 6. Per-pack promotion obligations

| Pack | Missing capabilities | Next obligation |
|---|---|---|
| ggen-verify-pack | Generated negative witnesses, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | Doc's own words: loop is 'operator-triggered, not unattended' -- closing cap11 means making the derive->actuate->receipt->re-observe loop run without a human re-triggering sync. |
| tcps-cli-pack | Generated verification | cap06 closed 2026-07-22 (see currentCeiling). 1 capability remains open: cap05 -- same posture as tcps-ffi-pack/tcps-wasm-pack (0/0 tests, honestly disclosed, reference ships none either); requires authoring real generated tests. Not attempted this pass. |
| tcps-core-pack | Generated provenance, Generated release surface, Generated evolution path | cap06 closed 2026-07-22 (see currentCeiling). 3 capabilities remain open: cap08, cap10, cap11 -- investigated this pass (real findings, not yet closing any): cap08 (Generated provenance) -- confirmed still genuinely open: the live sync receipt (.ggen-v2/receipt.json) DOES carry a per-generated-file admission log (evidence_id + decision + reason, e.g. 'skipped: unchanged: content identical') and a pack-level content_hash table (ggen.lock), but neither traces a specific generated file to the SPECIFIC ontology individual(s) and template CONTENT VERSION that produced it -- only to the whole pack's aggregate hash. tcps-release-pack's own source_manifest_sha256.tmpl (packs/tcps-release-pack/templates/) looks like a candidate fix but is a hand-authored static blob (same literal-transcription pattern as its other templates), not a live-computed digest chain -- confirmed by reading it: it renders one hardcoded `tcps:artifactBody` literal verbatim, so it cannot be cap08's proof (this is also why tcps-release-pack's own missingCap still lists cap08). A real closure needs either an engine-level per-artifact provenance field (out of scope for pack-only work) or a new generated per-file header/manifest computed AT SYNC TIME from the actual consumed facts, plus a script that recomputes and checks it -- not attempted this pass, sizing it correctly matters more than a decorative stand-in (see coding-agent-mistakes.md's Decorative Completion class). cap10 (Generated release surface) -- tcps-release-pack (a sibling pack composed into the SAME examples/tcps-generated consumer) already generates the CI/packaging/release surface for the TCPS family as a whole, so tcps-core-pack's own missingCap here may be double-counting rather than a real independent gap; not adjudicated this pass -- needs a decision on whether cap10 is scored per-pack or per-ecosystem-composition before further work, not a fabricated closure either way. cap11 (Generated evolution path) -- unchanged from the prior investigation (no diffing tool exists; only demonstrated that a live ontology edit propagates through re-sync with a no-manual-repair guarantee, which this pass's own non-vacuity checks reconfirm live for cap06's fixtures, but that is not the same as a semantic-diff surface). |
| tcps-ffi-pack | Generated verification, Generated negative witnesses | cap05 requires authoring real generated tests where the reference crate has none -- a genuine gap, not a doc omission. cap06 investigated 2026-07-22 (wave3 tcps-cluster cell), NOT closed: a real, genuine refusal path exists and was identified -- tcps_select_v1's 選択する外部関数 (packs/tcps-ffi-pack/ontology.ttl's LibRsModule sourceText) delegates to tcps-core's 自動選択::選択する, which returns 選択結果::拒否{理由,...} (encoded as 種別=2 in the C-ABI 外部選択結果 struct) for policy-rejected requests, vs 選択結果::選択(提案) (種別=1) for accepted ones -- the exact same reusable pattern tcps-cli-pack's cap06 closure used for a CLI-argv refusal path, just at the C-ABI boundary instead. NOT closed this pass because, unlike tcps-cli-pack's binary dispatch (a simple string match) or tcps-std-pack's single io::Result branch, constructing genuinely rejecting vs accepting 外部選択要求 inputs requires tracing 自動選択::選択する's real policy-matching logic against 正準方策() (examples/tcps-generated/src/自動選択.rs's 測度/候補/方策 machinery, not yet read in enough depth this pass to derive correct boundary values without guessing) -- closing this without that derivation would risk a fixture that happens to pass rather than one that is provably tied to the real accept/reject boundary, which the coding-agent-mistakes.md Decorative Completion class warns against exactly this shortcut. |
| tcps-release-pack | Generated verification, Generated negative witnesses, Generated provenance | cap05: 'no generated test suite of its own; correctness checked by the workspace-level cargo build/test/clippy inspection receipt instead' (doc's own words) -- author a real generated proof specific to the release surface itself. Not audited further this pass (wave3 tcps-cluster cell prioritized tcps-core/std/cli's cap06 closures instead, see those packs' currentCeiling) -- this pack's own 77-template surface (the largest in the cluster by far) makes a real cap05/cap06 closure a substantially bigger lift than the other 5 packs; deferred, not silently skipped. One real finding from this pass: templates/source_manifest_sha256.tmpl renders a single hand-authored `tcps:artifactBody` literal verbatim (same static-blob pattern as this pack's other templates), not a live-computed digest -- confirmed NOT a cap08 closure candidate despite the name, so cap08 stays correctly listed as open here too. |
| tcps-std-pack | Generated verification | cap06 closed 2026-07-22 (see currentCeiling). 1 capability remains open: cap05 -- 1/1 pre-existing test (受領証文字列は日本語項目を保つ) is real but thin; grow real generated proof coverage (SPARQL-derived, not the hand-written #[cfg(test)] mod already in tcps_std:sourceText) before claiming cap05 closed. Not attempted this pass. |
| tcps-wasm-pack | Generated verification, Generated negative witnesses | same as tcps-ffi-pack: cap06 investigated 2026-07-22, NOT closed -- tcps_wasm_select_canonical_v1 (packs/tcps-wasm-pack/ontology.ttl's LibModule sourceText) wraps the exact same tcps-core 自動選択::選択する / 選択結果::拒否 refusal path as tcps-ffi-pack's tcps_select_v1, just bit-packed into a single u64 return value (top bit set = accept, else = the numeric 理由番号) instead of a repr(C) struct. Same blocker as tcps-ffi-pack: needs the real 自動選択::選択する policy-boundary derivation (正準方策()/測度/候補), not yet done this pass -- closing both tcps-ffi-pack and tcps-wasm-pack's cap06 together (they share the identical underlying refusal path) is the efficient next step once that derivation exists, not two separate investigations. |
| clap-noun-verb-pack | Complete generation surface, Generated negative witnesses, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | cap05 closed 2026-07-21 (see currentCeiling) via a second, independent live-SPARQL proof alongside the frozen embedded one. 6 capabilities remain open: cap02, cap06, cap08, cap09, cap10, cap11. Handler-gap (cap02): every generated verb still delegates to a hand-written handler -- close by moving common dispatch behavior into the ontology as behavior contracts, the wave-2 machinery's central target. |
| wasm4pm-algorithms-pack | Complete generation surface, Generated negative witnesses, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | cap05 closed 2026-07-22 (see currentCeiling), verified with a real live drift test, not just template inspection. cap02 (Handler-gap) is real-but-narrow, not zero and not N/A: the one composable dispatch arm requires no pack-specific hand-written domain logic (the behavior lives entirely in the pinned external crate), but the other 59/60 rows are structurally unreachable (wasm4pm itself cannot be a native dependency per CLAUDE.md's process-intelligence-boundary rule), not merely undone -- so this pack cannot single-handedly close cap02 by adding more dispatch arms. The wider cap02 gate this pack shares with every other receiptctl-wired pack is the consumer's own lib.rs: `include!("ggen_pack_mods.rs")` plus hand-written `pub mod verbs;`/`run()` (independently re-confirmed by reading examples/receiptctl/src/lib.rs directly this pass) -- same generic seam praxis-core-pack's cell-b investigation already named (this file's praxis-core-pack entry: 'no accepted precedent anywhere in this repo for a single include! line closing cap02'), not a defect specific to this pack. 5 capabilities remain genuinely unaudited this pass: cap06, cap08, cap09, cap10, cap11. Note for reconciliation (not closed here, out of this cell's assigned-pack scope): `ggen receipt verify` against examples/receiptctl (which wires this pack) returned valid:true/signed:true/signature_valid:true this pass, using the SAME guard-pack-proofs.sh mechanism praxis-core-pack's cap09 closure (commit f944775ba) cited as sufficient for cap09 -- that argument, if sound, would generalize to every pack wired into any of guard-pack-proofs.sh's 4 CONSUMERS (this pack, wasm4pm-facts-pack, clap-noun-verb-pack, chicago-tdd-tools-pack, wasm4pm-cognition-pack, wasm4pm-compat-pack via receiptctl; star-toml-pack; cargo-cicd-pack), yet only praxis-core-pack's entry reflects it -- flagging the inconsistency for whoever reconciles cap09 across packs rather than closing it unilaterally here. |
| wasm4pm-facts-pack | Complete generation surface, Generated negative witnesses, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | cap05 closed 2026-07-22 (see currentCeiling). Handler-gap adjudicated legitimately N/A/L1-terminal for THIS pack specifically (see currentCeiling) -- do not force a fabricated dispatch surface onto a pure fact-registry pack merely to move a number; the honest ceiling is that cap02 as a WHOLE still doesn't close, for the same shared reason as every other receiptctl-wired pack (src/lib.rs's hand-written `include!("ggen_pack_mods.rs")` plus hand-written `pub mod verbs;`/`run()` -- see wasm4pm-algorithms-pack's entry and praxis-core-pack's cell-b investigation, this file's praxis-core-pack entry), not a defect unique to this pack. Also re-confirmed, already-disclosed by this pack's own WIRING.md (not this pass's discovery): the bundled `ontology/rules/breed_standing.n3` law rule is NOT activated in examples/receiptctl -- `grep -n law examples/receiptctl/ggen.toml` returns nothing -- so every breed row in the committed consumer renders the `ADMITTED` placeholder rather than the law-derived `EvidenceBound` standing; relevant to Consumer-effort/cap01 more than cap02. 5 capabilities remain genuinely unaudited this pass: cap06, cap08, cap09, cap10, cap11. Same cap09 reconciliation flag as wasm4pm-algorithms-pack's entry applies here too (see that entry) -- not unilaterally closed here, out of this cell's assigned-pack authority to decide for the other packs the same argument would touch. |
| chicago-tdd-tools-pack | Complete generation surface, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | cap02 investigated 2026-07-22, NOT closed -- found structurally entangled with clap-noun-verb-pack's own, separately-tracked cap02 gap (line 143 of this file: 'every generated verb still delegates to a hand-written handler'), not something chicago-tdd-tools-pack can close alone: the CLI this pack boundary-tests (examples/receiptctl) has real hand-written logic in src/verbs/handlers.rs precisely so the boundary tests exercise real observable behavior, not trivial static responses -- eliminating that hand-written seam (e.g. by pointing every ctt:CliBoundaryTest at only zero-handler cnv:staticResponse commands like SessionPing) would close the metric without closing the capability's intent, and generating a wholly separate, self-contained reference CLI purely for this pack's own tests would duplicate clap-noun-verb-pack's job rather than chicago-tdd-tools-pack's own (CLI *boundary-testing*, not CLI generation). Same adjudicate-before-promoting posture as wasm4pm-facts-pack/wasm4pm-algorithms-pack's own Handler-gap notes. cap06 closed this pass (see currentCeiling) -- 4 capabilities remain open: cap02 (see above), cap08, cap09, cap10, cap11, none audited this pass. |
| wasm4pm-cognition-pack | Complete generation surface, Generated negative witnesses, Generated provenance, Generated receipts, Generated release surface, Generated evolution path, Consumer replacement | Dispatch handler is 'fails-closed, unwired to the real wasm-only implementation' by the pack's own deliberate design (native/wasm boundary) -- cap12 (consumer replacement) may be structurally blocked without a native-callable reference; name as Incompatible-pending-proof if unresolvable, do not leave silently InProgress forever. |
| wasm4pm-compat-pack | Complete generation surface, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | cap06 closed 2026-07-21 (see currentCeiling) -- systematic OcelRefusal sabotage suite, not one incidental case (compile-fail sub-modality still open, see scope note). 5 capabilities remain open: cap02, cap08, cap09, cap10, cap11 -- none audited in this pass, no priority order established yet. |
| affidavit-pack | Authoritative semantic source, Generated negative witnesses, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | cap01 is NOT closed by the 2026-07-21 partial fix above -- stated precisely, not rounded up. Still hand-written, hardcoded, NOT ontology-derived: Blake3Hash's methods/Display (thin blake3-crate delegation), Receipt's struct body/sealed()/custom Deserialize (the chain-hash forgery check -- real security logic), canonical_bytes/sort_value (generic JSON-canonicalization algorithm), and all of src/affidavit_chain.rs (ChainAssembler's rolling-BLAKE3 fold_event loop) and src/affidavit_verifier.rs (the six stage_* functions' real format/hash/commitment/threshold checks). These are genuine algorithmic/cryptographic logic, not enumerable data facts -- SPARQL-projecting them would require a computation-IR vocabulary in the ontology, not attempted this round, assessed as too large for a single pass. cap01 stays in missingCap until that logic either gets a real ontology-derived expression or a reasoned, named argument for why it structurally cannot (mirroring how cap02's Handler-gap question is already flagged 'adjudicate' for other packs rather than silently left InProgress forever). |
| anti-llm-cheat-lsp-pack | Complete generation surface, Generated negative witnesses, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | Handler-gap L1 by deliberate design (reference-only, avoids a heavy lsp_max dependency) -- same structural-N/A question as wasm4pm-facts-pack/wasm4pm-algorithms-pack; adjudicate rather than assume promotable. |
| cargo-cicd-pack | Generated negative witnesses, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | cap05 investigated 2026-07-22 and found ALREADY CLOSED, not merely not-applicable -- independently re-verified by reading both templates directly: tests/cargo_cicd_catalog_proof.rs.tmpl and tests/cargo_cicd_dispatch_proof.rs.tmpl each carry a real `sparql:` frontmatter (`rows`/`error_variants` queries identical to the ones cargo_cicd_catalog.rs.tmpl/cargo_cicd_dispatch.rs.tmpl themselves use, rendered via a from-scratch Tera for-loop, not hand-transcribed literals), so this pack's proof suite was already ontology-derived before this pass -- the hand-transcription gap praxis-core-pack's/star-toml-pack's cap05 closures fixed does not apply here, and no second SPARQL-derived proof template was needed. Closed alongside cap02; do NOT re-investigate cap05 for this pack in a future pass. 5 capabilities remain open: cap06, cap08, cap09, cap10, cap11 -- none audited this pass. Also surfaced, disclosed not fixed: this pack's freeze_policy:checksum artifacts do not silently auto-regenerate on ontology drift when the on-disk file is otherwise untouched -- ggen-engine's write.rs decision order means an unedited-but-stale file hits [FM-WRITE-005] rather than being rewritten, requiring `rm ggen.lock` plus deleting the stale target file(s) before the next sync picks up a real change (documented in pack.toml and .specify/maturity.ttl's cg_010 mat:d6). |
| lsp-max-pack | Complete generation surface, Generated negative witnesses, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | cap05 closed 2026-07-21 (see currentCeiling). 6 capabilities remain open: cap02, cap06, cap08, cap09, cap10, cap11. Target-API-fidelity is UNVERIFIED (not re-audited) -- re-verify against the real lsp-max RulePackServer before any further promotion claim. |
| mcpp-pack | Complete generation surface, Generated negative witnesses, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | Has cap06 groundwork already (real SHACL sabotage gate proven live). Target-API-fidelity is blocked on an external, pre-existing wasm4pm-compat build break outside this pack's control -- name as a real, disclosed external dependency, not a pack defect. |
| mfact-pack | Authoritative semantic source, Complete generation surface, Generated verification, Generated negative witnesses, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | README-derived fidelity is the named weak point -- source-audit against the real ~/mfact repo before any generation-depth promotion, same lesson cargo-cicd-pack's own round-2/3 correction already taught this pack family. |
| mfw-pack | Authoritative semantic source, Complete generation surface, Generated verification, Generated negative witnesses, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | Same fidelity-sourcing weakness as mfact-pack (AGENTS.md, not source-audited). Has real transition-dispatch logic already (per its own pack.toml) -- closest of the 3 mfact/mfw/wasm4pm-pack trio to a genuine Handler-gap win once fidelity is re-sourced. |
| osx-clnr-pack | Complete generation surface, Generated negative witnesses, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | Furthest-along wave-5 pack on Test-generation. Real named gap, narrowed not closed (round 2, 2026-07-20, source-verified against the real ~/osx-clnr on this host): an exhaustive grep of all 10 real ~/osx-clnr/tests/*.rs files found exactly one more test function (test_size_string_roundtrip_convention_resolved) portable without new domain-module generation; it is now ported. The other ~8 files' tests all depend on domain modules this pack does not yet model (receipt, ocel, tool_roots beyond human_bytes, github, tmutil, scan_cache, nouns::privacy, MCP/subprocess execution) -- closing further requires generation-depth work per module, not another copy step. The 2 hand-transcribed functions (identify_thinned_snapshots, SnapshotThinReceipt::new) were re-examined, not newly re-derived: both still have no free ontology parameter a SPARQL query could supply, so that half of the original gap statement remains a documented judgment call, not a tractable close. |
| praxis-core-pack | Complete generation surface, Generated negative witnesses, Generated provenance, Generated release surface, Generated evolution path | cap05 and cap09 closed 2026-07-21 (see currentCeiling). 5 capabilities remain open: cap02, cap06, cap08, cap10, cap11. Cell-b's investigation this pass (not yet closing any of these, but narrowing next steps): cap02 -- examples/receiptctl's src/lib.rs still needs one hand-written `include!("ggen_pack_mods.rs")` line plus hand-written `pub mod verbs;`/`run()`; pack_clap_noun_verb's own missingCap still lists cap02 too -- no accepted precedent anywhere in this repo for a single include! line closing cap02. cap06 -- one real but thin existing check (`generated_table_rejects_unknown_scenario_name`, closer to a sanity check than a systematic sabotage suite); wasm4pm-compat-pack's cap06 closure this same wave (see l5p:pack_wasm4pm_compat) is the pattern to reapply here. cap08 -- the sync receipt's closure map hashes every ontology/template file touched globally per sync run, not a per-artifact trace keyed to each generated file -- needs a provenance manifest or per-file header. cap10 -- no packaging/CI templates exist for this pack; would need new template categories (cf. tcps-release-pack's CI-YAML generation). cap11 -- no diffing tool exists; only demonstrated that a live ontology edit propagates through re-sync, not that it surfaces as a semantic diff with a no-manual-repair guarantee. This pack remains the reference implementation for wave2's reusable SPARQL-derived-proof pattern (see l5p:wave2 l5p:waveDesc). |
| rmcp-pack | Authoritative semantic source, Complete generation surface, Generated provenance, Generated release surface, Generated evolution path, Consumer replacement | 6 capabilities remain open, none closed in this census pass (adjudication/reconciliation only). cap01 (authoritative semantic source) -- real, disclosed gap: rmcp_stub_handler.rs.tmpl's `echo` tool's name/description/return string are template-authored facts with no backing ontology individual (the ontology models rmcp's real API surface, not this pack's own demo tool's business logic) -- same shape as affidavit-pack's own disclosed cap01 gap, smaller in surface area. cap02 (complete generation surface) -- the consumer still needs one hand-written `include!("rmcp_lib_wiring.rs");` line in src/lib.rs -- the same universal gap every domain pack in this census carries (pack_clap_noun_verb, pack_praxis_core, pack_star_toml, pack_lsp_max all list it too); no accepted repo precedent closes it yet. cap08 (generated provenance) -- .ggen-v2/receipt.json's closure map hashes every ontology/template file touched globally per sync run, not a per-generated-artifact trace (confirmed by reading the live receipt) -- matches the universal census gap named in pack_praxis_core's own nextPromotionObligation. cap10 (generated release surface) -- no packaging/CI templates exist for rmcp-pack itself; would need a new template category (cf. tcps-release-pack's CI-YAML generation, the only pack in this census that has one). cap11 (generated evolution path) -- no automated diffing tool surfaces upstream rmcp API changes as a semantic diff; this pass only demonstrated (via the cap05/cap06 drift-sensitivity edits) that an ontology-level change propagates through resync with refusal-then-remediation, not an unattended semantic-diff loop. cap12 (consumer replacement) -- blocked on cap02: the cap04 erase-and-rebuild evidence is necessary but not sufficient for cap12's stronger 'from ggen.toml + the pack alone, zero hand-authored file' bar while the include! line remains required. |
| star-toml-pack | Complete generation surface, Generated negative witnesses, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | cap05 closed 2026-07-21 (see currentCeiling) via a second, SPARQL-derived proof alongside the existing hand-transcribed one, reapplying praxis-core-pack's wave2 reference pattern to star-toml-pack's own stp:ConfigSection/stp:ConfigField ontology shape -- the hand-transcribed proof's anti-tautology rationale still holds and it was not removed or replaced. CI-wiring closed the same day (see currentCeiling) -- examples/star-toml-verify is now a guard-pack-proofs.sh consumer, not itself a numbered capability but formerly a disclosed gap. 6 capabilities remain open: cap02, cap06, cap08, cap09, cap10, cap11 -- none audited in this pass, no priority order established yet. |
| wasm4pm-pack | Authoritative semantic source, Complete generation surface, Generated verification, Generated negative witnesses, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | Crate-map catalog only (L1->L2 across most dims, Cargo.toml-derived Target-API-fidelity) -- one of the least-advanced packs; needs real generation-depth work, not just CI wiring. |
| dogfood-lifecycle-pack | Complete generation surface, Generated verification, Generated negative witnesses, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | Least-advanced hook pack; effect-receipting is UNVERIFIED (not just incomplete) -- re-verify current real state before promotion design. |
| ma-case-study-pack | Complete generation surface, Generated verification, Generated negative witnesses, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | Per-CQ test coverage is UNVERIFIED; adversarial fixtures exist but UNVERIFIED this session; planning integration UNVERIFIED. Re-run and re-verify all three before any promotion design -- this pack's doc entry is the most stale of the 27 scored packs. |
| self-monitoring-pack | Complete generation surface, Generated negative witnesses, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | UPDATED (wiring pass, 2026-07-21): the capture script IS now wired into this pack's real admission path -- scripts/capture_and_validate.sh chains transcript_to_turtle.py (capture) -> scripts/run_gates.py (admission, new: runs the real gates/*.rq files -- shapes.ttl was deleted repo-wide by commit ad9106702's SHACL-to-SPARQL-gates migration, which had silently broken this script's old --shapes call) -> scripts/actuate_escalation.py (hook actuation + receipt on the newly admitted facts, --emit-receipt-graph --verify-round-trip). Verified end to end against a real, previously-unused Claude Code session transcript (real capture: 206 turns/1239 triples; real admission: 4/4 gates pass; real actuation: all 6 kh:Hook actions evaluated, receipted, round-trip-verified) -- see packs/self-monitoring-pack/README.md's 'L5-push, round 4' section for the full transcript. Remaining honest gap, unchanged from before this pass: still one operator/CI-triggered invocation per transcript, not a continuous capture daemon (L4) or pack-owned retention policy (L5); Actuation-closure remains L1-L2 (queryable + receipted, not unattended). |
| claude-code-pack |  | First-ever maturity adjudication still needed: score against all 7 codegen dimensions once a real consumer exists. Immediate blocker before scoring: add a committed examples/claude-code-verify/ (same pattern as examples/cargo-cicd-verify) so Consumer-effort/Test-generation evidence is CI-checkable, not TempDir-ephemeral. |
| ggen-constitution-pack | Authoritative semantic source, Complete generation surface, Generated verification, Generated negative witnesses, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | Two independent next steps, neither started: (a) correct `ccn:LawVII`'s stale sync.rs:1857/'hardcoded Green' citation (a real, self-contradicted fact this pass found and independently reconfirmed, not fixed here since this pass is reconciliation-only) -- correcting it likely closes a genuine cap01 defect; (b) wire `packs/ggen-constitution-pack/gates/{010_every_law_has_statement,020_mechanized_laws_have_test}.rq` into an actual `[validation].gates` array somewhere (root ggen.toml's own list omits both; independently reconfirmed zero references anywhere in the repo this pass) and add a sabotage test proving they refuse a malformed `ccn:Law` -- currently real, well-formed SPARQL but entirely inert, the clearest concrete cap06 gap. cap04 (fixed-point/erase-and-rebuild) stays genuinely UNVERIFIED this pass, not audited either way -- not claimed closed by omission. |
| ggen-release-pack | Authoritative semantic source, Complete generation surface, Generated verification, Generated negative witnesses, Generated provenance, Generated receipts, Generated release surface, Generated evolution path | Two concrete, disjoint next steps: (a) ggen_release_pack_workflow_e2e.rs is confirmed GREEN, not red (see currentCeiling correction) -- the real remaining gap is coverage breadth, not a failing test: extend real, dedicated proof coverage to the 4 outputs (publish-candidate.yml, RELEASE_STANDING.json, RELEASE_STANDING.md, docs/PUBLICATION_JUDGMENT.md) that currently have zero dedicated test coverage anywhere in the repo; (b) correct the 'v26.7.20' staleness in RELEASE_STANDING.json/.md/docs/PUBLICATION_JUDGMENT.md's literal ontology bodies to match the real project version (26.7.26, independently reconfirmed this pass), and either extend scripts/ci/guard-version-consistency.sh to check this pack's own artifacts or add a dedicated freshness gate -- currently nothing in the repo catches this drift. cap04 (fixed-point/erase-and-rebuild) stays genuinely UNVERIFIED this pass, not audited either way -- not claimed closed by omission. |
| gh-terraform-pack |  | First-ever maturity adjudication still needed. Same immediate blocker as claude-code-pack: no committed examples/ consumer exists to make Test-generation/Consumer-effort evidence CI-checkable rather than TempDir-ephemeral -- a real gap, disclosed by the wave7 census, not glossed over. |
| github-actions-pack |  | Two separate obligations, in order: (1) fix this pack's own ontology.ttl gha:L5C03/L5C06/L5C07 self-description before anything else -- a pack whose own semantic self-model contradicts its filesystem state cannot honestly claim cap01 (Authoritative semantic source) regardless of what else it has; (2) once corrected, run the first-ever full 12-capability adjudication. |
| level-five-book-pack |  | First-ever maturity adjudication still needed -- likely against a 4th maturity SHAPE (this pack transcribes book content, book:Chapter/book:Listing individuals, not a CLI/dispatch surface like the other wave7 packs; adjudicate type before scoring against the existing 3 matrices). cap08 (Generated provenance)/cap09 (Generated receipts) specifically are closer to closeable here than anywhere else in this file given the real receipt-log.jsonl/MANIFEST.sha256 evidence above -- prioritize this pack's cap08/cap09 audit over the other wave7 packs' first-ever scoring. |


## 7. Consumer-replacement ledger

Capability 12 ("Consumer replacement") status per pack is tracked via the missing-capabilities
column above (§6) — a pack with cap12 absent from its missing list has a proven
delete-and-regenerate consumer path; see `.specify/pack-l5-promotion.ttl`'s per-pack
`l5p:missingCap` triples for the authoritative source.

## 8. Level 5 standing ledger

| Standing | Pack count | Packs |
|---|---|---|
| furthest-advanced pack(s); source of reusable promotion machinery, itself not yet full Level5 | 2 | ggen-verify-pack, tcps-core-pack |
| promotion actively underway; ≥1 capability closed, ≥1 open | 26 | tcps-cli-pack, tcps-ffi-pack, tcps-release-pack, tcps-std-pack, tcps-wasm-pack, clap-noun-verb-pack, wasm4pm-algorithms-pack, wasm4pm-facts-pack, chicago-tdd-tools-pack, wasm4pm-cognition-pack, wasm4pm-compat-pack, affidavit-pack, anti-llm-cheat-lsp-pack, cargo-cicd-pack, lsp-max-pack, mcpp-pack, mfact-pack, mfw-pack, osx-clnr-pack, praxis-core-pack, rmcp-pack, star-toml-pack, wasm4pm-pack, self-monitoring-pack, ggen-constitution-pack, ggen-release-pack |
| counted, not yet adjudicated against the 12-capability bar | 6 | dogfood-lifecycle-pack, ma-case-study-pack, claude-code-pack, gh-terraform-pack, github-actions-pack, level-five-book-pack |


## 9. Incompatibility and supersession ledger

No pack has yet reached `Incompatible` or `Superseded` terminal standing with a mechanically
justified proof. This section populates as promotion work produces one.

## 10. Maximal-closure manifest / autosel admission manifest

## Completion condition

This program is complete only when every pack below has reached `Level5`, `Incompatible`, `Superseded`, or `RejectedWithProof` -- AND `autosel` is generated from the maximal lawful closure of the resulting Level5 population -- AND `autosel` can be completely deleted and regenerated without hand-authored product implementation. It is NOT complete when `autosel` compiles, when the 6 TCPS packs compose, or when this workflow merely exists.

**Current admitted population: 0 packs.** No pack has reached full `Level5` standing yet
(see §8 — the `Level5` row is currently empty). `autosel` may not be manufactured until this
section lists a non-empty, mechanically-verified `Level5` population.

## See Also

- `docs/packs/PACK_MATURITY_MODEL.md` — the per-dimension maturity scoring this program's
  census cross-references
- `.specify/pack-l5-promotion.ttl` — edit here, not this file
- `examples/tcps-generated/` — the wave-1 reference implementation this program's promotion
  machinery generalizes from
