# ggen APS claims ledger — machine-readable standing for release-relevant claims.
#
# Modeled on the Agile Protocol Specification 2030 claim/falsifier/standing shape
# (see docs/aps/README.md for provenance), using THIS repo's existing
# no-overclaiming vocabulary for `standing`:
#   ALIVE      = falsifier ran and passed at the recorded evidence coordinate
#   PARTIAL    = some named paths proven, others not (see notes)
#   BLOCKED    = proven broken, or fix blocked upstream (cite file/bug)
#   UNVERIFIED = no real evidence either way; never round up
#
# Verifier of record: GitHub Actions CI (required gates: Build, Check, Doctest,
# Phase 2, Test, cargo-cicd workspace health) at the pinned commit of each PR
# merge, plus the in-repo independent observers (ggen-cheat-scanner,
# chicago-tdd-tools CliHarness real-subprocess tests).
#
# Declared nonclaim (APS "no self-certification" residual): this is a
# single-maintainer repository. The same human authors the code, the tests, and
# this ledger. CI provides an independent EXECUTION environment, not an
# independent human verifier. This ledger therefore proves "the declared
# falsifiers ran and produced the recorded outcomes at the recorded
# coordinates" — not third-party certification.
#
# Evidence-coordinate honesty note: entries dated 2026-07-17 with method
# "jtbd-workflow" come from a combinatorial real-execution verification pass
# (13 agents, one per CLI noun; real subprocess invocations of a binary built
# from the working tree between commits f39b7972c and a1ecb070d; real exit
# codes, stdout, and filesystem checks — no mocks). Falsifiers are re-runnable;
# staleness of a coordinate is a warning, not a refutation ("a failed tool is
# not a failed claim", and equally, old evidence is not fresh evidence).

schema = "ggen-aps-claims.v1"
toolchain = "nightly-2026-06-22"

# ── CLI noun JTBD claims ─────────────────────────────────────────────────────

[[claims]]
id = "cli.sync.run"
standing = "ALIVE"
gates = []
falsifier = "cargo test -p ggen-engine --test cli_boundary"
evidence = { commit = "f39b7972c", date = "2026-07-17", method = "jtbd-workflow + cli_boundary 28/28" }
notes = "Dry-run writes nothing; real run generates correct file content; idempotent on re-run; corrupt ggen.toml fails with a specific error."

[[claims]]
id = "cli.graph.validate"
standing = "ALIVE"
gates = []
falsifier = "cargo test -p ggen-engine --test lint_validate_e2e"
evidence = { commit = "f39b7972c", date = "2026-07-17", method = "jtbd-workflow, 11/11 cases" }
notes = "Valid Turtle passes; invalid Turtle and SHACL violations fail with real diagnostics; missing file fails cleanly."

[[claims]]
id = "cli.receipt.verify"
standing = "ALIVE"
gates = []
falsifier = "cargo test -p ggen-engine --test receipt_chain_e2e"
evidence = { commit = "1b32d7b06", date = "2026-07-17", method = "receipt_chain_e2e 16/16 incl. tamper/fail-closed cases" }
notes = "Verifies after real sync; fails closed on bit-flip/truncation tamper; distinguishes chain failure from signature failure."

[[claims]]
id = "cli.init"
standing = "ALIVE"
gates = []
falsifier = "cargo test -p ggen-cli-lib --test proof_init_test"
evidence = { commit = "627f84c5", date = "2026-07-17", method = "jtbd-workflow, 9 cases, real filesystem checks" }
notes = "Both previously-tracked FOOTGUN bugs are fixed (627f84c5, #265): malformed --force bool now rejected via strict parse_bool_flag instead of silently coercing to false (BUG-002, init.rs:466,523-532); InitOutput{status:\"error\"} now promotes to a real Err instead of exiting 0 (BUG-003, init.rs:497-506). See docs/jira/2026-07-17-JTBD-VERIFICATION-DISCOVERED-BUGS.md."

[[claims]]
id = "cli.law"
standing = "ALIVE"
gates = []
falsifier = "target/debug/ggen law --help && cargo test -p ggen-engine --test graphlaw_e2e"
evidence = { commit = "f39b7972c", date = "2026-07-17", method = "jtbd-workflow, all 5 verbs (load/derive/explain/validate/export) incl. a designed-to-fail gate case" }

[[claims]]
id = "cli.pack"
standing = "ALIVE"
gates = []
falsifier = "cargo test -p ggen-cli-lib --test proof_pack_test"
evidence = { commit = "f39b7972c", date = "2026-07-17", method = "jtbd-workflow, add->list/show->remove->doctor straight-line path with real lockfile+receipt checks" }

[[claims]]
id = "cli.packs"
standing = "PARTIAL"
gates = []
falsifier = "cargo test -p ggen-cli-lib --test proof_packs_test"
evidence = { commit = "f39b7972c", date = "2026-07-17", method = "jtbd-workflow" }
notes = "3 of 4 verbs proven (install/validate/list) with real lockfile digest + receipt assertions; 4th verb not fully exercised."

[[claims]]
id = "cli.agent"
standing = "PARTIAL"
gates = []
falsifier = "cargo test -p ggen-cli-lib --test agent_lifecycle_test"
evidence = { commit = "627f84c5", date = "2026-07-17", method = "jtbd-workflow, full lifecycle re-run fresh with on-disk state checks" }
notes = "`ggen agent install` is disabled (627f84c5, #265): it now returns a typed execution_error citing BUG-004 ('already installed' check reads a fixed global path instead of the project-local lockfile) instead of running with the bug live. Real logic preserved as #[allow(dead_code)] install_impl pending a fix (crates/ggen-cli/src/cmds/agent.rs:144-155). Standing downgraded from ALIVE to PARTIAL to reflect that the verb is currently a refusal, not a working path."

[[claims]]
id = "cli.doctor"
standing = "ALIVE"
gates = []
falsifier = "cargo test -p ggen-engine --test doctor_e2e"
evidence = { commit = "95aace906", date = "2026-08-03", method = "real sabotage fixtures against the current `doctor run` command (lockfile/pack drift, orphaned artifacts, receipt-vs-disk staleness)" }
notes = "UPDATED 2026-08-03: repointed from crates/ggen-cli/tests/doctor_adversarial_tests.rs, which is now archived. That file tested `doctor config`/`doctor ontology`/`doctor security` -- three subcommands confirmed live to no longer exist (the current `doctor` noun is `inspect`/`run`/`domain`; this is a real removal, not a rename, so there was no current command to repoint those 3 tests at). doctor_e2e.rs already provides real, currently-passing coverage of the doctor noun's real current behavior (5/5: never_synced_project_is_healthy_by_definition, clean_synced_project_is_healthy, orphaned_artifact_after_template_deletion_fails_independently, deleted_output_fails_receipt_staleness_independently_of_other_checks, corrupted_pack_content_fails_lockfile_drift). Prior BUG-005 fix note (627f84c5, #265, handle_doctor dispatching through classify_ggen_toml/schema_dispatch.rs instead of unconditional GgenConfig::load) is historical context, not re-verified by this update -- it concerns dispatch-routing correctness, which doctor_e2e.rs's fixtures exercise incidentally (they run against real declarative/frontmatter projects) but do not target directly."

[[claims]]
id = "cli.capability"
standing = "PARTIAL"
gates = []
falsifier = "target/debug/ggen capability list && target/debug/ggen capability inspect mcp"
evidence = { commit = "f39b7972c", date = "2026-07-17", method = "jtbd-workflow, real lockfile side-effect verification" }
notes = "GAP-001 fixed (2026-07-18, v26.7.18 release hardening pass, crates/ggen-cli/src/cmds/capability.rs): atomic_packs_for now propagates resolve_capability_to_packs's 'unknown surface' error instead of unwrap_or_default()-discarding it, so `enable`/`inspect` return a typed error for an unknown surface instead of silently returning atomic_packs: []; project_root() now checks ggen.toml exists before `enable` touches the lockfile. `inspect` re-enabled (was disabled 2026-07-17 pending exactly this fix). Remaining accepted gap: --projection is format-checked (must be a valid PackageId fragment) but not validated against a real projection registry — no such registry exists yet. Standing stays PARTIAL for that reason, not for the fixed issues above. Evidence coordinate will refresh to this pass's commit on next falsifier re-run."

[[claims]]
id = "cli.utils"
standing = "PARTIAL"
gates = []
falsifier = "target/debug/ggen utils env --list"
evidence = { commit = "f39b7972c", date = "2026-07-17", method = "jtbd-workflow" }
notes = "Only one real subcommand exists (env, with --list/--get/--set flags); proven against real environment variables. No other verbs exist to verify."

[[claims]]
id = "cli.ontology"
standing = "UNVERIFIED"
gates = []
falsifier = "target/debug/ggen ontology list --embedded"
evidence = { commit = "f39b7972c", date = "2026-07-17", method = "jtbd-workflow enumerated 7 real verbs; several are network/registry-dependent and could not be exercised end-to-end offline" }
notes = "Zero pre-existing real test coverage (all prior tests targeted verbs that don't exist or were feature-archived). Verbs exist and parse; JTBD outcomes not proven."

[[claims]]
id = "cli.policy"
standing = "ALIVE"
gates = []
falsifier = "target/debug/ggen policy validate --profile enterprise-strict"
evidence = { commit = "627f84c5", date = "2026-07-17", method = "jtbd-workflow, deterministic reproduction across isolated HOME environments" }
notes = "BUG-001 fixed (627f84c5, #265): load_pack_contexts_from_project (crates/ggen-cli/src/cmds/policy.rs:112-175) now skips a malformed PackageId entry and reports it via LoadedPackContexts::malformed instead of aborting the whole command via `?` before Profile::enforce ever ran for any other pack. check/validate no longer fail 100% of invocations on one bad lockfile entry. See docs/jira/2026-07-17-JTBD-VERIFICATION-DISCOVERED-BUGS.md. Falsifier note (2026-07-18, still accurate): `ggen policy check` has no --profile flag (confirmed via --help); `policy validate` does take --profile."

# ── Release-level claims ─────────────────────────────────────────────────────

[[claims]]
id = "release.receipt-chain-verifies"
standing = "ALIVE"
gates = ["publish"]
falsifier = "cargo test -p ggen-engine --test receipt_chain_e2e"
evidence = { commit = "1b32d7b06", date = "2026-07-17", method = "16/16 incl. legacy-unsigned, env-var key precedence, and history tamper cases" }

[[claims]]
id = "release.package-contents-clean"
standing = "ALIVE"
gates = ["publish"]
falsifier = "cargo package --list -p ggen --allow-dirty && cargo package --list -p ggen-graph --allow-dirty"
evidence = { commit = "1b32d7b06", date = "2026-07-17", method = "PR #260: root include-pattern leak fixed (855->503 files); ggen-graph audit/ excluded (39.8MB->583KiB); all 7 targets re-verified via cargo publish --dry-run in topological order" }
notes = "This was a live instance of APS case study 4 (PACKAGE_MISMATCH): package contents diverged from repository reality until the include/exclude fixes landed. Publish-target set narrowed 2026-07-18: cpmp, genesis-types-v2, and genesis-core-v2 (which depends on genesis-types) were experimental crates that had no business being publish-eligible -- all three now carry publish = false. The real target count is 4 (ggen, ggen-config, ggen-graph, ggen-marketplace), not the 7 this evidence entry's method field describes as of PR #260 -- that field is left as an accurate historical record, not corrected in place."

[[claims]]
id = "release.rustsec-quick-xml"
standing = "BLOCKED"
gates = ["publish"]
falsifier = "cargo audit"
evidence = { commit = "1b32d7b06", date = "2026-07-17", method = "cargo audit + cargo tree -i per version + two real cargo update --precise 0.41.0 attempts, both refused by upstream version reqs" }
notes = "RUSTSEC-2026-0194/0195 (quick-xml, severity 7.5 high, DoS-class): fix requires quick-xml >=0.41.0, but all three vulnerable copies (0.31.0, 0.36.2, 0.37.5) are pinned by third parties — oxigraph 0.5.9's oxrdfxml/sparesults (sparesults pinned =0.3.3 by praxis-graphlaw), wasm4pm-compat 26.6.29 via star-toml, and dev-only process_mining. Not fixable in this repo without upstream releases. Exception requires explicit maintainer admission — set exception_admitted_by to record it. RISK ACCEPTED 2026-07-20 by the repo owner (Sean Chatman): both advisories are DoS-class (unbounded allocation / quadratic runtime on adversarial XML input), not RCE or memory-corruption; ggen's own runtime does not parse untrusted XML from an external attacker over a network boundary (oxrdfxml/sparesults consume locally-authored or CI-controlled RDF/XML, quick-xml is dev-only via process_mining). Standing stays BLOCKED — this is an admitted exception, not a fix; re-evaluate once sparesults/oxigraph or the praxis-graphlaw pin ships quick-xml >=0.41.0."
exception_admitted_by = "sean-chatman-2026-07-20"

# ── Dev-gate (pre-commit) claims ─────────────────────────────────────────────
# L5 condition 10 (docs/PUBLICATION_JUDGMENT.md) asks whether required evidence
# enters ONLY via this ledger. Investigation 2026-07-20 found `just pre-commit`'s
# own 9-gate outcome had zero ledger presence — the gates run and pass/fail, but
# nothing routes that result here. This entry closes that gap for exactly one of
# the 9 gates (the one currently red); the other 8 remain untracked as claims —
# see that condition's Basis note for the itemized scope of what does and does
# not route through this ledger.

[[claims]]
id = "dev.guard-cheat-scan"
standing = "BLOCKED"
gates = ["pre-commit"]
falsifier = "cargo run --quiet -p ggen-cheat-scanner --bin ggen-cheat-scanner"
evidence = { commit = "5630f4fac", date = "2026-07-20", method = "real run via `just guard-cheat-scan`, exit code 1" }
notes = "464 findings (7 CHEAT-T01 vacuous-assert, 456 CHEAT-T03 no-assertion-test, 1 CHEAT-T04 mock-import) across 1151 scanned files — reconfirmed live at this coordinate, matching the count justfile's own guard-cheat-scan comment and docs/jira/2026-07-17-JTBD-VERIFICATION-DISCOVERED-BUGS.md's TECH-DEBT-001 already documented in prose. This is real, tracked, pre-existing debt, not a regression: standing is BLOCKED (proven failing), not silenced or downgraded to a warning. `just pre-commit` as a whole is red until this is triaged. Prior to this entry, this gate's outcome existed only as prose (the justfile comment + the jira doc); it had no ledger entry, no falsifier command recorded as re-runnable evidence, and guard-claims-schema (which validates THIS ledger's own structure) had no way to notice its absence. This entry is the mechanism-level fix for that one gap — it does not imply the other 8 pre-commit gates (fmt-check, check, lint, test-lib, coherence-check, guard-process-intelligence-boundary, guard-claims-schema, guard-pack-proofs) now have ledger entries; they still do not."

[[claims]]
id = "dev.lint-workspace"
standing = "ALIVE"
gates = ["pre-commit"]
falsifier = "cargo clippy --workspace --all-targets --keep-going -- -D warnings -A unexpected_cfgs"
evidence = { commit = "95aace906", date = "2026-08-03", method = "real run, default CARGO_TARGET_DIR, exit code 0, grep-confirmed 0 error/clippy:: lines across the full log (not just tail output); cargo test --workspace --lib 2256 passed/0 failed as a no-regression check" }
notes = "UPDATED 2026-08-03 (second update, same day): real count is now 0, down from 649 (2026-08-02) then 4 (earlier today). The residual 4 -- chicago-tdd-tools' clippy::redundant_closure (receipt.rs:130, .any(|s| predicate(s)) -> .any(predicate), verified predicate: impl Fn(&str) -> bool matches Iterator::any directly) and ggen-cli-lib's clippy::too_long_first_doc_paragraph + clippy::single_element_loop in the GENERATED crates/ggen-cli/src/generated_commands.rs -- were fixed at their real source. The generated file was not hand-edited: the fix went into .specify/templates/cli/commands-reference.rs.tera (a blank //! line splitting the over-long first doc paragraph; the single-element for-loop replaced with a direct binding), then proved via a real `ggen sync run` regeneration (git diff --stat shows exactly the 2 targeted lines changed, nothing else). just lint now exits 0. Standing promoted to ALIVE. Full evidence: docs/jira/2026-07-17-JTBD-VERIFICATION-DISCOVERED-BUGS.md's TECH-DEBT-002 second 2026-08-03 update."

[[claims]]
id = "dev.test-integration"
standing = "BLOCKED"
gates = []
falsifier = "just test-integration"
evidence = { commit = "95aace906", date = "2026-08-03", method = "real full completed run, cargo test --workspace --no-fail-fast --features integration,a2a,mcp, 372 real targets (test_telco_routing excluded by design); 13 of the 372 targets independently re-run fresh this session as a spot-check" }
notes = "UPDATED 2026-08-03. Two separate fixes landed since the 2026-08-02 partial/killed-by-hang evidence: (1) the originally-named 7 failures + the economics_measured_evidence_test.rs hang are fixed and reconfirmed passing (independently re-run fresh: ci_g0_inventory_e2e, cli_boundary, config_schema_dispatch_e2e, cross_pack_matrix, custom_behavior_e2e, performance, system_crate_map_parity_test all green; hang did not reproduce across multiple full runs). (2) A separate, previously-undiscovered bug in this recipe itself: `just test-integration` hard-errored at cargo's target-selection step (before compiling anything) because required-features targets need --features integration,a2a,mcp, which the committed recipe never actually passed -- fixed in justfile 2026-08-03 (also excludes test_telco_routing, permanently unrunnable by design, from the enumerated targets: 373 -> 372). With both fixes, a genuine full run completed for the first time: 2070 passed, 90 failed, 256 ignored, 15/372 targets with at least one failure -- a real, larger, previously-never-exercised residual, not a regression (independently re-spot-checked in this pass: 11 legacy dead-CLI-surface targets referencing subcommands removed by the v26.7.16 CLI routing flip, e.g. `unrecognized subcommand` on cli_command_tests/doctor_adversarial_tests, confirmed live against the current `ggen`/`ggen doctor` --help noun surface; 2 more strict_mode ORDER BY fixtures, generation_rules_e2e 14 fns + generation_rules_typed_causes_e2e 1 fn, exact counts reproduced; product_mirror_conformance's diagnostic-code-catalog drift, 1 fn; manifest_contract_test's plugin-version drift, .claude-plugin/marketplace.json still says 26.7.4 vs the crate's real 26.8.4, reproduced exactly). gates=[] stays deliberate (not pre-commit): the promotion condition (full suite green) is not met -- 15/372 targets are real and reproducible red, tracked as task #33 in this session's tracker, not this ledger's job to invent a task ID for. Full breakdown: docs/jira/2026-07-17-JTBD-VERIFICATION-DISCOVERED-BUGS.md's TECH-DEBT-003 2026-08-03 update."
