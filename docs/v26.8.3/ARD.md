# Architecture Requirements Document — ggen v26.8.3

## Authority

This ARD operationalizes the v26.8.3 PRD without widening it. The machine-readable release authority and cross-repository verifier are normative for this bounded documentation rail.

## Architecture Objective

The target is a projectional, policy-governed repository manufacturing kernel: observation → admission → candidate construction → planning → manufacture → brokered execution → evidence → independent standing.

## Repository Topology

ggen is the generalized kernel. ggen-legacy is the executable corpus and independent receiver. Product repositories remain independent authority domains.

## Components

### GGEN-C-001 — Constitution

Chatman Equation, authority precedence, claim ceilings and process boundaries. Actuation: **no**.

### GGEN-C-002 — Observation Plane

Exact Git, filesystem, dependency and runtime observation. Actuation: **no**.

### GGEN-C-003 — Admission Plane

Schema, provenance, policy, contradiction and semantic admission. Actuation: **no**.

### GGEN-C-004 — Candidate Plane

CMD reversible candidate construction. Actuation: **no**.

### GGEN-C-005 — Planning Plane

Plan and policy adapters preserving semantic distinctions. Actuation: **no**.

### GGEN-C-006 — Manufacturing Plane

Resolve, Enrich, Extract, Render, Write and Receipt. Actuation: **no**.

### GGEN-C-007 — BRCE Actuation Boundary

Scoped consequential execution grants and actuators. Actuation: **yes, BRCE-scoped only**.

### GGEN-C-008 — Evidence Plane

Observed consequences, signatures, receipts, telemetry and replay. Actuation: **no**.

### GGEN-C-009 — Standing Plane

Independent Truthforge evaluation and Gall standing. Actuation: **no**.

### GGEN-C-010 — Interface Plane

CLI, MCP, A2A, LSP, Doctor, Wizard and Telco projections. Actuation: **no**.

### GGEN-C-011 — Kernel Corpus Adapter

Exact contract with ggen-legacy corpus and independent receiver. Actuation: **no**.

## Interfaces

The kernel/corpus protocol binds both exact bases, authority digest, corpus digest, pack identity, toolchain digest, receipt schema, compatibility profile and standing ceiling. CLI, MCP, A2A and LSP are projections; they do not become independent semantic authorities.

## Trust Boundaries

Untrusted source, observation sandbox, semantic admission, candidate/planning, rendering, BRCE grant, actuator, immutable evidence, independent verifier and external production/device are distinct boundaries.

## Verification Architecture

The ladder is schema/protocol → unit/property → integration → black-box E2E → security/chaos/stress/benchmark → receipt sabotage → clean-room replay → independent crown. This PRD/ARD bundle declares only structural, semantic, mutation and replay verification.

## Acceptance Architecture

Every PRD requirement must map to named components, owner, verifier and acceptance; every generated document digest must match; only `GGEN-C-007` may actuate; process analysis remains outside ggen; and ggen-legacy must independently return `ALIVE` for this authority bundle.
