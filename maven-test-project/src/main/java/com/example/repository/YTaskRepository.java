package com.example.repository;

import com.example.entity.YTask;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;

import java.util.List;
import java.util.Optional;

/**
 * Generated Spring Data Repository: YTaskRepository
 * Provides CRUD operations and custom queries for YTask entities.
 *
 * Generated by ggen Rule 4: Repositories
 * Source: YAWL ontology
 */
@Repository
public interface YTaskRepository extends JpaRepository<YTask, String> {

    /**
     * Find all tasks in a given net.
     */
    List<YTask> findByNetId(String netId);

    /**
     * Find all tasks of a specific type.
     */
    List<YTask> findByTaskType(String taskType);

    /**
     * Find all enabled tasks in a net.
     */
    @Query("SELECT t FROM YTask t WHERE t.netId = :netId AND t.isEnabled = true")
    List<YTask> findEnabledTasksByNetId(@Param("netId") String netId);

    /**
     * Find tasks by name pattern.
     */
    @Query("SELECT t FROM YTask t WHERE t.taskName LIKE %:taskNamePattern%")
    List<YTask> findByTaskNameContaining(@Param("taskNamePattern") String taskNamePattern);

    /**
     * Count tasks in a net.
     */
    long countByNetId(String netId);

    /**
     * Check if a task exists in a specific net.
     */
    boolean existsByTaskIdAndNetId(String taskId, String netId);
}
