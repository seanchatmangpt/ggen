package com.example.repository;

import com.example.entity.YWorkItem;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;

import java.util.List;
import java.util.Optional;

/**
 * Generated Spring Data Repository: YWorkItemRepository
 * Provides CRUD operations and custom queries for YWorkItem entities.
 *
 * Generated by ggen Rule 4: Repositories
 * Source: YAWL ontology
 */
@Repository
public interface YWorkItemRepository extends JpaRepository<YWorkItem, String> {

    /**
     * Find all work items for a given case.
     */
    List<YWorkItem> findByCaseId(String caseId);

    /**
     * Find all work items for a given task.
     */
    List<YWorkItem> findByTaskId(String taskId);

    /**
     * Find all work items with a specific status.
     */
    List<YWorkItem> findByStatus(String status);

    /**
     * Find all work items for a case with a specific status.
     */
    @Query("SELECT wi FROM YWorkItem wi WHERE wi.caseId = :caseId AND wi.status = :status")
    List<YWorkItem> findByCaseIdAndStatus(@Param("caseId") String caseId, @Param("status") String status);

    /**
     * Find all work items assigned to a resource.
     */
    List<YWorkItem> findByResourceAssigned(String resourceAssigned);

    /**
     * Count work items in a specific case.
     */
    long countByCaseId(String caseId);

    /**
     * Check if a work item exists for a given task and case.
     */
    boolean existsByTaskIdAndCaseId(String taskId, String caseId);
}
