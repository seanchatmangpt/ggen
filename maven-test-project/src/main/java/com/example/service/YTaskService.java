package com.example.service;

import com.example.entity.YTask;
import com.example.repository.YTaskRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;
import java.util.Optional;

/**
 * Generated Service: YTaskService
 * Business logic layer for YTask operations.
 *
 * Generated by ggen Rule 8: Services
 * Source: YAWL ontology
 */
@Service
@Transactional
public class YTaskService {

    @Autowired
    private YTaskRepository repository;

    /**
     * Find task by ID.
     */
    @Transactional(readOnly = true)
    public Optional<YTask> findById(String taskId) {
        return repository.findById(taskId);
    }

    /**
     * Find all tasks.
     */
    @Transactional(readOnly = true)
    public List<YTask> findAll() {
        return repository.findAll();
    }

    /**
     * Find all tasks in a net.
     */
    @Transactional(readOnly = true)
    public List<YTask> findByNetId(String netId) {
        return repository.findByNetId(netId);
    }

    /**
     * Find all tasks of a specific type.
     */
    @Transactional(readOnly = true)
    public List<YTask> findByTaskType(String taskType) {
        return repository.findByTaskType(taskType);
    }

    /**
     * Find enabled tasks in a net.
     */
    @Transactional(readOnly = true)
    public List<YTask> findEnabledTasksByNetId(String netId) {
        return repository.findEnabledTasksByNetId(netId);
    }

    /**
     * Find tasks by name pattern.
     */
    @Transactional(readOnly = true)
    public List<YTask> findByTaskNameContaining(String taskNamePattern) {
        return repository.findByTaskNameContaining(taskNamePattern);
    }

    /**
     * Save a task.
     */
    public YTask save(YTask task) {
        return repository.save(task);
    }

    /**
     * Save all tasks.
     */
    public List<YTask> saveAll(List<YTask> tasks) {
        return repository.saveAll(tasks);
    }

    /**
     * Delete a task by ID.
     */
    public void deleteById(String taskId) {
        repository.deleteById(taskId);
    }

    /**
     * Delete a task.
     */
    public void delete(YTask task) {
        repository.delete(task);
    }

    /**
     * Count tasks in a net.
     */
    @Transactional(readOnly = true)
    public long countByNetId(String netId) {
        return repository.countByNetId(netId);
    }

    /**
     * Check if task exists.
     */
    @Transactional(readOnly = true)
    public boolean existsById(String taskId) {
        return repository.existsById(taskId);
    }
}
