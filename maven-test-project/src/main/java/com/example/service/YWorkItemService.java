package com.example.service;

import com.example.entity.YWorkItem;
import com.example.repository.YWorkItemRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;
import java.util.Optional;

/**
 * Generated Service: YWorkItemService
 * Business logic layer for YWorkItem operations.
 *
 * Generated by ggen Rule 8: Services
 * Source: YAWL ontology
 */
@Service
@Transactional
public class YWorkItemService {

    @Autowired
    private YWorkItemRepository repository;

    /**
     * Find work item by ID.
     */
    @Transactional(readOnly = true)
    public Optional<YWorkItem> findById(String workItemId) {
        return repository.findById(workItemId);
    }

    /**
     * Find all work items.
     */
    @Transactional(readOnly = true)
    public List<YWorkItem> findAll() {
        return repository.findAll();
    }

    /**
     * Find all work items for a case.
     */
    @Transactional(readOnly = true)
    public List<YWorkItem> findByCaseId(String caseId) {
        return repository.findByCaseId(caseId);
    }

    /**
     * Find all work items for a task.
     */
    @Transactional(readOnly = true)
    public List<YWorkItem> findByTaskId(String taskId) {
        return repository.findByTaskId(taskId);
    }

    /**
     * Find all work items with a specific status.
     */
    @Transactional(readOnly = true)
    public List<YWorkItem> findByStatus(String status) {
        return repository.findByStatus(status);
    }

    /**
     * Find all work items for a case with a specific status.
     */
    @Transactional(readOnly = true)
    public List<YWorkItem> findByCaseIdAndStatus(String caseId, String status) {
        return repository.findByCaseIdAndStatus(caseId, status);
    }

    /**
     * Save a work item.
     */
    public YWorkItem save(YWorkItem workItem) {
        return repository.save(workItem);
    }

    /**
     * Save all work items.
     */
    public List<YWorkItem> saveAll(List<YWorkItem> workItems) {
        return repository.saveAll(workItems);
    }

    /**
     * Delete a work item by ID.
     */
    public void deleteById(String workItemId) {
        repository.deleteById(workItemId);
    }

    /**
     * Delete a work item.
     */
    public void delete(YWorkItem workItem) {
        repository.delete(workItem);
    }

    /**
     * Count work items in a case.
     */
    @Transactional(readOnly = true)
    public long countByCaseId(String caseId) {
        return repository.countByCaseId(caseId);
    }

    /**
     * Check if work item exists.
     */
    @Transactional(readOnly = true)
    public boolean existsById(String workItemId) {
        return repository.existsById(workItemId);
    }
}
