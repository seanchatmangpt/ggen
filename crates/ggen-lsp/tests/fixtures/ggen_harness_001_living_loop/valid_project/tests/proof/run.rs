// The proof file the lawful fixture's [[test]] path declaration points at.
// Its existence is what makes the valid_project fixture clean (no GGEN-HARNESS-001).
#[test]
fn placeholder_proof() {
    assert_eq!(2 + 2, 4);
}
