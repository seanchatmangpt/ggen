// Generated by ggen - DO NOT EDIT MANUALLY
// Prolog8 Types

pub use crate::ids::*;
use serde::{Serialize, Deserialize};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
pub enum ProofMode {
    Positive,
    Negative,
    Full,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
pub enum ProofKind {
    Fact,
    Rule,
    Builtin,
    Negation,
    AntiJoin,
    Aggregate,
    Foreign,
    MissingFact,
    FailedJoin,
    BlockedAlternative,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
pub enum DecisionKind {
    Allow,
    Deny,
    Escalate,
    Unknown,
    Invalid,
}

{% for entity in entities %}
{% if entity.label == "Atom8" %}
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
pub struct {{ entity.label }} {
    pub pred_id: PredicateId,
    pub arity: u8,
    pub binding_mask: u8,
    pub args: [TermId; 8],
}
{% elif entity.label == "Rule8" %}
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
pub struct {{ entity.label }} {
    pub rule_id: RuleId,
    pub head: Atom8,
    pub body: [Atom8; 8],
    pub body_len: u8,
    pub body_mask: u8,
    pub negation_mask: u8,
    pub builtin_mask: u8,
    pub var_count: u8,
    pub var_live_mask: u8,
    pub feature_mask: u8,
    pub proof_mask: u8,
    pub plan_id: u32,
}
{% elif entity.label == "QueryAtom8" %}
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
pub struct {{ entity.label }} {
    pub atom: Atom8,
    pub output_mask: u8,
    pub proof_mode: ProofMode,
    pub epoch: EpochId,
}
{% elif entity.label == "FactRow8" %}
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
pub struct {{ entity.label }} {
    pub pred_id: PredicateId,
    pub arity: u8,
    pub args: [TermId; 8],
    pub source_id: SourceId,
    pub fact_hash: Hash,
}
{% elif entity.label == "FactBlock8" %}
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct {{ entity.label }} {
    pub pred_id: PredicateId,
    pub arity: u8,
    pub arg_order: [u8; 8],
    pub row_count: u32,
    pub rows: Vec<FactRow8>,
    pub block_hash: Hash,
}
{% elif entity.label == "FactBlockMeta8" %}
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
pub struct {{ entity.label }} {
    pub pred_id: PredicateId,
    pub arity: u8,
    pub row_count: u32,
    pub block_hash: Hash,
}
{% elif entity.label == "ProofNode8" %}
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
pub struct {{ entity.label }} {
    pub node_id: ProofNodeId,
    pub kind: ProofKind,
    pub pred_id: PredicateId,
    pub rule_id: Option<RuleId>,
    pub fact_hash: Option<Hash>,
    pub children: [ProofNodeId; 8],
    pub child_count: u8,
    pub node_hash: Hash,
}
{% elif entity.label == "ProofDAG8" %}
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct {{ entity.label }} {
    pub nodes: Vec<ProofNode8>,
    pub root: ProofNodeId,
    pub dag_hash: Hash,
}
{% elif entity.label == "Receipt8" %}
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct {{ entity.label }} {
    pub engine_version: String,
    pub catalog_root: Hash,
    pub rule_root: Hash,
    pub fact_root: Hash,
    pub input_root: Hash,
    pub proof_root: Hash,
    pub output_root: Hash,
    pub decision: DecisionKind,
    pub epoch: EpochId,
    pub receipt_hash: Hash,
}
{% elif entity.label == "QueryResult8" %}
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct {{ entity.label }} {
    pub bindings: Vec<[TermId; 8]>,
    pub proof: ProofDAG8,
    pub receipt: Receipt8,
}
{% elif entity.label == "ReplayStatus8" %}
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct {{ entity.label }} {
    pub verified: bool,
    pub proof_root_matches: bool,
    pub output_root_matches: bool,
}
{% elif entity.label == "AccessPath8" %}
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
pub struct {{ entity.label }} {
    pub pred_id: PredicateId,
    pub binding_mask: u8,
    pub index_key: u8,
}
{% elif entity.label == "Plan8" %}
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct {{ entity.label }} {
    pub ops: Vec<PlanOp8>,
    pub op_count: u8,
}
{% elif entity.label == "PlanOp8" %}
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
pub struct {{ entity.label }} {
    pub op_kind: u8,
    pub pred_id: PredicateId,
    pub binding_mask: u8,
}
{% elif entity.label == "ExecutionTrace8" %}
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct {{ entity.label }} {
    pub results: Vec<[TermId; 8]>,
    pub decision: DecisionKind,
}
{% endif %}
{% endfor %}
