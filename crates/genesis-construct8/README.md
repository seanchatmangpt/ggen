# KNHK Construct8 Genesis Engine

> **KNHK decides what may exist. Construct8 Genesis makes it exist eight triples at a time. μ-forge turns it into bytes. Receipts prove it happened. Replay proves it again.**

*See the full [Genesis 2030 Working Backwards Press Release and Vision](../../GENESIS_2030_PRESS_RELEASE.md) for the complete strategic doctrine.*

`knhk-construct8` is the physical construction layer for KNHK. While KNHK’s upper layers define the governing frame (A = μ(O), determinism, typing, guard laws), this engine provides the concrete, lawful materialization of trillion-scale triple corpora through bounded 8-triple construction packets.

## Doctrine

Genesis8 is the triple Big Bang engine for KNHK. It materializes lawful triples from admitted evidence through bounded `Construct8Packet`s, then forges canonical boundary artifacts with `BLAKE3` receipts and replay verification. 

Large graphs are **not** constructed as giant in-memory objects. They are born **eight triples at a time** on the structural tape.

## The Absolute Invariants

1. **No exported bytes bypass the forge.** All canonical representations (N-Quads, Turtle, etc.) are deterministically generated by the forge.
2. **No hot-path packet contains more than eight active triples.** Controlled by `Construct8Packet`.
3. **No private predicate authority in public-vocabulary mode.** (Future M-milestone guard.)
4. **Every emitted artifact has a hash.** Backed by `blake3`.
5. **Every hash can be bound into a receipt.**
6. **Every receipt can be replayed.** And verifiable without state side-effects.
7. **Every refusal is materialized as a BLOCK artifact.**
8. **Large graphs are packet streams, not giant objects.**

## Core Type: `Construct8Packet`

The heart of the engine is a C-compatible representation with no strings or heap allocations in the hot path:

```rust
#[repr(C)]
pub struct Construct8Packet {
    pub epoch: u64,
    pub law_ref: u64,
    pub subjects: [u32; 8],
    pub predicates: [u32; 8],
    pub objects: [u32; 8],
    pub kind_mask: u8,
    pub valid_mask: u8,
    pub emit_mask: u8,
    pub block_mask: u8,
    pub order: u32,
    pub receipt_seed: [u8; 32],
}
```

## Operations CLI (`knhk8`)

The engine provides an operational surface via the `knhk8` binary:

```bash
# 1. Forge canonical bytes from candidate packets
knhk8 forge nquads evidence.c8 --out evidence.nq --receipt

# 2. Verify an artifact against its receipt
knhk8 verify evidence.nq --receipt evidence.nq.receipt.json

# 3. Simulate replay via receipts
knhk8 replay evidence.nq.receipt.json
```

## Milestones

- [x] **M1:** Construct8 packet stream → canonical N-Quads → BLAKE3 receipt → replay verification
- [ ] **M2:** Turtle output
- [ ] **M3:** OCEL v2 output
- [ ] **M4:** SHACL refusal reports
- [ ] **M5:** PROV/DCAT evidence catalogs
- [ ] **M6:** WASM ABI
- [ ] **M7:** trillion-scale shard benchmark
