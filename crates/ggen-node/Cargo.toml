[package]
name = "ggen-node"
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/seanchatmangpt/ggen"
description = "Node.js N-API bindings for ggen"

[lib]
crate-type = ["cdylib"]
name = "ggen_node"

[dependencies]
# Using napi v3 for improved async support and better Node.js integration
napi = { version = "3.4", features = ["napi6", "tokio_rt", "async"] }
napi-derive = "3.0"
tokio = { version = "1.47", features = ["rt-multi-thread"] }
ggen-cli-lib = { path = "../ggen-cli", version = "2.6.0" }
serde_json = "1.0"

[dev-dependencies]
tokio = { version = "1.47", features = ["rt-multi-thread", "macros"] }
futures = "0.3"
chicago-tdd-tools = { workspace = true }

[build-dependencies]
napi-build = "2"

[package.metadata.napi]
name = "ggen-node"

