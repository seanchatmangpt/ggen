// {{ domain_name }} domain logic
// Generated by ggen

use anyhow::Result;

pub struct {{ domain_name | pascal }} {
    // Domain state
}

impl {{ domain_name | pascal }} {
    pub fn new() -> Self {
        Self {}
    }

    pub fn process(&self) -> Result<()> {
        // Domain logic here
        Ok(())
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_{{ domain_name | snake }}_new() {
        let domain = {{ domain_name | pascal }}::new();
        assert!(domain.process().is_ok());
    }
}
