PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX yawl: <http://yawlfoundation.org/ontology#>

SELECT ?type (COUNT(?task) AS ?count)
WHERE {
  ?task a ?type .
  FILTER (?type IN (yawl:AtomicTask, yawl:CompositeTask, yawl:Task))
}
GROUP BY ?type
ORDER BY DESC(?count)
