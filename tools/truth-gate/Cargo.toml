[package]
name = "truth-gate"
version = "0.1.0"
edition = "2021"
description = "Structural enforcement gate for CodeManufactory — blocks fake evidence paths before they land"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[workspace]

