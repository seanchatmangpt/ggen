# Audit Result: ggen template list
# Schema: ../../contracts/audit-result.schema.yaml

command: "ggen template list"
version_tested: "5.3.2"
date: "2024-12-14"
tester: "claude-code"

functional_correctness:
  executes: true
  help_works: true
  happy_path: true
  error_handling: true
  exit_codes: true

agent_score: 52
agent_breakdown:
  parseable_output: 8     # Text list, no JSON format
  error_messages: 12      # Basic clap errors
  idempotency: 15         # Read-only - fully idempotent
  progress_feedback: 4    # Silent
  dry_run: 0              # Not applicable (read-only)
  documentation: 4        # Minimal help
  exit_codes: 9           # Good exit codes

avatar_notes:
  claude_code: "Read-only list. No JSON output format option."
  cursor_ai: "Simple command. Short help."
  copilot: "Pattern: --directory optional. Simple."
  aider: "Idempotent read-only. Safe to repeat."
  devin: "Lists templates in directory. Self-contained."
  openhands: "Directory customizable."
  windsurf: "Text output - could show in terminal panel."

maturity_level: L3
maturity_blockers:
  - "No JSON output format"

evidence_files:
  - template-list-help.log

recommendations:
  - priority: P2
    description: "Add --output-format json for structured output"
    effort: low
    impact: high
  - priority: P3
    description: "Add usage examples to help"
    effort: low
    impact: medium
