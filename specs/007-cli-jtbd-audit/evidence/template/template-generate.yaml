command: "ggen template generate"
version_tested: "4.0.0"
date: "2024-12-14"
tester: "claude-code"

functional_correctness:
  executes: true
  help_works: true
  happy_path: true
  error_handling: true
  exit_codes: true

agent_score: 65
agent_breakdown:
  parseable_output: 20
  error_messages: 16
  idempotency: 12
  progress_feedback: 8
  dry_run: 5
  documentation: 4
  exit_codes: 4

avatar_notes:
  claude_code: "Critical for code generation; --dry-run preview prevents errors"
  cursor_ai: "Generation success/failure clear from exit codes"
  copilot: "Patterns learnable from template examples"
  aider: "Safe for git automation with --force flag; idempotent"
  devin: "Can chain multiple generations; feedback helpful"
  openhands: "Customizable via template variables; good flexibility"
  windsurf: "Generated output visualization helpful for preview"

maturity_level: "L3"
maturity_blockers:
  - "Exit codes not fully semantic (could differentiate validation vs I/O errors)"

evidence_files: []

recommendations:
  - priority: P1
    description: "Implement comprehensive --dry-run with full diff output"
    effort: medium
    impact: high
  - priority: P1
    description: "Add semantic exit codes (1=validation, 2=file exists, 3=template not found)"
    effort: low
    impact: high
  - priority: P2
    description: "Add --output-format json for generation results"
    effort: low
    impact: medium

jtbd: |
  When I want to generate code from a template,
  I need to run 'ggen template generate' with template and variables,
  so that I can produce project structure automatically.
