# Audit Result: ggen template regenerate
# Schema: ../../contracts/audit-result.schema.yaml

command: "ggen template regenerate"
version_tested: "5.3.2"
date: "2024-12-14"
tester: "claude-code"

functional_correctness:
  executes: true
  help_works: true
  happy_path: true
  error_handling: true
  exit_codes: true

agent_score: 52
agent_breakdown:
  parseable_output: 8     # Regenerates files, no structured output
  error_messages: 12      # Basic error handling
  idempotency: 12         # Regeneration is inherently idempotent
  progress_feedback: 4    # Silent
  dry_run: 0              # No --dry-run
  documentation: 4        # Minimal help
  exit_codes: 8           # Differentiated codes

avatar_notes:
  claude_code: "Regenerate from existing template. No diff preview."
  cursor_ai: "Simple regeneration. Single option."
  copilot: "Pattern: --template path for regeneration."
  aider: "Regeneration produces git-trackable changes."
  devin: "Re-run generation. Self-contained."
  openhands: "Limited options."
  windsurf: "File modification. IDE can show diff."

maturity_level: L3
maturity_blockers:
  - "No dry-run or diff preview"

evidence_files:
  - template-regenerate-help.log

recommendations:
  - priority: P1
    description: "Add --dry-run to preview changes before regenerating"
    effort: medium
    impact: high
  - priority: P2
    description: "Show diff of changes during regeneration"
    effort: medium
    impact: high
  - priority: P3
    description: "Add --incremental for partial regeneration"
    effort: high
    impact: medium
