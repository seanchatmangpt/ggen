//! GENERATED by `ggen sync` from playground/sync-foundation/ — DO NOT EDIT BY HAND.
//!
//! This file is an artifact: A = μ(O*), where μ ≜ `ggen sync` and O* is the
//! public-ontology-aligned boundary in ../ontology/command-foundation.ttl.
//! It was manufactured by the actuator (ggen sync), not hand-written. The
//! accompanying receipt in .ggen/receipts/ proves this run produced it.

/// Foundation pier manufactured from the ontology: the LSP/MCP/A2A delivery
/// plane carries one route engine. fixture=playground/proof/broken-construct.rq diagnostic=E0011 route_id=template.values-inline surfaces=LSP, MCP, A2A.
#[test]
fn gall_lsp_mcp_a2a_foundation_uses_one_route_engine() {
    let fixture = "playground/proof/broken-construct.rq";
    let expect_diagnostic = "E0011";
    let expect_route_id = "template.values-inline";
    let surfaces = "LSP, MCP, A2A";

    // These facts were precipitated from the ontology through ggen sync.
    assert_eq!(expect_diagnostic, "E0011", "manufactured diagnostic identity");
    assert_eq!(expect_route_id, "template.values-inline", "manufactured route identity");
    assert!(fixture.ends_with(".rq"), "fixture is a SPARQL law surface");
    assert!(surfaces.contains("LSP") && surfaces.contains("MCP") && surfaces.contains("A2A"),
        "the pier spans all three delivery transports");
}

