# TOGAF Building Blocks - {{ sparql_results[0]["?contextName"] | default(value="Attribution") }} Context

> **Generated Document - DO NOT EDIT**
>
> This file is automatically generated by ggen from ontology/
> To make changes, edit the ontology and run: ggen sync

## Architecture Building Blocks (ABBs)

### Business Architecture

**Bounded Context**: {{ sparql_results[0]["?contextName"] | default(value="Attribution") }}

### Application Architecture

#### Aggregates (Domain Models)
{% set aggregates = [] -%}
{% for row in sparql_results -%}
{%- if row["?aggregateName"] and row["?aggregateName"] not in aggregates %}
{%- set_global aggregates = aggregates | concat(with=row["?aggregateName"]) -%}
- **{{ row["?aggregateName"] }}**: Aggregate root managing {{ row["?aggregateName"] | lower }} lifecycle
{% endif -%}
{%- endfor %}

#### Entities (Value Objects)
{% set entities = [] -%}
{% for row in sparql_results -%}
{%- if row["?entityName"] and row["?entityName"] not in entities %}
{%- set_global entities = entities | concat(with=row["?entityName"]) -%}
- **{{ row["?entityName"] }}**: Domain entity
{% endif -%}
{%- endfor %}

## Solution Building Blocks (SBBs)

### Commands (Write Operations)
{% set commands = [] -%}
{% for row in sparql_results -%}
{%- if row["?commandName"] and row["?commandName"] not in commands %}
{%- set_global commands = commands | concat(with=row["?commandName"]) -%}
- **{{ row["?commandName"] }}**: Command handler for {{ row["?commandName"] | lower }} operation
{% endif -%}
{%- endfor %}

### Events (State Changes)
{% set events = [] -%}
{% for row in sparql_results -%}
{%- if row["?eventName"] and row["?eventName"] not in events %}
{%- set_global events = events | concat(with=row["?eventName"]) -%}
- **{{ row["?eventName"] }}**: Domain event representing {{ row["?eventName"] | lower }}
{% endif -%}
{%- endfor %}

## Technology Architecture

### Technology Stack
- **Language**: Rust (type-safe, zero-cost abstractions)
- **Web Framework**: Axum (async HTTP server)
- **Serialization**: Serde (JSON, YAML, TOML)
- **Runtime**: Tokio (async runtime)
- **Event Sourcing**: Custom (durable event log)

### Integration Patterns
- **Command-Query Responsibility Segregation (CQRS)**: Separate write model (aggregates) from read model (projections)
- **Event Sourcing**: All state changes captured as immutable events
- **Domain Events**: Asynchronous communication between bounded contexts

---

**Generated by**: ggen v6.0.0
**Source**: ontology/
**Last Updated**: {{ now() | date(format="%Y-%m-%d %H:%M:%S UTC") }}
