#!/usr/bin/env bash
# Live-check script — generated by ggen
# Source: ontology/ggen-semconv.ttl
# DO NOT EDIT — regenerate with: ggen sync
set -e

REGISTRY_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/generated"
WEAVER="${WEAVER:-weaver}"

echo "=== ggen OTel Semantic Convention Live-Check ==="
echo "Registry: ${REGISTRY_DIR}"
echo ""

# Validate the generated Weaver registry
"${WEAVER}" registry check \
  --registry "${REGISTRY_DIR}"

echo ""
echo "Semantic convention groups:"
{% for row in sparql_results %}echo "  [{{ row["groupType"] }}] {{ row["groupId"] }}"
{% endfor %}
echo ""
echo "Live-check complete."
