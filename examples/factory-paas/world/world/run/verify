#!/usr/bin/env bash
# Generated World Closure: VERIFY - DO NOT EDIT
#
# This file is automatically generated by ggen from ontology/
# To make changes, edit the ontology and run: ggen sync

set -euo pipefail

PROJECT_NAME="Attribution"
SERVICE_PORT="8080"

echo "🧪 TCPS: Verifying Attribution World"

EXIT_CODE=0

# Check if service is running
echo "1️⃣ Checking if service is running..."
PID_FILE="/tmp/${PROJECT_NAME}.pid"
if [ -f "$PID_FILE" ] && ps -p $(cat "$PID_FILE") > /dev/null 2>&1; then
    echo "   ✅ Service is running"
else
    echo "   ❌ Service is not running"
    EXIT_CODE=1
fi

# Check if port is listening
echo "2️⃣ Checking if port ${SERVICE_PORT} is listening..."
if command -v lsof &> /dev/null; then
    if lsof -i :${SERVICE_PORT} > /dev/null 2>&1; then
        echo "   ✅ Port ${SERVICE_PORT} is listening"
    else
        echo "   ❌ Port ${SERVICE_PORT} is not listening"
        EXIT_CODE=1
    fi
else
    echo "   ⚠️  lsof not available, skipping port check"
fi

# Run generated tests
echo "3️⃣ Running generated tests..."
cd "$(dirname "$0")/.."
if cargo test --manifest-path world/Cargo.toml; then
    echo "   ✅ All tests passed"
else
    echo "   ❌ Some tests failed"
    EXIT_CODE=1
fi

# Summary
echo ""
if [ $EXIT_CODE -eq 0 ]; then
    echo "✅ World verification PASSED"
else
    echo "❌ World verification FAILED"
fi

exit $EXIT_CODE
