//! Generated Command Handlers - DO NOT EDIT
//!
//! This file is automatically generated by ggen from ontology/commands.ttl
//! To make changes, edit the ontology and run: ggen sync

use crate::commands::*;
use crate::events::*;
use crate::aggregates::*;
use crate::otel_tracing::{create_command_span, SpanContext};
use crate::otel_logs::{log_command_start, log_command_success, log_command_error};
use std::time::Instant;
use uuid::Uuid;

pub type Result<T> = std::result::Result<T, HandlerError>;

#[derive(Debug, thiserror::Error)]
pub enum HandlerError {
    #[error("Validation failed: {0}")]
    Validation(String),
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Business rule violated: {0}")]
    BusinessRule(String),
}

/// Handle CancelSubscription command with OpenTelemetry instrumentation
pub fn handle_cancelsubscription(
    aggregate: &mut RevenueAggregate,
    command: CancelSubscription,
    span_context: &SpanContext,
) -> Result<SubscriptionCanceled> {
    let command_id = Uuid::new_v4();
    let aggregate_id = Uuid::new_v4(); // Get from aggregate
    let start_time = Instant::now();

    // Create instrumented span
    let span = create_command_span("CancelSubscription", &command_id, &aggregate_id);
    let _guard = span.enter();

    // Log command start
    log_command_start(
        "CancelSubscription",
        &command_id,
        &aggregate_id,
        span_context.trace_id,
        span_context.span_id,
    );

    // Business logic validation
    // This is where domain invariants are enforced
    let result = (|| -> Result<SubscriptionCanceled> {
        // TODO: Add validation logic here

        // Emit event
        Ok(SubscriptionCanceled {
            // Map command fields to event fields
            ..Default::default()
        })
    })();

    let duration_ms = start_time.elapsed().as_millis() as u64;

    // Log result
    match &result {
        Ok(_) => {
            log_command_success(
                "CancelSubscription",
                &command_id,
                &aggregate_id,
                duration_ms,
                span_context.trace_id,
                span_context.span_id,
            );
        }
        Err(e) => {
            log_command_error(
                "CancelSubscription",
                &command_id,
                &aggregate_id,
                &e.to_string(),
                span_context.trace_id,
                span_context.span_id,
            );
        }
    }

    result
}

/// Handle RecordRecurringCommission command with OpenTelemetry instrumentation
pub fn handle_recordrecurringcommission(
    aggregate: &mut RevenueAggregate,
    command: RecordRecurringCommission,
    span_context: &SpanContext,
) -> Result<RecurringCommissionRecorded> {
    let command_id = Uuid::new_v4();
    let aggregate_id = Uuid::new_v4(); // Get from aggregate
    let start_time = Instant::now();

    // Create instrumented span
    let span = create_command_span("RecordRecurringCommission", &command_id, &aggregate_id);
    let _guard = span.enter();

    // Log command start
    log_command_start(
        "RecordRecurringCommission",
        &command_id,
        &aggregate_id,
        span_context.trace_id,
        span_context.span_id,
    );

    // Business logic validation
    // This is where domain invariants are enforced
    let result = (|| -> Result<RecurringCommissionRecorded> {
        // TODO: Add validation logic here

        // Emit event
        Ok(RecurringCommissionRecorded {
            // Map command fields to event fields
            ..Default::default()
        })
    })();

    let duration_ms = start_time.elapsed().as_millis() as u64;

    // Log result
    match &result {
        Ok(_) => {
            log_command_success(
                "RecordRecurringCommission",
                &command_id,
                &aggregate_id,
                duration_ms,
                span_context.trace_id,
                span_context.span_id,
            );
        }
        Err(e) => {
            log_command_error(
                "RecordRecurringCommission",
                &command_id,
                &aggregate_id,
                &e.to_string(),
                span_context.trace_id,
                span_context.span_id,
            );
        }
    }

    result
}

/// Handle TrackSubscription command with OpenTelemetry instrumentation
pub fn handle_tracksubscription(
    aggregate: &mut RevenueAggregate,
    command: TrackSubscription,
    span_context: &SpanContext,
) -> Result<SubscriptionCreated> {
    let command_id = Uuid::new_v4();
    let aggregate_id = Uuid::new_v4(); // Get from aggregate
    let start_time = Instant::now();

    // Create instrumented span
    let span = create_command_span("TrackSubscription", &command_id, &aggregate_id);
    let _guard = span.enter();

    // Log command start
    log_command_start(
        "TrackSubscription",
        &command_id,
        &aggregate_id,
        span_context.trace_id,
        span_context.span_id,
    );

    // Business logic validation
    // This is where domain invariants are enforced
    let result = (|| -> Result<SubscriptionCreated> {
        // TODO: Add validation logic here

        // Emit event
        Ok(SubscriptionCreated {
            // Map command fields to event fields
            ..Default::default()
        })
    })();

    let duration_ms = start_time.elapsed().as_millis() as u64;

    // Log result
    match &result {
        Ok(_) => {
            log_command_success(
                "TrackSubscription",
                &command_id,
                &aggregate_id,
                duration_ms,
                span_context.trace_id,
                span_context.span_id,
            );
        }
        Err(e) => {
            log_command_error(
                "TrackSubscription",
                &command_id,
                &aggregate_id,
                &e.to_string(),
                span_context.trace_id,
                span_context.span_id,
            );
        }
    }

    result
}


