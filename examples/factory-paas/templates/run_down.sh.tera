#!/usr/bin/env bash
# Generated World Closure: DOWN - DO NOT EDIT
#
# This file is automatically generated by ggen from ontology/
# To make changes, edit the ontology and run: ggen sync

set -euo pipefail

PROJECT_NAME="{{ sparql_results[0]["?contextName"] | default(value="attribution") }}"

echo "🛑 TCPS: Shutting down {{ sparql_results[0]["?contextName"] | default(value="Attribution") }} World"

PID_FILE="/tmp/${PROJECT_NAME}.pid"

if [ -f "$PID_FILE" ]; then
    PID=$(cat "$PID_FILE")
    echo "Stopping service (PID: ${PID})..."
    kill $PID 2>/dev/null || true
    rm -f "$PID_FILE"
    echo "✅ World is down"
else
    echo "⚠️  No PID file found. Service may not be running."
fi
