# TOGAF Technology Realization - {{ sparql_results[0]["?contextName"] | default(value="Attribution") }} Context

> **Generated Document - DO NOT EDIT**
>
> This file is automatically generated by ggen from ontology/infra.ttl
> To make changes, edit the ontology and run: ggen sync

## Infrastructure Realization

### Compute Resources

{% for row in sparql_results -%}
{%- if row["?resourceName"] %}
#### {{ row["?resourceName"] }}

- **Provider**: {{ row["?provider"] | upper }}
- **Shape**: {{ row["?shape"] | default(value="VM.Standard.A1.Flex") }}
- **Purpose**: Host {{ sparql_results[0]["?contextName"] | default(value="Attribution") }} service

**Provisioning**: Terraform (see `world/infra/terraform/`)

{% endif -%}
{%- endfor %}

## Deployment Architecture

```
┌─────────────────────────────────────┐
│  Cloud Provider ({{ sparql_results[0]["?provider"] | upper | default(value="OCI") }})              │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ Compute Instance              │ │
│  │                               │ │
│  │ ┌─────────────────────────┐   │ │
│  │ │ {{ sparql_results[0]["?contextName"] | default(value="Attribution") }} Service      │   │ │
│  │ │ (Rust + Axum)           │   │ │
│  │ └─────────────────────────┘   │ │
│  │                               │ │
│  │ ┌─────────────────────────┐   │ │
│  │ │ Event Store             │   │ │
│  │ │ (Durable log)           │   │ │
│  │ └─────────────────────────┘   │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
```

## Operations

### Provisioning
```bash
cd world/infra/terraform
terraform init
terraform plan
terraform apply
```

### Deployment
```bash
./world/run/up
```

### Monitoring
```bash
./world/run/status
./world/run/verify
```

---

**Generated by**: ggen v6.0.0
**Source**: ontology/infra.ttl
**Last Updated**: {{ now() | date(format="%Y-%m-%d %H:%M:%S UTC") }}
