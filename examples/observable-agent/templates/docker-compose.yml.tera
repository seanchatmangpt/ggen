# docker-compose.yml — generated by ggen
# Source: ontology/observable-agent.ttl
# DO NOT EDIT — regenerate with: ggen sync
# Agent: {{ sparql_results | first | get(key="agentName") }} v{{ sparql_results | first | get(key="agentVersion") }}
version: "3.9"

services:
{% for row in sparql_results %}  {{ row["serviceName"] }}:
    image: {{ row["serviceImage"] }}
    ports:
      - "{{ row["servicePort"] }}:{{ row["servicePort"] }}"
    restart: unless-stopped

{% endfor %}
networks:
  default:
    name: observable-agent-net
