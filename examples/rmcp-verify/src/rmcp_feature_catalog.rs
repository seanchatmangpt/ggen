//! Typed catalog of the 15 real Cargo feature flags `rmcp`'s own README.md
//! documents across its 3 real markdown tables ("Feature Flags",
//! "Transport features", "TLS backend options").
//!
//! Generated by `rmcp-pack` from `packs/rmcp-pack/ontology.ttl`. NOT a
//! complete account of `rmcp`'s `Cargo.toml` `[features]` table, which
//! defines 26 flags total -- see the ontology's own header for the 11
//! undocumented flags this pass deliberately left out of scope.

/// One real Cargo feature flag documented in `rmcp`'s README.md.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct RmcpFeature {
    pub feature_name: &'static str,
    /// Verbatim one-line description from README.md's own feature tables.
    pub feature_desc: &'static str,
    /// true only for the 2 rows README.md's own "Default" column marks
    /// (server, macros). `Cargo.toml`'s real default array additionally
    /// includes `base64`, out of this pass's modeled scope -- see
    /// ontology.ttl's rmcp:featureDefault comment.
    pub feature_default: bool,
}

/// All 15 real, README-documented Cargo feature flags, ordered by name.
pub const RMCP_FEATURES: &[RmcpFeature] = &[
    RmcpFeature { feature_name: "auth", feature_desc: "OAuth 2.0 authentication support", feature_default: false },
    RmcpFeature { feature_name: "client", feature_desc: "Client functionality", feature_default: false },
    RmcpFeature { feature_name: "elicitation", feature_desc: "Elicitation support", feature_default: false },
    RmcpFeature { feature_name: "macros", feature_desc: "#[tool] / #[prompt] macros (re-exports rmcp-macros)", feature_default: true },
    RmcpFeature { feature_name: "reqwest", feature_desc: "Uses rustls -- pure Rust TLS (recommended default)", feature_default: false },
    RmcpFeature { feature_name: "reqwest-native-tls", feature_desc: "Uses platform-native TLS (OpenSSL / Secure Transport / SChannel)", feature_default: false },
    RmcpFeature { feature_name: "reqwest-tls-no-provider", feature_desc: "Uses rustls without a default crypto provider (bring your own)", feature_default: false },
    RmcpFeature { feature_name: "schemars", feature_desc: "JSON Schema generation for tool definitions", feature_default: false },
    RmcpFeature { feature_name: "server", feature_desc: "Server functionality and the tool system", feature_default: true },
    RmcpFeature { feature_name: "transport-async-rw", feature_desc: "Generic async read/write transport", feature_default: false },
    RmcpFeature { feature_name: "transport-child-process", feature_desc: "Client-side stdio transport (spawns a child process)", feature_default: false },
    RmcpFeature { feature_name: "transport-io", feature_desc: "Server-side stdio transport", feature_default: false },
    RmcpFeature { feature_name: "transport-streamable-http-client", feature_desc: "Streamable HTTP client (transport-agnostic)", feature_default: false },
    RmcpFeature { feature_name: "transport-streamable-http-client-reqwest", feature_desc: "Streamable HTTP client with default reqwest backend", feature_default: false },
    RmcpFeature { feature_name: "transport-streamable-http-server", feature_desc: "Streamable HTTP server transport", feature_default: false },
];
