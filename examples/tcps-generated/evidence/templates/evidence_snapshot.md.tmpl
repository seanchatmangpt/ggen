---
to: receipts/EVIDENCE_SNAPSHOT.md
force: true
sparql:
  rows: |
    PREFIX ver: <http://seanchatmangpt.github.io/packs/ggen-verify#>
    SELECT ?name ?exitCode ?recordedAt ?hash WHERE {
      ?c a ver:Check ;
         ver:name ?name ;
         ver:exitCode ?exitCode .
      OPTIONAL { ?c ver:recordedAt ?recordedAt }
      OPTIONAL { ?c ver:verifiedGraphHash ?hash }
    } ORDER BY ?name
---
# Evidence Snapshot

Raw admitted `ver:Check` facts as of the last sync (source: this consumer's
`evidence/ontology.ttl`, regenerated by `scripts/verify.sh`).

| Check | Exit | Recorded | Bound to graph hash |
|---|---|---|---|
{% for r in rows %}| {{ r.name }} | {{ r.exitCode }} | {{ r.recordedAt | default(value="-") }} | {{ r.hash | default(value="-") }} |
{% endfor %}
