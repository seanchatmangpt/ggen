/-
GENERATED by ggen (crown-conjecture-pack) from `.specify`-adjacent RDF facts transcribed
directly from the real Lean source cited below. Never hand-edit this file -- edit
`packs/crown-conjecture-pack/ontology.ttl` and resync.

This file contains NO `sorry`, `admit`, or any other incomplete-proof placeholder, by
design: it restates the real obligation as `def : Prop`, exactly matching
`mfw-theory/MFW/Kernel.lean`'s own stated "Constructive Lean Boundary" -- "never a
theorem closed by an incomplete-proof placeholder" (Kernel.lean:24-26, confirmed by
direct read). ggen does not attempt the mathematics and does not fabricate a proof
attempt that isn't real; it projects the obligation's known structure (real cited
definitions, a real blocking finding, the real reasoning steps a completed proof would
need) so a mathematician has a grounded starting point, not a placeholder to fill.
-/
import Mathlib
import MFW.TransformBasic
import MFW.Concurrency
import MFW.Kernel

namespace MFW


/-!
## BLOCKING FINDING (Load-bearing -- blocks any real (non-vacuous) attempt at proof step 4)

Source: `mfw-theory/MFW/Concurrency.lean:36-44`

The only TemporalIndependenceWitness instance ever constructed anywhere in this codebase, trivialTemporalIndependenceWitness, sets ALL FIVE real components (effectsCommute, preconditionStable, invariantStable, numericFlowCompatible, trajectoryPreserved) to `fun _ _ _ => True` -- vacuously true. The source's own comment states this outright: 'carries no proof obligations between its fields, so any concrete assignment instantiates it. mfact never constructed one.' A proof of TraceSwapPreservesLawful attempted against this witness would prove something about a vacuous independence notion, not real PDDL 3.1 semantics -- the swap constructor's hypothesis I.independent a b would carry no real content connecting it to what the two actions actually do. This is a real, present gap, not a hypothetical caution: constructing a genuinely non-trivial TemporalIndependenceWitness (with the 5 components' real proof obligations to Th.transition/Th.State properly threaded through) is a real PRIOR obligation the Crown Conjecture proof depends on, not optional polish.

Proof steps below that depend on a real (non-vacuous) independence witness cannot be
honestly completed until this finding is resolved -- see `step4_side_conditions`.
-/


/-!
## Real definitions this obligation depends on

Transcribed directly from source, not paraphrased. Re-verify against the live file if it
may have moved since this ontology was written.
-/

/-
`BehaviorTrace` (Standing: Proven) — `mfw-theory/MFW/TransformBasic.lean:82-88`

structure BehaviorTrace (Th : PlanningTheory) where
  events : List Th.Action
  timestamps : List Real
  length_match : events.length = timestamps.length
-/


/-
`TraceEquiv` (Standing: Proven) — `mfw-theory/MFW/Concurrency.lean:64-73`

inductive TraceEquiv {Action : Type} (I : IndependenceRelation Action) : List Action -> List Action -> Prop
  | refl (s : List Action) : TraceEquiv I s s
  | swap (pre : List Action) (a b : Action) (suf : List Action) (h : I.independent a b) :
      TraceEquiv I (pre ++ [a, b] ++ suf) (pre ++ [b, a] ++ suf)
  | trans (s1 s2 s3 : List Action) : TraceEquiv I s1 s2 -> TraceEquiv I s2 s3 -> TraceEquiv I s1 s3
  | symm (s1 s2 : List Action) : TraceEquiv I s1 s2 -> TraceEquiv I s2 s1
-/


/-
`IsLawful` (Standing: Proven) — `mfw-theory/MFW/TransformBasic.lean:106-112`

def IsLawful (Th : PlanningTheory) (b : BehaviorTrace Th) : Prop :=
  exists (trace : List Th.State), b.stateTrace = some trace and trace.length = b.events.length + 1 and
    (exists final in trace.getLast?, Th.isGoal final) and
    (forall tc in Th.temporalConstraints, Th.temporalSatisfied tc b.timestamps) and
    (forall trc in Th.trajectoryConstraints, Th.trajectorySatisfied trc trace)
-/


/-
`TemporalIndependenceWitness` (Standing: Stated) — `mfw-theory/MFW/Concurrency.lean:24-34`

structure TemporalIndependenceWitness (Action State : Type) where
  effectsCommute : Action -> Action -> State -> Prop
  preconditionStable : Action -> Action -> State -> Prop
  invariantStable : Action -> Action -> Prop
  numericFlowCompatible : Action -> Action -> Prop
  trajectoryPreserved : Action -> Action -> Prop
-/


/-
`IndependenceRelation` (Standing: Proven) — `mfw-theory/MFW/Concurrency.lean:17-21`

structure IndependenceRelation (Action : Type) where
  independent : Action -> Action -> Prop
  symm : forall a b, independent a b -> independent b a
-/




/-!
## Proof obligation: `TraceSwapPreservesLawful`

Source: `mfw-theory/MFW/Kernel.lean:287-290`. Standing: Conjectural (from the ontology's
own `cc:standing` fact, not hand-typed here -- re-sync after editing the ontology to see
this change reflected). The real statement:

```
def TraceSwapPreservesLawful {Th : PlanningTheory} (I : IndependenceRelation Th.Action) (b1 b2 : BehaviorTrace Th) : Prop :=
  TraceEquiv I b1.events b2.events -> IsLawful Th b1 -> IsLawful Th b2
```

Structural decomposition below (`4` real reasoning steps a completed
proof would need, not a placeholder for each):

1. Unpack real definitions -- Ground 'lawful' as IsLawful's real 5-conjunct existential (defined state trace, correct length, goal satisfied, temporal constraints, trajectory constraints) and 'independent' as IndependenceRelation.independent, grounded via a real (non-trivial) TemporalIndependenceWitness's 5 components -- not the symbolic gloss, the real Lean terms.

2. Single-swap commutation lemma -- The real mathematical core: for adjacent independent a, b with I.independent a b, show that if pre ++ [a,b] ++ suf is lawful then pre ++ [b,a] ++ suf is lawful, by case analysis over IsLawful's 5 conjuncts, using the witness's 5 components to show neither action's effect/precondition/invariant/numeric-flow/trajectory-constraint is disturbed by the other.
   Real tactic sketch: Corresponds exactly to the TraceEquiv.swap constructor case (Concurrency.lean:67-69): pre ++ [a,b] ++ suf -> pre ++ [b,a] ++ suf under h : I.independent a b.

3. Lift by induction over TraceEquiv's real inductive structure -- TraceEquiv is confirmed inductive with 4 constructors (refl/swap/trans/symm), not an abstract closure -- so `induction h` on the real TraceEquiv derivation h : TraceEquiv I b1.events b2.events is the real proof strategy, not a bespoke length/position induction. refl: trivial (b1 = b2). swap: exactly proof step 2. trans: chain two induction hypotheses through the intermediate trace's lawfulness. symm: requires IsLawful to transfer backward too -- worth flagging explicitly since the conjecture's own statement (TraceEquiv -> IsLawful b1 -> IsLawful b2) is directional, so the symm case needs either a separately-proved backward direction or a demonstration that TraceEquiv's symm constructor doesn't actually need a fresh case (since TraceEquiv I s t already implies TraceEquiv I t s by symm, the forward statement instantiated at the swapped pair may suffice -- this needs to be checked against the real term, not assumed).

4. Discharge side conditions on the independence witness -- Requires a real, non-trivial TemporalIndependenceWitness with the 5 components' real proof obligations correctly connected to Th.transition/Th.State -- currently blocked, see cc:finding_vacuous_witness. Attempting steps 1-3 against trivialTemporalIndependenceWitness would technically type-check but prove nothing about real PDDL 3.1 semantics, since I.independent a b would carry no real content under that instantiation.
   Real tactic sketch: Not attemptable as a tactic sketch until a real witness exists -- this step's real content is constructing that witness, not proving a lemma about it.

-/
def TraceSwapPreservesLawful_restated {Th : PlanningTheory}
    (I : IndependenceRelation Th.Action)
    (b1 b2 : BehaviorTrace Th) : Prop :=
    TraceEquiv I b1.events b2.events -> IsLawful Th b1 -> IsLawful Th b2
-- Standing: Conjectural (ontology-derived, see cc:standing on this obligation --
-- not hand-typed). This `def` restates the real obligation for cross-reference; it
-- proves nothing and claims nothing proved. Resolving it (proving OR refuting) is real
-- mathematical work this pack does not attempt.



end MFW
