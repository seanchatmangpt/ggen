# Workspace Project Example

A complete Rust workspace with multiple crates.

- **core**: Domain models
- **web**: HTTP server
- **cli**: Command-line tool

## Quick Start

```bash
cargo build --all
cargo test --all
cargo make pre-commit
```

## Tests

All crates have tests: `cargo test --all`
