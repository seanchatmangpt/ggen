---
to: "docs/ARCHITECTURE.md"
---
# Architecture

```text
parse request -> validate UIMessage[] -> authenticated agent -> model/tool selection -> mutation approval -> ToolBroker -> adapter/refusal -> streamed receipt
```

No arbitrary JavaScript is stored in RDF. No tool actuates outside `ToolBroker`. No mutating tool is admitted without approval. No `generated/` directory is used.
