//! Generated Property Tests - DO NOT EDIT
//!
//! This file is automatically generated by ggen from ontology/entities.ttl
//! To make changes, edit the ontology and run: ggen sync

use proptest::prelude::*;
use uuid::Uuid;
use chrono::Utc;

use world::*;

// Property test strategy for AffiliateLinkRoute
prop_compose! {
    fn arb_affiliatelinkroute()(
        active in prop::bool::ANY,// Property test strategy for AffiliateLinkRoute
prop_compose! {
    fn arb_affiliatelinkroute()(
        affiliate_id in prop::strategy::Just(Uuid::new_v4()),// Property test strategy for AffiliateLinkRoute
prop_compose! {
    fn arb_affiliatelinkroute()(
        created_at in prop::strategy::Just(Utc::now()),// Property test strategy for AffiliateLinkRoute
prop_compose! {
    fn arb_affiliatelinkroute()(
        destination_url in "[a-zA-Z0-9 ]{1,100}",// Property test strategy for AffiliateLinkRoute
prop_compose! {
    fn arb_affiliatelinkroute()(
        id in prop::strategy::Just(Uuid::new_v4()),// Property test strategy for AffiliateLinkRoute
prop_compose! {
    fn arb_affiliatelinkroute()(
        source_path in "[a-zA-Z0-9 ]{1,100}",// Property test strategy for AffiliateLinkRoute
prop_compose! {
    fn arb_affiliatelinkroute()(
        tracking_params in "[a-zA-Z0-9 ]{1,100}",// Property test strategy for Offer
prop_compose! {
    fn arb_offer()(
        active in prop::bool::ANY,// Property test strategy for Offer
prop_compose! {
    fn arb_offer()(
        id in prop::strategy::Just(Uuid::new_v4()),// Property test strategy for Offer
prop_compose! {
    fn arb_offer()(
        name in "[a-zA-Z0-9 ]{1,100}",// Property test strategy for Offer
prop_compose! {
    fn arb_offer()(
        payout_amount in 0.0..1000000.0f64,// Property test strategy for Offer
prop_compose! {
    fn arb_offer()(
        payout_currency in "[a-zA-Z0-9 ]{1,100}",// Property test strategy for Publisher
prop_compose! {
    fn arb_publisher()(
        created_at in prop::strategy::Just(Utc::now()),// Property test strategy for Publisher
prop_compose! {
    fn arb_publisher()(
        email in "[a-zA-Z0-9 ]{1,100}",// Property test strategy for Publisher
prop_compose! {
    fn arb_publisher()(
        id in prop::strategy::Just(Uuid::new_v4()),// Property test strategy for Publisher
prop_compose! {
    fn arb_publisher()(
        name in "[a-zA-Z0-9 ]{1,100}",// Property test strategy for Publisher
prop_compose! {
    fn arb_publisher()(
        total_revenue in 0.0..1000000.0f64,// Property test strategy for SaasProgram
prop_compose! {
    fn arb_saasprogram()(
        active in prop::bool::ANY,// Property test strategy for SaasProgram
prop_compose! {
    fn arb_saasprogram()(
        api_integration in "[a-zA-Z0-9 ]{1,100}",// Property test strategy for SaasProgram
prop_compose! {
    fn arb_saasprogram()(
        billing_cycle in "[a-zA-Z0-9 ]{1,100}",// Property test strategy for SaasProgram
prop_compose! {
    fn arb_saasprogram()(
        commission_rate in 0.0..1000000.0f64,// Property test strategy for SaasProgram
prop_compose! {
    fn arb_saasprogram()(
        created_at in prop::strategy::Just(Utc::now()),// Property test strategy for SaasProgram
prop_compose! {
    fn arb_saasprogram()(
        id in prop::strategy::Just(Uuid::new_v4()),// Property test strategy for SaasProgram
prop_compose! {
    fn arb_saasprogram()(
        name in "[a-zA-Z0-9 ]{1,100}",// Property test strategy for SaasProgram
prop_compose! {
    fn arb_saasprogram()(
        webhook_secret in "[a-zA-Z0-9 ]{1,100}",// Property test strategy for Subscription
prop_compose! {
    fn arb_subscription()(
        cancel_date in prop::strategy::Just(Utc::now()),// Property test strategy for Subscription
prop_compose! {
    fn arb_subscription()(
        created_at in prop::strategy::Just(Utc::now()),// Property test strategy for Subscription
prop_compose! {
    fn arb_subscription()(
        currency in "[a-zA-Z0-9 ]{1,100}",// Property test strategy for Subscription
prop_compose! {
    fn arb_subscription()(
        customer_id in "[a-zA-Z0-9 ]{1,100}",// Property test strategy for Subscription
prop_compose! {
    fn arb_subscription()(
        id in prop::strategy::Just(Uuid::new_v4()),// Property test strategy for Subscription
prop_compose! {
    fn arb_subscription()(
        lifetime_commission in 0.0..1000000.0f64,// Property test strategy for Subscription
prop_compose! {
    fn arb_subscription()(
        mrr_amount in 0.0..1000000.0f64,// Property test strategy for Subscription
prop_compose! {
    fn arb_subscription()(
        next_billing_date in prop::strategy::Just(Utc::now()),// Property test strategy for Subscription
prop_compose! {
    fn arb_subscription()(
        plan in "[a-zA-Z0-9 ]{1,100}",// Property test strategy for Subscription
prop_compose! {
    fn arb_subscription()(
        program_id in prop::strategy::Just(Uuid::new_v4()),// Property test strategy for Subscription
prop_compose! {
    fn arb_subscription()(
        publisher_id in prop::strategy::Just(Uuid::new_v4()),// Property test strategy for Subscription
prop_compose! {
    fn arb_subscription()(
        start_date in prop::strategy::Just(Utc::now()),// Property test strategy for Subscription
prop_compose! {
    fn arb_subscription()(
        status in "[a-zA-Z0-9 ]{1,100}",
    ) ->  {
         {
        }
    }
}

proptest! {
    #[test]
    fn test__serialization_roundtrip(entity in arb_()) {
        // Serialize to JSON
        let json = serde_json::to_string(&entity).unwrap();

        // Deserialize back
        let deserialized:  = serde_json::from_str(&json).unwrap();

        // Should be identical
        assert_eq!(entity, deserialized);
    }
}
