{% include "_header.erl.tera" %}

-module({{ module_prefix }}_governor_app).
-behaviour(application).

-export([start/2, stop/1]).

start(_Type, _Args) ->
  {{ module_prefix }}_governor_sup:start_link().

stop(_State) ->
  ok.
