# wasm4pm Pipeline Stage Plan

> Auto-generated by `ggen sync` from `queries/extract-pipeline-stages.rq`
> (one row per pipeline stage). Do not edit by hand.
>
> This is the ordered 9-stage TPOT2 pipeline plan: each stage is one wasm4pm
> `pi:category` bucket, and the operator count is how many candidate operators
> the genetic search ranks within that stage.

| # | stage_id | label | category | # operators |
|---|----------|-------|----------|-------------|
{%- for row in sparql_results %}
| {{ row["stage_order"] | default(value="") }} | `{{ row["stage_id"] | default(value="") }}` | {{ row["stage_label"] | default(value="") }} | `{{ row["for_category"] | default(value="") }}` | {{ row["operator_count"] | default(value="0") }} |
{%- endfor %}

_9 stages · 60 operators total (bijection to the 9 wasm4pm categories)._
