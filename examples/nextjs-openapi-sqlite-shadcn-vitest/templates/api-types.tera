---
to: lib/types/api.ts
description: TypeScript types for API
metadata:
  category: types
---
/**
 * @fileoverview TypeScript API types
 * DO NOT EDIT - auto-generated by ggen sync
 */

{%- set current_entity = "" -%}
{%- for row in sparql_results -%}
{%- set entityName = row["?entityName"] | default(value="") -%}
{%- set propName = row["?propName"] | default(value="") -%}
{%- set propType = row["?propType"] | default(value="string") -%}
{%- set required = row["?required"] | default(value="false") -%}
{%- set enumValues = row["?enumValues"] | default(value="") -%}

{%- if entityName != current_entity -%}
{%- if current_entity != "" %}
}

export interface Create{{ current_entity }}Request extends Omit<{{ current_entity }}, "id" | "createdAt"> {}
export interface Update{{ current_entity }}Request extends Partial<Omit<{{ current_entity }}, "id" | "createdAt">> {}

{% endif -%}
{%- set_global current_entity = entityName %}
/** {{ entityName }} entity */
export interface {{ entityName }} {
{%- endif %}
  {{ propName }}{% if required != "true" %}?{% endif %}: {% if propType == "string" or propType == "datetime" %}string{% elif propType == "integer" %}number{% elif propType == "boolean" %}boolean{% elif propType == "enum" %}{% for val in enumValues | split(pat=",") %}"{{ val }}"{% if not loop.last %} | {% endif %}{% endfor %}{% else %}string{% endif %};
{%- endfor %}
}

export interface Create{{ current_entity }}Request extends Omit<{{ current_entity }}, "id" | "createdAt"> {}
export interface Update{{ current_entity }}Request extends Partial<Omit<{{ current_entity }}, "id" | "createdAt">> {}

/** API error response */
export interface ApiError {
  error: string;
  details?: unknown;
}

/** Paginated response */
export interface PaginatedResponse<T> {
  data: T[];
  total: number;
  page: number;
  pageSize: number;
}
