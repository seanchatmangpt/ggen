# Cargo.toml fragment — generated by ggen
# Source: ontology/mcp-server.ttl
# Add to your workspace Cargo.toml to enable the MCP server
#
# Package: {{ sparql_results | first | get(key="serverName") }} v{{ sparql_results | first | get(key="serverVersion") }}

[dependencies]
rmcp = { version = "1.3", features = ["server", "transport-io"] }
serde = { version = "1", features = ["derive"] }
schemars = "0.8"
tokio = { version = "1", features = ["full"] }
anyhow = "1"
