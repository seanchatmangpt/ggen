# Generated by ggen - Performance Library Lifecycle
# This lifecycle configuration was created using ggen's AI generation

[project]
name = "perf-library"
description = "High-performance Rust library with custom data structures and benchmarks"

[lifecycle.bench-baseline]
command = "cargo bench -- --save-baseline baseline"

[lifecycle.bench]
command = "cargo bench"

[lifecycle.bench-compare]
command = "cargo bench -- --baseline baseline"

[lifecycle.profile]
command = "cargo flamegraph --bench performance -- --bench"

[lifecycle.optimize]
commands = [
    "RUSTFLAGS='-C target-cpu=native' cargo build --release",
    "cargo bench -- --baseline baseline"
]

[lifecycle.validate]
commands = [
    "cargo test",
    "cargo clippy --all-targets -- -D warnings"
]

[lifecycle.test]
command = "cargo test --all-features"

[lifecycle.build]
command = "cargo build --release"

[env]
RUSTFLAGS = "-C target-cpu=native"
CARGO_PROFILE_RELEASE_LTO = "fat"
