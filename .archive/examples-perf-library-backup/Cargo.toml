[package]
name = "perf-library"
version = "0.1.0"
edition = "2021"

[dependencies]
ahash = "0.8"
rayon = "1.8"
crossbeam = "0.8"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "performance"
harness = false

[profile.release]
lto = true
codegen-units = 1
opt-level = 3

# Generated by: ggen ai generate --description "High-performance library..."

[workspace]
# Exclude from parent workspace
