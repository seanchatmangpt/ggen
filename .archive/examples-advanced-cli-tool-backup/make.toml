# Generated by ggen - Advanced CLI Tool Lifecycle
# This lifecycle configuration was created using ggen's AI project generation

[project]
name = "advanced-cli-tool"
description = "Advanced CLI tool with async I/O and comprehensive error handling"

[lifecycle.format]
command = "cargo fmt -- --check"

[lifecycle.lint]
command = "cargo clippy --all-targets --all-features -- -D warnings"

[lifecycle.build]
command = "cargo build --release"

[lifecycle.test]
command = "cargo test --all-features"

[lifecycle.bench]
command = "cargo bench --no-fail-fast"

[lifecycle.audit]
command = "cargo audit"

[lifecycle.doc]
command = "cargo doc --no-deps --open"

[lifecycle.deploy]
commands = [
    "cargo build --release",
    "strip target/release/advanced-cli-tool || true",
    "mkdir -p dist",
    "cp target/release/advanced-cli-tool dist/"
]

[hooks]
before_build = ["format", "lint"]
after_build = ["test"]
before_deploy = ["audit", "test", "build"]

[env]
RUST_BACKTRACE = "1"
RUST_LOG = "info"
