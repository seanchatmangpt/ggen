[package]
name = "perf-lib"
version = "0.1.0"
edition = "2021"

[[bench]]
name = "performance"
harness = false

[dependencies]
ahash = "0.8"
rayon = "1.8"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
