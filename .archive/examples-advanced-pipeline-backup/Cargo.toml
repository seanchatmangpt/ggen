[package]
name = "advanced-pipeline"
version = "0.1.0"
edition = "2021"

[dependencies]
ggen-core = { path = "../../ggen-core" }
anyhow = "1.0"
tokio = { version = "1.40", features = ["full"] }

[dev-dependencies]
tempfile = "3.13"
