<!-- GENERATED by ggen gall-semantic-work. Edit the canonical graph, not this projection. -->
# {{ work_order_iri }}

- Checkpoint: `{{ checkpoint_iri }}`
- Repository identity: `{{ repository_identity }}`
- Base SHA: `{{ base_sha }}`
- Graph digest: `{{ graph_digest }}`
- Provider: `{{ provider }}`
- Verifier: `{{ verifier_suite }}`
- Execution policy: `{{ execution_policy }}`
- Standing: `{{ standing }}`
- Authority: `NONE`

## Goal

{{ goal }}

## Dependency witnesses

```json
{{ dependencies | json_encode(pretty=true) }}
```

This file is a deterministic human projection of the canonical semantic work-order graph. It is not an authority grant, lease, receipt, or source of standing.
