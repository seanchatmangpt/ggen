[package]
name = "sector-support-hooks-8020"
version = "1.0.0"
edition = "2021"
authors = ["ggen Contributors"]
description = "8020 Bundle: Intelligent support case routing and hook patterns covering 80% of customer support workflows"
license = "MIT OR Apache-2.0"
repository = "https://github.com/seanchatmangpt/ggen"

# 8020 Innovation: Critical fields for bundle curation
is_8020 = true
is_8020_certified = false  # Certification pending completion of Guard8020Coverage validation
sector = "support"
dark_matter_reduction_target = "Eliminates ~90% of case routing work (from 30 min/case to 3 min/case)"

[dependencies]
# This bundle composes these core packages
hooks-engine = "1.0.0"
case-routing-patterns = "1.0.0"

[features]
default = []

[metadata]
keywords = ["support", "customer-service", "case-routing", "hooks", "automation", "workflow"]
categories = ["development-tools", "customer-support", "automation"]
