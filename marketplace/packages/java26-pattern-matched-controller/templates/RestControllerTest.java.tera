// Generated by ggen — Pattern: PATTERN-MATCHED CONTROLLER (#22)
// Regenerate: ggen sync --rule rest-controller-test
package {{ javaPackage }};

import com.fasterxml.jackson.databind.ObjectMapper;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.MockMvc;

import java.util.List;
import java.util.Optional;
import java.util.UUID;

import static org.mockito.ArgumentMatchers.any;
import static org.mockito.ArgumentMatchers.eq;
import static org.mockito.Mockito.*;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.*;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;

/**
 * Slice test for {@link {{ className }}Controller}.
 *
 * Pattern: PATTERN-MATCHED CONTROLLER (Java 26 Pattern Language #22)
 *
 * @WebMvcTest loads only the web layer — controller, filters, exception handlers.
 * The service is replaced by @MockBean; no full application context is needed.
 * This keeps tests fast (< 2s) and focused on HTTP contract verification.
 */
@WebMvcTest({{ className }}Controller.class)
@DisplayName("{{ className }}Controller — REST contract tests")
class {{ className }}ControllerTest {

    @Autowired
    private MockMvc mockMvc;

    @Autowired
    private ObjectMapper objectMapper;

    @MockBean
    private {{ className }}Service service;

    // ── GET /api/{{ className | lower }}s ─────────────────────────────────────────────

    @Test
    @DisplayName("GET /api/{{ className | lower }}s returns 200 with list")
    void getAll_returns200WithList() throws Exception {
        // Arrange
        List<{{ className }}> entities = List.of(new {{ className }}(), new {{ className }}());
        when(service.findAll()).thenReturn(entities);

        // Act & Assert
        mockMvc.perform(get("/api/{{ className | lower }}s")
                .accept(MediaType.APPLICATION_JSON))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.length()").value(2));

        verify(service).findAll();
    }

    // ── GET /api/{{ className | lower }}s/{id} ────────────────────────────────────────

    @Test
    @DisplayName("GET /api/{{ className | lower }}s/{id} returns 200 when found")
    void getById_returns200_whenFound() throws Exception {
        // Arrange
        UUID id = UUID.randomUUID();
        {{ className }} entity = new {{ className }}();
        when(service.findById(id)).thenReturn(Optional.of(entity));

        // Act & Assert
        mockMvc.perform(get("/api/{{ className | lower }}s/{id}", id)
                .accept(MediaType.APPLICATION_JSON))
            .andExpect(status().isOk());

        verify(service).findById(id);
    }

    @Test
    @DisplayName("GET /api/{{ className | lower }}s/{id} returns 404 when not found")
    void getById_returns404_whenNotFound() throws Exception {
        // Arrange
        UUID id = UUID.randomUUID();
        when(service.findById(id)).thenReturn(Optional.empty());

        // Act & Assert
        mockMvc.perform(get("/api/{{ className | lower }}s/{id}", id)
                .accept(MediaType.APPLICATION_JSON))
            .andExpect(status().isNotFound());
    }

    // ── POST /api/{{ className | lower }}s ────────────────────────────────────────────

    @Test
    @DisplayName("POST /api/{{ className | lower }}s returns 200 with created entity")
    void create_returns200_withCreatedEntity() throws Exception {
        // Arrange
        {{ className }} entity = new {{ className }}();
        when(service.create(any())).thenReturn(entity);

        String body = "{}";   // minimal JSON — customize per domain

        // Act & Assert
        mockMvc.perform(post("/api/{{ className | lower }}s")
                .contentType(MediaType.APPLICATION_JSON)
                .content(body))
            .andExpect(status().isOk());

        verify(service).create(any());
    }

    // ── DELETE /api/{{ className | lower }}s/{id} ─────────────────────────────────────

    @Test
    @DisplayName("DELETE /api/{{ className | lower }}s/{id} returns 204 on success")
    void delete_returns204_onSuccess() throws Exception {
        // Arrange
        UUID id = UUID.randomUUID();
        doNothing().when(service).delete(id);

        // Act & Assert
        mockMvc.perform(delete("/api/{{ className | lower }}s/{id}", id))
            .andExpect(status().isNoContent());

        verify(service).delete(id);
    }

    @Test
    @DisplayName("DELETE /api/{{ className | lower }}s/{id} returns 404 when entity not found")
    void delete_returns404_whenNotFound() throws Exception {
        // Arrange
        UUID id = UUID.randomUUID();
        doThrow(new jakarta.persistence.EntityNotFoundException("not found"))
            .when(service).delete(id);

        // Act & Assert
        // Requires @ControllerAdvice mapping EntityNotFoundException → 404
        // If not configured, assert 500 here and add the advice separately.
        mockMvc.perform(delete("/api/{{ className | lower }}s/{id}", id))
            .andExpect(status().isNotFound());
    }

    // ── GET /api/{{ className | lower }}s/{id}/status ─────────────────────────────────

    @Test
    @DisplayName("GET /api/{{ className | lower }}s/{id}/status returns 200 when entity exists")
    void getStatus_returns200_whenEntityExists() throws Exception {
        // Arrange
        UUID id = UUID.randomUUID();
        {{ className }} entity = new {{ className }}();
        when(service.findById(id)).thenReturn(Optional.of(entity));

        // Act & Assert
        mockMvc.perform(get("/api/{{ className | lower }}s/{id}/status", id)
                .accept(MediaType.APPLICATION_JSON))
            .andExpect(status().isOk());
    }

    @Test
    @DisplayName("GET /api/{{ className | lower }}s/{id}/status returns 404 when entity not found")
    void getStatus_returns404_whenNotFound() throws Exception {
        // Arrange
        UUID id = UUID.randomUUID();
        when(service.findById(id)).thenReturn(Optional.empty());

        // Act & Assert
        mockMvc.perform(get("/api/{{ className | lower }}s/{id}/status", id))
            .andExpect(status().isNotFound());
    }
}
