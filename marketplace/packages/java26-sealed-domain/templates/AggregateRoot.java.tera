// Generated by ggen — DO NOT EDIT
// Pattern: AGGREGATE ROOT (Java 26 Pattern Language #8)
// Source:  ontology/domain.ttl
// Regenerate: ggen sync --rule aggregate-roots
package {{ javaPackage }};

import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.GeneratedValue;
import jakarta.persistence.GenerationType;
import jakarta.persistence.Id;
import jakarta.persistence.Table;
{% set field_list = fields | split(pat=",") %}
{% set needs_instant = false %}
{% set needs_uuid = false %}
{% set needs_bigdecimal = false %}
{% set needs_localdate = false %}
{% for field_triple in field_list %}
{% set parts = field_triple | trim | split(pat="|") %}
{% if parts | length >= 2 %}
{% if parts[1] == "Instant" %}{% set_global needs_instant = true %}{% endif %}
{% if parts[1] == "UUID" %}{% set_global needs_uuid = true %}{% endif %}
{% if parts[1] == "BigDecimal" %}{% set_global needs_bigdecimal = true %}{% endif %}
{% if parts[1] == "LocalDate" %}{% set_global needs_localdate = true %}{% endif %}
{% endif %}
{% endfor %}
{% if needs_instant %}
import java.time.Instant;
{% endif %}
{% if needs_localdate %}
import java.time.LocalDate;
{% endif %}
{% if needs_uuid %}
import java.util.UUID;
{% endif %}
{% if needs_bigdecimal %}
import java.math.BigDecimal;
{% endif %}
import java.util.Objects;

/**
 * JPA entity: {@code {{ className }}} — Aggregate Root.
 *
 * <p>This class is the consistency boundary for the {@code {{ className }}} aggregate.
 * All state changes go through this object's methods; the repository persists
 * the result. Never bypass this class to mutate the database directly.
 *
 * <p>Generated from {@code ontology/domain.ttl} via the AGGREGATE ROOT pattern (#8).
 * To add a field, declare a {@code java26:Field} in the TTL and run {@code ggen sync}.
 *
 * @generated ggen {{ className }}
 */
@Entity
@Table(name = "{{ className | lower }}")
public class {{ className }} {

{% for field_triple in field_list %}
{% set parts = field_triple | trim | split(pat="|") %}
{% if parts | length >= 3 %}
{% if parts[2] == "true" %}
    @Id
    @GeneratedValue(strategy = GenerationType.UUID)
    @Column(name = "{{ parts[0] }}", updatable = false, nullable = false)
    private {{ parts[1] }} {{ parts[0] }};

{% else %}
    @Column(name = "{{ parts[0] }}", nullable = false)
    private {{ parts[1] }} {{ parts[0] }};

{% endif %}
{% endif %}
{% endfor %}
    // -------------------------------------------------------------------------
    // Constructors
    // -------------------------------------------------------------------------

    /** JPA no-args constructor. Do not use in application code. */
    protected {{ className }}() {}

    /** Full constructor. Validates required fields at construction time. */
    public {{ className }}(
{% for field_triple in field_list %}
{% set parts = field_triple | trim | split(pat="|") %}
{% if parts | length >= 2 %}
            {{ parts[1] }} {{ parts[0] }}{% if not loop.last %},{% endif %}

{% endif %}
{% endfor %}
    ) {
{% for field_triple in field_list %}
{% set parts = field_triple | trim | split(pat="|") %}
{% if parts | length >= 2 %}
        this.{{ parts[0] }} = Objects.requireNonNull({{ parts[0] }}, "{{ parts[0] }} must not be null");
{% endif %}
{% endfor %}
    }

    // -------------------------------------------------------------------------
    // Accessors (read-only by convention — mutate via domain methods)
    // -------------------------------------------------------------------------

{% for field_triple in field_list %}
{% set parts = field_triple | trim | split(pat="|") %}
{% if parts | length >= 2 %}
    /** Returns the value of {@code {{ parts[0] }}}. */
    public {{ parts[1] }} get{{ parts[0] | capitalize }}() {
        return {{ parts[0] }};
    }

{% endif %}
{% endfor %}
    // -------------------------------------------------------------------------
    // Domain methods — add state-transition logic here, not in the service
    // -------------------------------------------------------------------------
    // Example:
    //   public void transitionTo(NewStatus next) {
    //       validate transition from this.status to next;
    //       this.status = next;
    //   }

    // -------------------------------------------------------------------------
    // equals / hashCode — identity-based (JPA standard)
    // -------------------------------------------------------------------------

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (!(o instanceof {{ className }} other)) return false;
{% set first_id = "" %}
{% for field_triple in field_list %}
{% set parts = field_triple | trim | split(pat="|") %}
{% if parts | length >= 3 and parts[2] == "true" and first_id == "" %}
{% set_global first_id = parts[0] %}
{% endif %}
{% endfor %}
{% if first_id != "" %}
        return Objects.equals({{ first_id }}, other.{{ first_id }});
{% else %}
        return Objects.equals(this.toString(), other.toString());
{% endif %}
    }

    @Override
    public int hashCode() {
{% if first_id != "" %}
        return Objects.hashCode({{ first_id }});
{% else %}
        return Objects.hash(
{% for field_triple in field_list %}
{% set parts = field_triple | trim | split(pat="|") %}
{% if parts | length >= 2 %}
                {{ parts[0] }}{% if not loop.last %}, {% endif %}
{% endif %}
{% endfor %}
        );
{% endif %}
    }

    @Override
    public String toString() {
        return "{{ className }}{" +
{% for field_triple in field_list %}
{% set parts = field_triple | trim | split(pat="|") %}
{% if parts | length >= 2 %}
                "{{ parts[0] }}=" + {{ parts[0] }}{% if not loop.last %} + ", " +{% endif %}

{% endif %}
{% endfor %}
                "}";
    }
}
