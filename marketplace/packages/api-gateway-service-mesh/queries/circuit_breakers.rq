PREFIX agw: <https://mcpp.ai/ontology/api-gateway#>
SELECT ?route ?failureThreshold ?timeout WHERE {
  ?route agw:hasCircuitBreaker ?cb .
  ?cb agw:failureThreshold ?failureThreshold ;
      agw:timeoutSeconds ?timeout .
}
