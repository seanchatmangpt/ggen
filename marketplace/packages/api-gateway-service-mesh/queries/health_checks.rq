PREFIX agw: <https://mcpp.ai/ontology/api-gateway#>
SELECT ?service ?interval ?path ?failureThreshold WHERE {
  ?service agw:hasHealthCheck ?hc .
  ?hc agw:healthCheckInterval ?interval ;
      agw:healthCheckPath ?path ;
      agw:failureThresholdHealth ?failureThreshold .
}
