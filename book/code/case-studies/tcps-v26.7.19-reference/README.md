# TCPS v26.7.19 reference (pointer)

Canonical copy: `packs/tcps-core-pack/reference/製品版/` at the repo root — all 132 files were verified byte-identical (sha256, full set via the packs' `source-manifest.json`) to the book bundle's copy before it was dropped here to avoid a second drifting tree.
