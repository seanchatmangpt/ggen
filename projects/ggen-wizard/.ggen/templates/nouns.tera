{# =============================================================================
   ggen Wizard CLI - Nouns JSON Export
   Generated from clap-noun-verb RDF specification
   ============================================================================= #}
{
  "nouns": [
{% for row in sparql_results %}
    {
      "name": "{{ row["?name"] }}",
      "description": "{{ row["?description"] | default(value=row["?name"]) }}"
    }{% if not loop.last %},{% endif %}
{% endfor %}
  ]
}
