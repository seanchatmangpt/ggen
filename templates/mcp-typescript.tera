// =============================================================================
// MCP Server: {{ server_name }} v{{ server_version }}
// =============================================================================
//
// Generated by ggen from {{ server_name }} ontology
// Pipeline: ontology (.ttl) -> SPARQL-star CONSTRUCT (.rq) -> Tera (.tera) -> this file
// Core axiom: A = mu(O), mu o mu = mu, hash(A) = hash(mu(O)), O |= Sigma
//
// {{ server_description }}
//
// Transport: {{ transport_type | default(value="stdio") }}
// Tools: {{ tools | length }}
// =============================================================================

import { Server } from "@modelcontextprotocol/sdk/server/index.js";
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";
{% if transport_type == "sse" %}
import { SSEServerTransport } from "@modelcontextprotocol/sdk/server/sse.js";
import express from "express";
{% endif %}
import {
  CallToolRequestSchema,
  ListToolsRequestSchema,
} from "@modelcontextprotocol/sdk/types.js";
import { z } from "zod";
import { zodToJsonSchema } from "zod-to-json-schema";

// =============================================================================
// Zod Input Schemas
// =============================================================================

{% for tool in tools %}
/**
 * Input schema for {{ tool.name }}
 * {{ tool.description }}
 */
const {{ tool.name | snake }}InputSchema = z.object({
{% for param in tool.parameters %}
  {{ param.name }}: {% if param.json_schema %}z.any(){% else %}{{ param.zod_type }}(){% endif %}{% if param.is_required != "true" %}.optional(){% endif %}{% if param.description %}.describe("{{ param.description }}"){% endif %}{% if param.default_value %}.default({{ param.default_value }}){% endif %}{% if param.enum_values %},
  // NOTE: enum constraint from ontology: {{ param.enum_values }}
  // Apply via .pipe(z.enum([...])) if runtime validation needed{% endif %},
{% endfor %}
});

type {{ tool.name | pascal }}Input = z.infer<typeof {{ tool.name | snake }}InputSchema>;

{% endfor %}

// =============================================================================
// Server Setup
// =============================================================================

const server = new Server(
  {
    name: "{{ server_name }}",
    version: "{{ server_version }}",
  },
  {
    capabilities: {
      tools: {
        listChanged: false,
      },
    },
  }
);

// =============================================================================
// Tool Handlers
// =============================================================================

{% for tool in tools %}
/**
 * Handle {{ tool.name }}
 * {{ tool.description }}
 */
async function handle{{ tool.name | pascal }}(
  args: Record<string, unknown>
): Promise<{ content: Array<{ type: string; text: string }>; isError?: boolean }> {
  const parsed = {{ tool.name | snake }}InputSchema.safeParse(args);

  if (!parsed.success) {
    return {
      content: [
        {
          type: "text",
          text: `Invalid arguments for {{ tool.name }}: ${parsed.error.message}`,
        },
      ],
      isError: true,
    };
  }

  const input = parsed.data as {{ tool.name | pascal }}Input;

  // TODO: Implement {{ tool.name }} logic here
  // Parameters available:
  {% for param in tool.parameters %}
  // - {{ param.name }}: {{ param.ts_type }}{% if param.description %} -- {{ param.description }}{% endif %}
  {% endfor %}

  return {
    content: [
      {
        type: "text",
        text: JSON.stringify({ success: true, tool: "{{ tool.name }}", input }, null, 2),
      },
    ],
  };
}

{% endfor %}

// =============================================================================
// Tool Registration
// =============================================================================

server.setRequestHandler(ListToolsRequestSchema, async () => {
  return {
    tools: [
{% for tool in tools %}
      {
        name: "{{ tool.name }}",
        description: "{{ tool.description }}",
        inputSchema: {% if tool.parameters | length > 0 %}zodToJsonSchema({{ tool.name | snake }}InputSchema, { name: "{{ tool.name }}Input" }){% else %}{
  type: "object",
  properties: {},
}{% endif %},
      },
{% endfor %}
    ],
  };
});

server.setRequestHandler(CallToolRequestSchema, async (request) => {
  const { name, arguments: args } = request.params;

  try {
    switch (name) {
{% for tool in tools %}
      case "{{ tool.name }}":
        return await handle{{ tool.name | pascal }}(args ?? {});
{% endfor %}
      default:
        return {
          content: [
            {
              type: "text",
              text: `Unknown tool: ${name}`,
            },
          ],
          isError: true,
        };
    }
  } catch (error) {
    const message = error instanceof Error ? error.message : String(error);
    return {
      content: [
        {
          type: "text",
          text: `Error executing ${name}: ${message}`,
        },
      ],
      isError: true,
    };
  }
});

// =============================================================================
// Transport & Startup
// =============================================================================

{% if transport_type == "sse" %}
// HTTP SSE Transport (for remote clients)
const app = express();
app.get("/sse", async (_req, res) => {
  const transport = new SSEServerTransport("/messages", res);
  await server.connect(transport);
});
app.post("/messages", async (req, res) => {
  // SSEServerTransport handles message routing internally
  res.sendStatus(200);
});

async function main() {
  const port = process.env.MCP_PORT ? parseInt(process.env.MCP_PORT, 10) : 3000;
  app.listen(port, () => {
    console.error(`{{ server_name }} MCP server listening on http://localhost:${port}/sse`);
  });
}
{% else %}
// Stdio Transport (default -- for local CLI clients)
async function main() {
  const transport = new StdioServerTransport();
  await server.connect(transport);
  console.error(`{{ server_name }} MCP server v{{ server_version }} running on stdio`);
}

// Uncomment below for HTTP transport alternative:
//
// import { StreamableHTTPServerTransport } from "@modelcontextprotocol/sdk/server/streamableHttp.js";
// import express from "express";
//
// async function main() {
//   const app = express();
//   app.post("/mcp", async (req, res) => {
//     const transport = new StreamableHTTPServerTransport({ sessionIdGenerator: () => crypto.randomUUID() });
//     await server.connect(transport);
//     // Handle the request via transport
//   });
//   const port = process.env.MCP_PORT ? parseInt(process.env.MCP_PORT, 10) : 3000;
//   app.listen(port, () => {
//     console.error(`{{ server_name }} MCP server listening on http://localhost:${port}/mcp`);
//   });
// }
{% endif %}

main().catch((error) => {
  console.error("Fatal error in {{ server_name }} MCP server:", error);
  process.exit(1);
});
