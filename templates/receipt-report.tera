{# receipt-report.tera: Generate provenance receipt chain report #}
{# Constitutional Equation: receipt-report.md = mu(provenance-chain.ttl) #}
{# Input: SPARQL CONSTRUCT results from provenance-chain-extract.rq #}
{# Output: Markdown report showing artifact provenance chain integrity #}

# Provenance Report -- Auto-generated by ggen

**Generated**: {{ generated_date | default(value=now() | date(format="%Y-%m-%d %H:%M:%S")) }}
**Generator**: {{ generator | default(value="ggen v2.0.0") }}
**Source Ontology**: {{ source_ontology | default(value="https://ggen.dev/ontology/provenance") }}

---

## Chain Summary

| Metric | Value |
|--------|-------|
| **Total Receipts** | {{ receipts | length }} |
{% set verified_count = receipts | filter(attribute="verification", value="verified") | length %}
{% set failed_count = receipts | filter(attribute="verification", value="failed") | length %}
{% set unverified_count = receipts | filter(attribute="verification", value="unverified") | length %}
| **Verified** | {{ verified_count }} |
| **Unverified** | {{ unverified_count }} |
| **Failed** | {{ failed_count }} |
| **Chain Integrity** | {% if chain_integrity %}INTACT{% else %}BROKEN{% endif %} |

{% if chain_integrity %}
> **CHAIN INTACT**: All parent-child receipt links are consistent. No tampering detected.
{% else %}
> **CHAIN BROKEN**: One or more parent receipt references do not match expected hashes. Investigation required.
{% endif %}

---

## Receipt Details

{% for receipt in receipts %}

### Receipt {{ loop.index }}: {{ receipt.artifactType | upper }}

**Source Triple:**
```
{{ receipt.sourceTriple }}
```

| Field | Value |
|-------|-------|
| **Source Entity** | `{{ receipt.sourceEntity | replace(from="https://ggen.dev/examples/provenance#", to="ex:") | replace(from="http://www.w3.org/1999/02/22-rdf-syntax-ns#", to="rdf:") | replace(from="http://www.w3.org/2002/07/owl#", to="owl:") }}` |
| **Source Predicate** | `{{ receipt.sourcePredicate | replace(from="https://ggen.dev/examples/provenance#", to="ex:") | replace(from="http://www.w3.org/1999/02/22-rdf-syntax-ns#", to="rdf:") | replace(from="http://www.w3.org/2002/07/owl#", to="owl:") | replace(from="https://ggen.dev/ontology/dod/", to="dod:") }}` |
| **Source Object** | `{{ receipt.sourceObject | replace(from="https://ggen.dev/examples/provenance#", to="ex:") | replace(from="http://www.w3.org/1999/02/22-rdf-syntax-ns#", to="rdf:") | replace(from="http://www.w3.org/2002/07/owl#", to="owl:") }}` |

**Generation Metadata:**

| Field | Value |
|-------|-------|
| **Commit** | ``{{ receipt.commit | truncate(length=12, end="...") }}`` |
| **Generator** | {{ receipt.generator | default(value="ggen") }} |
| **Template** | `{{ receipt.template | default(value="N/A") }}` |
| **Query** | `{{ receipt.query | default(value="N/A") }}` |
| **Ontology** | `{{ receipt.ontology | default(value="N/A") }}` |
| **Timestamp** | {{ receipt.timestamp | default(value="N/A") }} |

**Artifact Info:**

| Field | Value |
|-------|-------|
| **Path** | `{{ receipt.artifactPath }}` |
| **Type** | {{ receipt.artifactType }} |
| **Size** | {{ receipt.artifactSize | default(value="N/A") }} bytes |
| **Verification** | {% if receipt.verification == "verified" %}PASS{% elif receipt.verification == "failed" %}FAIL{% else %}UNKNOWN{% endif %} |

**Receipt Hash:** ``{{ receipt.hash | truncate(length=16, end="...") }}``
{% if receipt.parentHash %}
**Parent Hash:** ``{{ receipt.parentHash | truncate(length=16, end="...") }}``
{% else %}
**Parent Hash:** NONE (genesis receipt)
{% endif %}

{% if not loop.last %}
---
{% endif %}
{% endfor %}

---

## Verification Summary

| Status | Count | Percentage |
|--------|-------|------------|
| **Verified** | {{ verified_count }} | {% if receipts | length > 0 %}{{ "%.1f" | format(verified_count * 100 / (receipts | length)) }}%{% else %}N/A{% endif %} |
| **Unverified** | {{ unverified_count }} | {% if receipts | length > 0 %}{{ "%.1f" | format(unverified_count * 100 / (receipts | length)) }}%{% else %}N/A{% endif %} |
| **Failed** | {{ failed_count }} | {% if receipts | length > 0 %}{{ "%.1f" | format(failed_count * 100 / (receipts | length)) }}%{% else %}N/A{% endif %} |
| **Total** | {{ receipts | length }} | 100.0% |

### Verification Recommendations

{% if failed_count > 0 %}
1. **Investigate Failed Receipts**: {{ failed_count }} artifact(s) failed verification. Review the artifact path and re-run verification.
{% endif %}

{% if unverified_count > 0 %}
1. **Verify Unverified Artifacts**: {{ unverified_count }} artifact(s) have not been verified. Run the appropriate verification commands:
{% for receipt in receipts | filter(attribute="verification", value="unverified") %}
   - `{{ receipt.artifactPath }}` ({{ receipt.artifactType }})
{% endfor %}
{% endif %}

{% if verified_count == receipts | length and receipts | length > 0 %}
All artifacts are verified. The provenance chain is fully validated.
{% endif %}

---

## Chain Integrity Analysis

{% set hashes_seen = [] %}
{% set integrity_broken = false %}
{% for receipt in receipts %}
{% if receipt.parentHash %}
{% set parent_found = false %}
{% for earlier in receipts %}
{% if earlier.hash == receipt.parentHash %}
{% set _ = parent_found %}
{% set parent_found = true %}
{% endif %}
{% endfor %}
{% if not parent_found %}
{% set _ = integrity_broken %}
{% set integrity_broken = true %}
- **BROKEN LINK**: Receipt {{ loop.index }} references parent hash ``{{ receipt.parentHash | truncate(length=16, end="...") }}`` but no receipt with that hash exists in the chain.
{% endif %}
{% endif %}
{% endfor %}

{% if not integrity_broken %}
All parent-child links in the chain are valid. Each receipt correctly references the hash of the preceding receipt.
{% endif %}

---

*This report was generated by [ggen](https://github.com/nicepkg/ggen) from RDF 1.2 provenance chain data.*
*Methodology: SPARQL-star CONSTRUCT extraction + triple term annotation + SHA-256 receipt hashing.*
*Last updated: {{ now() | date(format="%Y-%m-%d") }}*
