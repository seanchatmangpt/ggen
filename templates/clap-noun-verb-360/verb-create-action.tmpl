---
to: "src/verbs/{{ verb | snake_case }}_verb.rs"
vars:
  verb: "CreateVerb"
  action: "Create_LOWER"
  description: "Create verb for noun operations"
---

use crate::CommandResult;

/// Create verb for noun-verb operations
pub struct CreateVerb;

impl CreateVerb {
    /// Execute Create action on a noun
    pub async fn execute(noun: &str) -> CommandResult<String> {
        Ok(format!("Create_LOWER({})", noun))
    }

    /// Validate Create preconditions
    pub fn validate(&self) -> CommandResult<()> {
        Ok(())
    }

    /// Describe what Create does
    pub fn describe() -> &'static str {
        "Create operation"
    }
}
