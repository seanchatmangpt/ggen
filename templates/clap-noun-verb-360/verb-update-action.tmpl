---
to: "src/verbs/{{ verb | snake_case }}_verb.rs"
vars:
  verb: "UpdateVerb"
  action: "Update_LOWER"
  description: "Update verb for noun operations"
---

use crate::CommandResult;

/// Update verb for noun-verb operations
pub struct UpdateVerb;

impl UpdateVerb {
    /// Execute Update action on a noun
    pub async fn execute(noun: &str) -> CommandResult<String> {
        Ok(format!("Update_LOWER({})", noun))
    }

    /// Validate Update preconditions
    pub fn validate(&self) -> CommandResult<()> {
        Ok(())
    }

    /// Describe what Update does
    pub fn describe() -> &'static str {
        "Update operation"
    }
}
