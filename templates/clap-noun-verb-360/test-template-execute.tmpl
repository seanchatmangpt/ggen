---
to: "tests/integration/test_{{ noun | snake_case }}_{{ verb | snake_case }}.rs"
vars:
  noun: "Template"
  verb: "Execute"
---

#[tokio::test]
async fn test_Template_LOWER_Execute_LOWER() {
    // Setup
    let noun = "Template";
    let verb = "Execute";

    // Execute
    let result = execute_noun_verb(noun, verb).await;

    // Assert
    assert!(result.is_ok());
}

#[tokio::test]
async fn test_Template_LOWER_Execute_LOWER_invalid_input() {
    // Test with invalid input
    let result = execute_noun_verb("", "Execute").await;
    assert!(result.is_err());
}

async fn execute_noun_verb(noun: &str, verb: &str) -> Result<String, Box<dyn std::error::Error>> {
    Ok(format!("{}({})", verb, noun))
}
