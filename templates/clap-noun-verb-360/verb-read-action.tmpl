---
to: "src/verbs/{{ verb | snake_case }}_verb.rs"
vars:
  verb: "ReadVerb"
  action: "Read_LOWER"
  description: "Read verb for noun operations"
---

use crate::CommandResult;

/// Read verb for noun-verb operations
pub struct ReadVerb;

impl ReadVerb {
    /// Execute Read action on a noun
    pub async fn execute(noun: &str) -> CommandResult<String> {
        Ok(format!("Read_LOWER({})", noun))
    }

    /// Validate Read preconditions
    pub fn validate(&self) -> CommandResult<()> {
        Ok(())
    }

    /// Describe what Read does
    pub fn describe() -> &'static str {
        "Read operation"
    }
}
