---
to: "src/verbs/{{ verb | snake_case }}_verb.rs"
vars:
  verb: "ListVerb"
  action: "List_LOWER"
  description: "List verb for noun operations"
---

use crate::CommandResult;

/// List verb for noun-verb operations
pub struct ListVerb;

impl ListVerb {
    /// Execute List action on a noun
    pub async fn execute(noun: &str) -> CommandResult<String> {
        Ok(format!("List_LOWER({})", noun))
    }

    /// Validate List preconditions
    pub fn validate(&self) -> CommandResult<()> {
        Ok(())
    }

    /// Describe what List does
    pub fn describe() -> &'static str {
        "List operation"
    }
}
