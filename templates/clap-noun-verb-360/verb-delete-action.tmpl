---
to: "src/verbs/{{ verb | snake_case }}_verb.rs"
vars:
  verb: "DeleteVerb"
  action: "Delete_LOWER"
  description: "Delete verb for noun operations"
---

use crate::CommandResult;

/// Delete verb for noun-verb operations
pub struct DeleteVerb;

impl DeleteVerb {
    /// Execute Delete action on a noun
    pub async fn execute(noun: &str) -> CommandResult<String> {
        Ok(format!("Delete_LOWER({})", noun))
    }

    /// Validate Delete preconditions
    pub fn validate(&self) -> CommandResult<()> {
        Ok(())
    }

    /// Describe what Delete does
    pub fn describe() -> &'static str {
        "Delete operation"
    }
}
