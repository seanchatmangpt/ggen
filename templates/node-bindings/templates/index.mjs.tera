{# =============================================================================
   ES MODULE WRAPPER TEMPLATE - Generates ESM wrapper with JSDoc
   Uses sparql_results (keys have ? prefix in the array)
   ============================================================================= #}
/**
 * ggen-node - Node.js bindings (ESM)
 * Generated from RDF ontology - DO NOT EDIT MANUALLY
 *
 * @module ggen-node
 * @version 1.0.0
 */

import { createRequire } from 'module';
const require = createRequire(import.meta.url);

let native;
try {
  native = require('../../native/index.node');
} catch (err) {
  throw new Error(`Failed to load native addon: ${err.message}`);
}

// =============================================================================
// Exported Functions with JSDoc (Generated from SPARQL Results)
// =============================================================================

{%- for row in sparql_results %}

/**
 * {{ row["?description"] }}
 * @returns {{'{'}}{% if row["?async"] == "true" %}Promise<{{ row["?returnType"] }}>{% else %}{{ row["?returnType"] }}{% endif %}}
 {% if row["?throws"] == "true" %}* @throws {{'{'}}Error}{% endif %}
 */
export const {{ row["?funcName"] }} = native.{{ row["?funcName"] }};
{% endfor %}

// =============================================================================
// Default Export
// =============================================================================

/**
 * All exported functions
 * @type {{'{'}}Object}
 */
export default {
{%- for row in sparql_results %}
  {{ row["?funcName"] }},
{%- endfor %}
};
