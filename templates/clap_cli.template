// {{ name }} - CLI Definition
// Generated by ggen v2.2.0

use clap::{Parser, Subcommand};

#[derive(Parser, Debug)]
#[command(name = "{{ app_name }}")]
#[command(version = "{{ version }}")]
#[command(about = "{{ app_name }} - A clap-noun-verb CLI", long_about = None)]
pub struct Cli {
    #[command(subcommand)]
    pub command: Commands,
}

#[derive(Subcommand, Debug)]
pub enum Commands {
    /// Initialize a new project
    Init {
        /// Project name
        #[arg(short, long)]
        name: String,
    },

    /// Build the project
    Build {
        /// Release mode
        #[arg(short, long)]
        release: bool,
    },

    /// Run the project
    Run {
        /// Arguments to pass
        args: Vec<String>,
    },
}
