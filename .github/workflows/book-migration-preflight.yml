name: Book Migration Preflight

on:
  pull_request:
    paths:
      - 'book/scripts/**'
      - 'book/src/**'
      - 'packs/level-five-book-pack/**'
      - '.github/workflows/book-migration-preflight.yml'

permissions:
  contents: read

jobs:
  preflight:
    name: Complete book source-law migration preflight
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Quiet shallow clone and run migration
        run: |
          set -euo pipefail
          git clone --quiet --depth=1 \
            --branch agent/post-agi-docs-core-team \
            https://github.com/seanchatmangpt/ggen.git candidate
          cd candidate
          python3 book/scripts/migrate_full_book_to_aligned_pack_law.py
          python3 book/scripts/check_book.py
