---
to: src/価値流.rs
force: true
sparql:
  rows: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-core#>
    SELECT ?source WHERE { tcps:価値流Module tcps:sourceText ?source } ORDER BY ?source
---
{{ rows[0].source | trim_end }}
