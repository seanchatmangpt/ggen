---
to: src/ムリムラムダ.rs
force: true
sparql:
  rows: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-core#>
    SELECT ?source WHERE { tcps:ムリムラムダModule tcps:sourceText ?source } ORDER BY ?source
---
{{ rows[0].source }}
