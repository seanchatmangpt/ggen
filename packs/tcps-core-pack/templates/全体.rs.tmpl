---
to: src/全体.rs
force: true
sparql:
  rows: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-core#>
    SELECT ?source WHERE { tcps:全体Module tcps:sourceText ?source } ORDER BY ?source
---
{{ rows[0].source }}
