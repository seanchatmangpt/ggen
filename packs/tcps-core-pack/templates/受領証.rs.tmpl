---
to: src/受領証.rs
force: true
sparql:
  rows: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-core#>
    SELECT ?source WHERE { tcps:受領証Module tcps:sourceText ?source } ORDER BY ?source
---
{{ rows[0].source }}
