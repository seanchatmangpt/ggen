---
to: src/五回なぜ.rs
force: true
sparql:
  rows: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-core#>
    SELECT ?source WHERE { tcps:五回なぜModule tcps:sourceText ?source } ORDER BY ?source
---
{{ rows[0].source }}
