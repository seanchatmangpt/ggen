---
to: src/人間中心.rs
force: true
sparql:
  rows: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-core#>
    SELECT ?source WHERE { tcps:人間中心Module tcps:sourceText ?source } ORDER BY ?source
---
{{ rows[0].source }}
