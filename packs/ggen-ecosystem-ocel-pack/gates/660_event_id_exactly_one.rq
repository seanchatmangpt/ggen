# MESSAGE: each ManufacturingEvent must have exactly one eventId.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?event (COUNT(?v) AS ?count) WHERE { ?event a geocel:ManufacturingEvent . OPTIONAL { ?event geocel:eventId ?v } } GROUP BY ?event HAVING(COUNT(?v) != 1)
