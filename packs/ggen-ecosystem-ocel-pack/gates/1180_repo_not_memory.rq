# MESSAGE: RepositoryObject and ProjectMemoryObject identities must remain disjoint.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?x WHERE { ?x a geocel:RepositoryObject ; a geocel:ProjectMemoryObject . }
