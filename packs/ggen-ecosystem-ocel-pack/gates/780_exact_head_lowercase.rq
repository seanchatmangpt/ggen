# MESSAGE: exactHead must use canonical lowercase hexadecimal.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object ?v WHERE { ?object a geocel:RepositoryObject ; geocel:exactHead ?v . FILTER(!REGEX(STR(?v), "^[0-9a-f]{40}$")) }
