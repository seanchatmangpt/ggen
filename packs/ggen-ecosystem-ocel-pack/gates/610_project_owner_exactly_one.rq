# MESSAGE: each ManufacturingRun must have exactly one projectOwner.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?run (COUNT(?v) AS ?count) WHERE { ?run a geocel:ManufacturingRun . OPTIONAL { ?run geocel:projectOwner ?v } } GROUP BY ?run HAVING(COUNT(?v) != 1)
