# MESSAGE: each ManufacturingEvent must have exactly one eventType.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?event (COUNT(?v) AS ?count) WHERE { ?event a geocel:ManufacturingEvent . OPTIONAL { ?event geocel:eventType ?v } } GROUP BY ?event HAVING(COUNT(?v) != 1)
