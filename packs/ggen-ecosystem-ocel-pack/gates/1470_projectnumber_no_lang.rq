# MESSAGE: projectNumber literals must not carry language tags.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?s ?v WHERE { ?s geocel:projectNumber ?v . FILTER(isLiteral(?v) && lang(?v) != "") }
