# MESSAGE: ocelDigest literals must not carry language tags.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?s ?v WHERE { ?s geocel:ocelDigest ?v . FILTER(isLiteral(?v) && lang(?v) != "") }
