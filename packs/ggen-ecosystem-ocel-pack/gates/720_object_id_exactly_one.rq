# MESSAGE: each ManufacturingObject must have exactly one objectId.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object (COUNT(?v) AS ?count) WHERE { ?object a geocel:ManufacturingObject . OPTIONAL { ?object geocel:objectId ?v } } GROUP BY ?object HAVING(COUNT(?v) != 1)
