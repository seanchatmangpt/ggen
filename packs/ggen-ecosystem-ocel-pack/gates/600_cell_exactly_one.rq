# MESSAGE: each ManufacturingRun must have exactly one cell.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?run (COUNT(?v) AS ?count) WHERE { ?run a geocel:ManufacturingRun . OPTIONAL { ?run geocel:cell ?v } } GROUP BY ?run HAVING(COUNT(?v) != 1)
