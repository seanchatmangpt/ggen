# MESSAGE: Realize must retain canonical prefLabel "realize".
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
SELECT * WHERE { FILTER NOT EXISTS { geocel:Realize skos:prefLabel "realize" } }
