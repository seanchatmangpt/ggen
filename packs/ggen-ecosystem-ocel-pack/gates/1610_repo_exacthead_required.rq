# MESSAGE: every RepositoryObject must carry an exactHead binding.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object WHERE { ?object a geocel:RepositoryObject . FILTER NOT EXISTS { ?object geocel:exactHead ?head } }
