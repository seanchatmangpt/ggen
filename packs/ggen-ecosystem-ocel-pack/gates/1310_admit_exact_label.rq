# MESSAGE: Admit must retain canonical prefLabel "admit".
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
SELECT * WHERE { FILTER NOT EXISTS { geocel:Admit skos:prefLabel "admit" } }
