# MESSAGE: each QualificationObject must identify exactly one qualification.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object (COUNT(?v) AS ?count) WHERE { ?object a geocel:QualificationObject . OPTIONAL { ?object geocel:qualification ?v } } GROUP BY ?object HAVING(COUNT(?v) != 1)
