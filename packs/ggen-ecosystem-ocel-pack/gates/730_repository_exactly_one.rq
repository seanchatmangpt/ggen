# MESSAGE: each RepositoryObject must name exactly one repository.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object (COUNT(?v) AS ?count) WHERE { ?object a geocel:RepositoryObject . OPTIONAL { ?object geocel:repository ?v } } GROUP BY ?object HAVING(COUNT(?v) != 1)
