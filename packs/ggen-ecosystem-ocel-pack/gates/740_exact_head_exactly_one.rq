# MESSAGE: each RepositoryObject must bind exactly one exactHead.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object (COUNT(?v) AS ?count) WHERE { ?object a geocel:RepositoryObject . OPTIONAL { ?object geocel:exactHead ?v } } GROUP BY ?object HAVING(COUNT(?v) != 1)
