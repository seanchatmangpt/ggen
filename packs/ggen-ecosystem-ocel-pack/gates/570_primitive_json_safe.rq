# MESSAGE: primitive identity must survive deterministic receipt and JSON projection unchanged.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object ?value WHERE {
  ?object a geocel:GgenPrimitiveObject ; geocel:primitive ?value .
  FILTER(REGEX(STR(?value), "[\\x00-\\x1F]"))
}
