# MESSAGE: eventType values must be RDF resources, never literals.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?e ?t WHERE { ?e geocel:eventType ?t . FILTER(isLiteral(?t)) }
