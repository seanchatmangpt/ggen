# MESSAGE: eventId must remain declared with ManufacturingEvent domain.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
SELECT * WHERE { FILTER NOT EXISTS { geocel:eventId rdfs:domain geocel:ManufacturingEvent } }
