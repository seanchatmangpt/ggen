# MESSAGE: eventId must be an opaque token without whitespace.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?event ?v WHERE { ?event a geocel:ManufacturingEvent ; geocel:eventId ?v . FILTER(REGEX(STR(?v), "\\s")) }
