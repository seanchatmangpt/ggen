# MESSAGE: Select must retain canonical prefLabel "select".
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
SELECT * WHERE { FILTER NOT EXISTS { geocel:Select skos:prefLabel "select" } }
