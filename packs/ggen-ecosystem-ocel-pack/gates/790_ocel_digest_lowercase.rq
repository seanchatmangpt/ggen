# MESSAGE: ocelDigest must use canonical lowercase SHA-256 hexadecimal.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?run ?v WHERE { ?run a geocel:ManufacturingRun ; geocel:ocelDigest ?v . FILTER(!REGEX(STR(?v), "^[0-9a-f]{64}$")) }
