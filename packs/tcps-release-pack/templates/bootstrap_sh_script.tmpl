---
to: infra/scripts/bootstrap.sh
force: true
sparql:
  row: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-release#>
    SELECT ?body WHERE {
      tcps:BootstrapShScript tcps:artifactBody ?body .
    } ORDER BY ?body
---
{{ row[0].body }}