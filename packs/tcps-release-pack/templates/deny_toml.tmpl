---
to: infra/deny.toml
force: true
sparql:
  row: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-release#>
    SELECT ?body WHERE {
      tcps:DenyTomlArtifact tcps:artifactBody ?body .
    } ORDER BY ?body
---
{{ row[0].body }}
