---
to: infra/targets/target-matrix-snapshot.json
force: true
sparql:
  row: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-release#>
    SELECT ?body WHERE {
      tcps:TargetMatrixSnapshotArtifact tcps:artifactBody ?body .
    } ORDER BY ?body
---
{{ row[0].body }}
