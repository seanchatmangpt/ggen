---
to: infra/azure-pipelines.yml
force: true
sparql:
  row: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-release#>
    SELECT ?body WHERE {
      tcps:AzurePipelinesConfig tcps:workflowBody ?body .
    } ORDER BY ?body
---
{{ row[0].body }}
