# Local self-host templates

This directory is intentionally empty of executable templates. All self-host projections are owned by `packs/ggen-self-host-pack`; the directory exists because the frontmatter consumer schema requires an explicit local template root.
