[package]
name = "cli"
version = "1.0.0"
authors = ["Sean Chatman <sean@example.com>"]
description = "CLI interface for rgen"
edition = "2021"

[dependencies]
utils = { path = "../utils" }
core = { path = "../core" }
clap_complete = "4.5.50"

[dependencies.clap]
version = "4.5.37"
features = ["cargo", "derive"]

[dev-dependencies]
assert_cmd = "2"
assert_fs = "1"
